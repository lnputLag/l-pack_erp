<FlowDocument xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:local="clr-namespace:Client.Reports.Preproduction.CuttingStamp"
              xmlns:xrd="clr-namespace:CodeReason.Reports.Document;assembly=CodeReason.Reports"
              PageHeight="29.7cm" PageWidth="21cm" ColumnWidth="21cm" FontFamily="Verdana" FontSize="18">

    <FlowDocument.Resources>
        <Style x:Key="HeaderTextStyle" TargetType="{x:Type Paragraph}">
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style x:Key="CellStyle" TargetType="{x:Type TableCell}">
            <Setter Property="Padding" Value="0.1cm,0.03cm,0.1cm,0.03cm"/>
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="BorderThickness" Value="0.02cm"/>
            <Setter Property="TextAlignment" Value="Left"/>
        </Style>
    </FlowDocument.Resources>
    
    <Section FontFamily="Arial" FontSize="12">
        <Paragraph>
            Штанцформы
        </Paragraph>

        <Table CellSpacing="0" BorderThickness="1" >
            <Table.Columns>
                <TableColumn Width="5*" />
                <TableColumn Width="3*" />
                <TableColumn Width="15*" />
            </Table.Columns>


            <TableRowGroup>
                <TableRow>
                    <TableCell Style="{StaticResource CellStyle}">
                        <Paragraph Style="{StaticResource HeaderTextStyle}" >
                            Плательщик
                        </Paragraph>
                    </TableCell>
                    <TableCell Style="{StaticResource CellStyle}">
                        <Paragraph Style="{StaticResource HeaderTextStyle}" >
                            Номер заказа
                        </Paragraph>
                    </TableCell>
                    <TableCell Style="{StaticResource CellStyle}">
                        <Paragraph Style="{StaticResource HeaderTextStyle}" >
                            Техкарта
                        </Paragraph>
                    </TableCell>
                </TableRow>
            </TableRowGroup>

            <TableRowGroup>
                <xrd:TableRowForDataTable TableName="Info">
                    <TableCell Style="{StaticResource CellStyle}">
                        <Paragraph TextAlignment="Left">
                            <xrd:InlineTableCellValue PropertyName="Payer"  />
                        </Paragraph>
                    </TableCell>
                    <TableCell Style="{StaticResource CellStyle}">
                        <Paragraph TextAlignment="Left">
                            <xrd:InlineTableCellValue PropertyName="OrderNum"  />
                        </Paragraph>
                    </TableCell>
                    <TableCell Style="{StaticResource CellStyle}">
                        <Paragraph TextAlignment="Left">
                            <xrd:InlineTableCellValue PropertyName="TechCard"  />
                        </Paragraph>
                    </TableCell>
                </xrd:TableRowForDataTable>
            </TableRowGroup>
        </Table>
    </Section>
    
</FlowDocument>
