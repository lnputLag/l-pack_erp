<controls:ControlBase x:Class="Client.Interfaces.Production.PilloryMonitor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Client.Interfaces.Production"
             xmlns:controls="clr-namespace:Client.Interfaces.Main"
             
             mc:Ignorable="d" 
             d:DesignWidth="1600" d:DesignHeight="800"  
            Style="{DynamicResource UserControlCanvas}"  >

    <UserControl.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{DynamicResource DataGridContainerHolder}">
        <Grid Style="{DynamicResource DataGridContainer}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Grid.Column="0" Style="{DynamicResource GridControlPanel}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="10*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" Name="MainGridToolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left">
                    <Button Name="RePlaceBlocks" Content="Разместить гриды"  Style="{DynamicResource Button}" Click="RePlaceBlocks_Click"/>
                    <controls:SelectBox x:Name="PageSelectBox" SelectedItemChanged="PageSelectBox_SelectedItemChanged"  Style="{DynamicResource CustomFormField}" ListBoxMinHeight="80" Width="100" Autocomplete="False"/>
                    <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                    
                    <Button Name="RefreshButton" Content="Обновить"  Style="{DynamicResource Button}" Click="RefreshButton_Click"/>
                    <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                    <Button Name="DowntimeButton" Content="Простои"  Style="{DynamicResource Button}" Click="DowntimeButton_Click"/>
                    <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                    <Button Name="ConditioningButton" Content="Кондиционирование"  Style="{DynamicResource Button}" Click="ConditioningButton_Click"/>
                    <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                    <Button Name="SubProductButton" Content="Перестил" ToolTip="Заказать перестил" Style="{DynamicResource Button}" Click="SubProductButton_Click"/>
                    <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                    <Button Name="PalletByPlaceButton" Content="Поддоны в ячейке" Click="PalletByPlaceButton_Click" Style="{DynamicResource Button}">
                        <Button.ContextMenu>
                            <ContextMenu Name="PalletByPlaceButtonMenu">
                            </ContextMenu>
                        </Button.ContextMenu>
                    </Button>
                    <StackPanel Orientation="Horizontal" x:Name="PalletByPlaceStackPanel">
                    </StackPanel>
                    <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                    <TextBlock x:Name="GlueStatus" Text="Низкий уровень клея" Visibility="Collapsed" Style="{DynamicResource PilloryTextBlockInfo}" Margin="0,0,5,0"/>
                    <TextBlock Name="FireStatus" Text="Сообщение о пожаре" Visibility="Collapsed" Style="{DynamicResource PilloryTextBlockAlarm}" Margin="0,0,5,0"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="1" Style="{DynamicResource FormHeaderButtonsPanel}"  HorizontalAlignment="Right" >
                    <Label Name="LoadingStatusLabel"/>
                    <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                    <Button Name="FireAlarmButton" MinWidth="60" Content="Пожар" Margin="0" Style="{DynamicResource ButtonFire}" Click="FireAlarmButton_Click" />
                    <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>

                    <Button Name="SettingsButton"    ToolTip="Настройка" Style="{DynamicResource ButtonGlyph}" Click="SettingsButton_Click"  Margin="0,0,0,0"  >
                        <Image Style="{DynamicResource SettingsImage}" />
                        <Button.ContextMenu>
                            <ContextMenu Name="BurgerMenu">
                                <MenuItem Header="Документация"                         Name="HelpButton"        Click="HelpButton_Click"   Background="#ffffff" />
                                <MenuItem Header="О программе"                          Name="InfoButton"        Click="InfoButton_Click"   Background="#ffffff" />
                                <MenuItem Header="Обновить"                             Name="RestartButton"    Click="RestartButton_Click"  Background="#ffffff" />
                                <MenuItem Header="Выход"                                Name="ExitButton"       Click="ExitButton_Click"  Background="#ffffff" />
                            </ContextMenu>
                        </Button.ContextMenu>
                    </Button>
                </StackPanel>
            </Grid>

            <Border Grid.Row="1" Grid.Column="0" Margin="0,0,0,5" Style="{DynamicResource FormVerticalPanelSpacer}"/>

            <Viewbox Grid.Row="2" Stretch="Uniform" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <!--UniformToFill-->
                <!--Uniform-->
                <Grid Width="1820" Height="625" Name="MainGrid" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                </Grid>
            </Viewbox>

            <Image Grid.Row="2" x:Name="FireAlarmImage" Style="{DynamicResource FireAlarmImage}" Width="auto" Height="auto" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>

            <Border Grid.Row="3" Grid.Column="0" Height="150" >
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Grid.Column="0" x:Name="BarChartGrid" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    </Grid>

                    <Grid Grid.Row="0" Grid.Column="1" >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Viewbox Grid.Row="0" Grid.Column="0" Stretch="Uniform" MaxWidth="150" HorizontalAlignment="Right" VerticalAlignment="Stretch" >
                            <local:Clock x:Name="Clock" HorizontalAlignment="Right"/>
                        </Viewbox>

                        <local:PieChart Grid.Row="0" Grid.Column="1" x:Name="PieChart" HorizontalAlignment="Right">
                        </local:PieChart>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Border>
</controls:ControlBase>
