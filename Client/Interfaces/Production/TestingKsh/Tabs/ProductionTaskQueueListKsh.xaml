<controls:ControlBase  x:Class="Client.Interfaces.Production.Testing.ProductionTaskQueueListKsh"
             xmlns:controls="clr-namespace:Client.Interfaces.Main"
             xmlns:local="clr-namespace:Client.Interfaces.Production.Testing"
             
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="1200"
             Style="{DynamicResource UserControlCanvas}">

    <UserControl.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Style="{DynamicResource DataGridContainer}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0"  Grid.Column="0" Style="{DynamicResource GridControlPanel}">
            <Border>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Name="GridToolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left"  >
                        <Button Name="RefreshButton"    Content="Обновить" Tag="Refresh" Style="{DynamicResource Button}"  />

                        <Button Name="StatButton"    Content="Подсчет" Tag="Stat" Style="{DynamicResource Button}"  />
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                        <Label Content="Гофроагрегат:" Style="{DynamicResource TopLabel}" />
                        <controls:SelectBox  x:Name="Machine" SelectedItemChanged="SelectBoxItemChanged"  Style="{DynamicResource CustomFormField}"  Width="100" Autocomplete="False" />
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                        <Label Content="Картон:" Style="{DynamicResource TopLabel}" />
                        <controls:SelectBox  x:Name="Cardboard" SelectedItemChanged="SelectBoxItemChanged"  Style="{DynamicResource CustomFormField}"  Width="180" Autocomplete="True" />
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                        <Label Content="Тип композиции:" Style="{DynamicResource TopLabel}" />
                        <controls:SelectBox  x:Name="CompositionType" SelectedItemChanged="SelectBoxItemChanged"  Style="{DynamicResource CustomFormField}" ListBoxMinHeight="130" Width="100" Autocomplete="False" />
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                        <CheckBox Name="WithSheetCheckBox" Content="С листами"  Style="{DynamicResource CheckBoxTopPanel}" Click="WithSheetCheckBox_Click"/>
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                        <CheckBox Name="QueueOnlyCheckBox" Content="Очередь"  Style="{DynamicResource CheckBoxTopPanel}" IsChecked="True" Click="QueueOnlyCheckBox_Click"/>
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                        <Label Content="Поиск:"  Style="{DynamicResource TopLabel}" />
                        <TextBox Style="{DynamicResource FormFieldTopPanel}" Width="60" TextWrapping="NoWrap" Name="SearchText" Text=""  />
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="1" Style="{DynamicResource FormHeaderButtonsPanel}"  HorizontalAlignment="Right" >
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                        <CheckBox Name="AutoTestingCheckBox" Content="Авт. расстановка флагов" ToolTip="Автоматическая расстановка флагов Лаборатория в ПЗ" Style="{DynamicResource CheckBoxTopPanel}"/>
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                        <Button Name="HelpButton"  Width="24" MinWidth="24" Tag="Help" Content="?"  Style="{DynamicResource Button}"  />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Row="1"  Grid.Column="0" >
            <Border Style="{DynamicResource DataGridMainHolder}" >
                <controls:GridBox4 x:Name="Grid" />
            </Border>
        </Grid>
    </Grid>
</controls:ControlBase >
