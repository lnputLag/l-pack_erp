<controls:ControlBase 
    x:Class="Client.Interfaces.Production.MoldedContainer.ProductionTaskTab"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"         

    mc:Ignorable="d" 
    d:DesignWidth="1024" d:DesignHeight="200" 
    Style="{DynamicResource UserControlCanvas}"    
>

    <UserControl.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{DynamicResource DataGridContainerHolder}"  >
        <Grid Style="{DynamicResource DataGridContainer}">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"  Grid.Column="0" Style="{DynamicResource GridControlPanel}">
                <Border >
                    <Grid >

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="11*"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Name="OrderGridToolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left" Width="770" >
                            <TextBlock Height="24" Padding="0,4,0,0">Заявки</TextBlock>
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                            
                            <Button Name="OrderGridRefreshButton"    Content="Обновить"  Style="{DynamicResource Button}"  />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                            <Button Name="ProductionTaskGridCreate2Button"     Content="Создать ПЗ"  Style="{DynamicResource Button}"  />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                            
                            <Button Name="EditNote" Content="Изменить примечание" Style="{DynamicResource Button}" />
                            <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}" />

                            <Label Content="Поиск:"  Style="{DynamicResource TopLabel}" />
                            <TextBox Name="OrderGridSearch" Style="{DynamicResource FormFieldTopPanel}" MinWidth="120" MaxWidth="180"  TextWrapping="NoWrap"  Text=""  />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                            <CheckBox Name="HideComplete" Content="Скрыть выполненные" Click="ShowAllComplete_Click" Style="{DynamicResource CheckBoxTopPanel}"  />
                            
                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="1" Style="{DynamicResource FormHeaderButtonsPanel}"  HorizontalAlignment="Right" Width="24" >
                            <Button Name="HelpButton" Tag="help" Width="24" MinWidth="24"  Content="?"  Style="{DynamicResource Button}" Margin="0"  />
                        </StackPanel>

                    </Grid>
                </Border>
            </Grid>

            <Grid Grid.Row="1"  Grid.Column="0" >
                <Border Style="{DynamicResource DataGridMainHolder}">
                    <controls:GridBox4 x:Name="OrderGrid" />
                </Border>
            </Grid>

            <Grid Grid.Row="2"  Grid.Column="0" Style="{DynamicResource GridControlPanel}">
                <Border >
                    <Grid >

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="0*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Name="ProductionTaskGridToolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left">
                            <TextBlock Height="24" Padding="0,4,0,0">ПЗ</TextBlock>
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                            <Button Name="ProductionTaskGridRefreshButton"   Content="Обновить"  Style="{DynamicResource Button}"  />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                            <Button Name="ProductionTaskGridCreateButton" Content="Создать"   Style="{DynamicResource Button}"/>
                            <Button Name="ProductionTaskGridEditButton"   Content="Изменить"  Style="{DynamicResource Button}"/>
                            <Button Name="ProductionTaskGridDeleteButton" Content="Удалить"   Style="{DynamicResource Button}"/>
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                            <Label Content="Поиск:"  Style="{DynamicResource TopLabel}" />
                            <TextBox Name="ProductionTaskGridSearch" Style="{DynamicResource FormFieldTopPanel}" Width="120" MaxWidth="180" MinWidth="30" TextWrapping="NoWrap" Text=""  />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                            
                            <Label Content="Рабочиий центр: " Style="{DynamicResource TopLabel}"/>
                            <controls:SelectBox MinWidth="235" ListBoxMinHeight="235" x:Name="SelectWorkCenter" Style="{DynamicResource CustomFormField}" />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}" />
                            
                            <Label Content="Станок: " Style="{DynamicResource TopLabel}"/>
                            <controls:SelectBox MinWidth="150" ListBoxMinHeight="150" x:Name="SelectMachine" Style="{DynamicResource CustomFormField}" />
                            
                            <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>
                            <CheckBox Name="ShowAll" Content="Показать все" Click="ShowAll_Click" Style="{DynamicResource CheckBoxTopPanel}"  />

                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>

            <Grid Grid.Row="3"  Grid.Column="0" >
                <Border Style="{DynamicResource DataGridMainHolder}">
                    <controls:GridBox4 x:Name="ProductionTaskGrid" />
                </Border>
            </Grid>

        </Grid>
    </Border>

</controls:ControlBase>
