<controls:ControlBase 
    x:Class="Client.Interfaces.Production.MoldedContainer.RecyclingTab"
    xmlns:local="clr-namespace:Client.Interfaces.Production.MoldedContainer"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"         

    mc:Ignorable="d" 
    d:DesignWidth="1280" d:DesignHeight="600" 
    Style="{DynamicResource UserControlCanvas}"    
>

    <UserControl.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{DynamicResource DataGridContainerHolder}"  >
        <Grid Style="{DynamicResource DataGridContainer}">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="8*" />
                <ColumnDefinition Width="4*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"  Grid.Column="0" Grid.ColumnSpan="2" Style="{DynamicResource GridControlPanel}">
                <Border >
                    <Grid >

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="10*"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Name="ProductionTaskGridToolbar" Style="{DynamicResource FormHeaderButtonsPanel}"  Margin="0,0,0,0" >

                            <Label Content="Станок:"  Style="{DynamicResource TopLabel}" />
                            <controls:SelectBox  x:Name="Machines" SelectedItemChanged="Machines_SelectedItemChanged" Style="{DynamicResource CustomFormField}" Width="180" Autocomplete="False"  />
                            <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"/>

                            <Button Name="ProductionTaskGridRefreshButton" Content="Обновить"  Style="{DynamicResource Button}"  />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                            <Button Name="ProductionTaskGridStartButton" Content="Начать ПЗ"  Style="{DynamicResource Button}"   />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                            <Button Name="ProductionTaskGridEndButton" Content="Закончить ПЗ" Style="{DynamicResource Button}"  />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                            <Button Name="ShowExcelButton" Content="Открыть ТК" Style="{DynamicResource Button}"   />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                            <Button Name="ShowAllPz" Content="Все ПЗ" Style="{DynamicResource Button}"   />
                            <!--<CheckBox Name="ShowAllPz" Content="Все ПЗ" Click="ShowAllPz_Click" Style="{DynamicResource CheckBoxTopPanel}"  />-->

                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Right">
                            <Button Name="SettingsButton"    ToolTip="Настройка"   Style="{DynamicResource ButtonGlyph}" Click="SettingsButton_Click"  Margin="0,0,5,0"  >
                                <Image Style="{DynamicResource SettingsImage}" />
                                <Button.ContextMenu>
                                    <ContextMenu Name="BurgerMenu">
                                        <MenuItem Header="Настройка печати" Name="BurgerPrintSettings" Click="BurgerPrintSettings_Click"  Background="#ffffff" />
                                    </ContextMenu>
                                </Button.ContextMenu>
                            </Button>

                            <Button Name="HelpButton" Tag="help" Width="24" MinWidth="24"  Content="?"  Style="{DynamicResource Button}" Margin="0"  />
                        </StackPanel>

                    </Grid>
                </Border>
            </Grid>

            <Grid Grid.Row="1"  Grid.Column="0" >
                <Border Style="{DynamicResource DataGridMainHolder}">
                    <controls:GridBox4 x:Name="RecyclingGrid" />
                </Border>
            </Grid>

            <Grid Grid.Row="1"  Grid.Column="1" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="407*"/>
                    <ColumnDefinition Width="20*"/>
                </Grid.ColumnDefinitions>
                <Border Margin="10,0,5,0" Grid.ColumnSpan="2" >
                    <Grid >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="46*"/>
                            <ColumnDefinition Width="57*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                            <RowDefinition Height="5"></RowDefinition>
                            <RowDefinition Height="*"></RowDefinition>
                            <RowDefinition Height="10"></RowDefinition>
                            <RowDefinition Height="Auto"></RowDefinition>
                            <RowDefinition Height="5"></RowDefinition>
                            <RowDefinition Height="*"></RowDefinition>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"   Style="{DynamicResource FormHeaderButtonsPanel}" >
                            <Label Content="Списанные паллеты"  Style="{DynamicResource TopLabel}" />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                            <CheckBox Name="ShowAllPalletRashod" Content="Все паллеты" Click="ShowAllPalletRashod_Click" Style="{DynamicResource CheckBoxTopPanel}" />
                        </StackPanel>

                        <Grid Grid.Row="1"  Grid.Column="0" Grid.ColumnSpan="2" >
                            <Border Style="{DynamicResource DataGridMainHolder}">
                            </Border>
                        </Grid>

                        <Grid Grid.Row="2"  Grid.Column="0" Grid.ColumnSpan="2"  >
                            <Border Style="{DynamicResource DataGridMainHolder}">
                                <controls:GridBox4 x:Name="PalletRashodGrid" />
                            </Border>
                        </Grid>

                        <Grid Grid.Row="3"  Grid.Column="0" Grid.ColumnSpan="2" >
                            <Border Style="{DynamicResource DataGridMainHolder}">
                            </Border>
                        </Grid>

                        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2"  Style="{DynamicResource FormHeaderButtonsPanel}" >
                            <Button Name="PalletCreateButton"  Content="Создать паллету"  Style="{DynamicResource Button}"  />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                            <Button Name="PalletDeleteButton" Content="Удалить"  Style="{DynamicResource Button}"   />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                            <Button Name="PalletPrintButton" Content="Печать ярлыка"  Style="{DynamicResource Button}"  />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                            <CheckBox Name="ShowAllPalletPrihod" Content="Все паллеты" Click="ShowAllPalletPrihod_Click" Style="{DynamicResource CheckBoxTopPanel}" />

                            <!--<Label Content="ШК:"  Style="{DynamicResource TopLabel}" />
                            <TextBox Name="BarCodeTextBox" Text="" MaxLength="13"  HorizontalAlignment="Center" VerticalAlignment="Top" Style="{DynamicResource FormField}" Width="75" TextWrapping="NoWrap" IsEnabled="True" Visibility="Visible"/>-->
                        </StackPanel>

                        <Grid Grid.Row="5"  Grid.Column="0" Grid.ColumnSpan="2" >
                            <Border Style="{DynamicResource DataGridMainHolder}">
                            </Border>
                        </Grid>

                        <Grid Grid.Row="6"  Grid.Column="0" Grid.ColumnSpan="2" >
                            <Border Style="{DynamicResource DataGridMainHolder}">
                                <controls:GridBox4 x:Name="PalletPrihodGrid" />
                            </Border>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>


            <Grid Grid.Row="2"  Grid.Column="0" Style="{DynamicResource GridControlPanel}">
                <Border >
                    <Grid >

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                        </Grid.RowDefinitions>

                    </Grid>
                </Border>
            </Grid>

            <Grid Grid.Row="3"  Grid.Column="0" >
                <Border Style="{DynamicResource DataGridMainHolder}">

                </Border>
            </Grid>


            <!--<DockPanel Grid.Row="1" Grid.Column="1" Grid.RowSpan="2" Margin="5 0">
                <TextBlock Text="Списание поддонов" Style="{DynamicResource StatusBarTextBlock}" Foreground="Black" DockPanel.Dock="Top" HorizontalAlignment="Left" Margin="0 0 0 0"/>
                <Border Style="{DynamicResource DataGridMainHolder}" DockPanel.Dock="Top" >
                    <controls:GridBox x:Name="PalletRashodGrid" />
                </Border>

                <TextBlock Text="Оприходование поддонов" Style="{DynamicResource StatusBarTextBlock}" Foreground="Black" DockPanel.Dock="Top" HorizontalAlignment="Left" Margin="0 0 0 0"/>
                <Border Style="{DynamicResource DataGridMainHolder}" DockPanel.Dock="Top" >
                    <controls:GridBox x:Name="PalletPrihodGrid" />
                </Border>


            </DockPanel>-->

        </Grid>
    </Border>

</controls:ControlBase>
