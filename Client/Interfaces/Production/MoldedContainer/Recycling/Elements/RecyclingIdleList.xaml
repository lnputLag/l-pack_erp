<controls:ControlBase 
    x:Class="Client.Interfaces.Production.MoldedContainer.RecyclingIdleList"
    xmlns:local="clr-namespace:Client.Interfaces.Production.MoldedContainer"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"     
    mc:Ignorable="d"   
    d:DesignWidth="800" d:DesignHeight="600"
    Style="{DynamicResource UserControlCanvasStatic}"          
>
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border>
        <Grid  Style="{DynamicResource SHMonBlockGrid}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="0"/>
                <!--кнопка-->
                <RowDefinition Height="auto"/>
                <!--Простои грид-->
                <RowDefinition Height="*" />
                <!--горизонтальный разделитель-->
                <RowDefinition Height="5" />
                <!--информация-->
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Style="{DynamicResource GridControlPanel}">
                <StackPanel Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left"  >
                    <Label Content="Простои более 10 мин."  Style="{DynamicResource TopLabel}" />
                    <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                    <Button Name="IdlesEditButton" Content="Изменить" Style="{DynamicResource Button}" />

                </StackPanel>
            </Grid>

            <!--список простоев за тек. смену-->
            <Border Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Style="{DynamicResource DataGridMainHolder}" >
                <Grid>
                    <controls:GridBox4 x:Name="IdleGrid"/>
                </Grid>
            </Border>
            
            <!--Информация по простоям-->
            <Grid Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Style="{DynamicResource GridControlPanel}">
                <StackPanel Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left"  >
                    <Label Name="IdlesInfo" Content="        "  Style="{DynamicResource TopLabel}" />
                </StackPanel>
            </Grid>

        </Grid>
    </Border>
</controls:ControlBase>
