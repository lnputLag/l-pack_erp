<UserControl
        x:Class="Client.Interfaces.Production.UnitWeight"
        xmlns:controls="clr-namespace:Client.Interfaces.Main"
    
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        mc:Ignorable="d"
        d:DesignWidth="1435" d:DesignHeight="200"       
             
        Style="{DynamicResource UserControlCanvas}"   
>
    <UserControl.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>
    <Border Style="{DynamicResource DataGridContainerHolder}"  >
        <Grid Style="{DynamicResource DataGridContainer}" >

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="0"></RowDefinition>
                <RowDefinition Height="70
                               "></RowDefinition>
                <RowDefinition/>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"  Grid.Column="0" Style="{DynamicResource GridControlPanel}" >
            </Grid>

            <Grid Grid.Row="1"  Grid.Column="0" Style="{DynamicResource GridControlPanel}">
                <Border >
                    <Grid >

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1039*"/>
                            <ColumnDefinition Width="396*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Name="GridToolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left" Height="58" Margin="0,2,0,0" Width="1029" >

                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                            <Label Content="Вес, кг: "  Style="{DynamicResource TouchFormLabel}" />
                            <TextBox  Name="Weight" Text="" TextWrapping="NoWrap" Style="{DynamicResource FormFieldInput}"  MaxLength="7" Width="120"  PreviewTextInput="PreviewTextInput" />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                            <Button  Name="SetButton" Tag="access_mode_full_access" Style="{DynamicResource RollReelButton}" Click="SetButton_Click" Width="145" HorizontalAlignment="Left" Margin="0,4,10,4">
                                Добавить
                            </Button>

                            <Button  Name="UpdateButton" Tag="access_mode_full_access" Style="{DynamicResource RollReelButton}" Click="UpdateButton_Click" Width="145" HorizontalAlignment="Left" Margin="0,4,10,4">
                                Изменить
                            </Button>

                            <TextBlock  x:Name="FireStatus" Text="Пожар на объекте!"  TextAlignment="Left" HorizontalAlignment="Stretch" Style="{DynamicResource TouchFormStatusBar}" Height="43" Padding="0,13,0,0" VerticalAlignment="Top"/>

                        </StackPanel>

                        <StackPanel Grid.Row="0" Style="{DynamicResource FormHeaderButtonsPanel}"  HorizontalAlignment="Right" Width="271" Height="60" Grid.Column="1" >

                            <Button  Name="FireAlarmButton" Style="{DynamicResource RollReelButton}" Click="FireAlarmButton_Click" Width="145" HorizontalAlignment="Left" Margin="0,4,10,4">
                                Пожар
                            </Button>

                            <Button Name="BurgerButton" Style="{DynamicResource RollReelButton}" Width="50" Click="BurgerButton_Click" >
                                <StackPanel Orientation="Horizontal"  HorizontalAlignment="Center" >
                                    <Border Width="40">
                                        <Image Style="{DynamicResource RollReelMenuIcon}"  />
                                    </Border>
                                </StackPanel>
                                <Button.ContextMenu >
                                    <ContextMenu Name="BurgerMenu">
                                        <MenuItem Header="Информация"       Name="InfoButton"       Click="InfoButto_Click"    FontSize="18" Height="32" Background="#ffffff" FontWeight="SemiBold" />
                                        <MenuItem Header="Обновить"         Name="RestartButton"    Click="RestartButton_Click" FontSize="18" Height="32" Background="#ffffff" FontWeight="SemiBold" />
                                        <MenuItem Header="Выход"            Name="BurgerExit"       Click="BurgerExit_Click"     FontSize="18" Height="32" Background="#ffffff" FontWeight="SemiBold" />
                                    </ContextMenu>
                                </Button.ContextMenu>
                            </Button>


                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                            <Button  Name="HelpButton" Style="{DynamicResource RollReelButton}" Click="HelpButton_Click" Width="50" HorizontalAlignment="Left" Margin="0,5,10,5">
                                ?
                            </Button>


                        </StackPanel>

                    </Grid>
                </Border>
            </Grid>

            <Grid Grid.Row="2"  Grid.Column="0" >
                <Border Style="{DynamicResource DataGridMainHolder}" >
                    <controls:GridBox x:Name="Grid" />
                </Border>
            </Grid>

            <Grid Grid.Row="2"  Grid.Column="0" >
                <Border Style="{DynamicResource DataGridMainHolder}" >
                    <Image x:Name="FireAlarmImage" Style="{DynamicResource FireAlarmImage}"/>
                </Border>
            </Grid>


        </Grid>
    </Border>
</UserControl>
