<UserControl 
    x:Class="Client.Interfaces.Production.ProcessingMachines.ProductionTaskDiagram"
    xmlns:local="clr-namespace:Client.Interfaces.Production.ProcessingMachines"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"         
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"    
    xmlns:s="clr-namespace:System;assembly=mscorlib"

    mc:Ignorable="d" 
    d:DesignWidth="900" d:DesignHeight="200" 
    Style="{DynamicResource UserControlCanvas}"            
>

    <UserControl.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{DynamicResource DataGridContainerHolder}"  >
        <Grid Style="{DynamicResource DataGridContainer}">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"  Grid.Column="0" Style="{DynamicResource GridControlPanel}">
                <Border >
                    <Grid >

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="6*"/>
                            <ColumnDefinition Width="4*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Name="Toolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left"  >

                            <Label Content="Дата:"  Style="{DynamicResource TopLabel}"/>
                            <Grid Style="{DynamicResource FormFieldDateHolder}">
                                <!--<TextBox Name="Today"  MaxLength="10"  Style="{DynamicResource FormFieldDate}" IsReadOnly="True" />
                                <DatePicker  SelectedDate="{Binding Path=Text, Mode=OneWayToSource, ElementName=Today, Converter={StaticResource DateDate} }"  Style="{DynamicResource FormFieldDP}"  Margin="0,1,0,0">
                                </DatePicker>-->

                                <controls:DatePicker x:Name="Today" Style="{DynamicResource DateEditStyle}" >
                                </controls:DatePicker>
                            </Grid>
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                            <Button Name="RefreshButton"    Content="Обновить"  Style="{DynamicResource Button}" Click="RefreshButton_Click"   />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="1" Style="{DynamicResource FormHeaderButtonsPanel}"  HorizontalAlignment="Right" >
                            <Button Name="HelpButton"  Width="24" MinWidth="24"  Content="?"  Style="{DynamicResource Button}" Click="HelpButton_Click_1"  />
                        </StackPanel>

                    </Grid>
                </Border>
            </Grid>

            <Grid Grid.Row="1" Grid.Column="0" Style="{DynamicResource DataGridContainer}">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <!-- 0 кнопки управления -->
                    <RowDefinition Height="Auto"></RowDefinition>
                    <!-- 1 разметка -->
                    <RowDefinition Height="1"></RowDefinition>
                    <!-- 2 заголовки таблиц -->
                    <RowDefinition Height="25"></RowDefinition>
                    <!-- 3 таблицы данных -->
                    <RowDefinition Height="*" ></RowDefinition>
                    <!-- 4 скроллеры -->
                    <RowDefinition Height="20"></RowDefinition>
                </Grid.RowDefinitions>

                <!-- 1 разметка -->
                <Grid Grid.Row="1" Grid.Column="0" >

                    <ScrollViewer Style="{DynamicResource SHMonContainerVScroll}" VerticalScrollBarVisibility="Visible" >
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="140" />
                                <ColumnDefinition Width="10" />
                                <ColumnDefinition Width="1*" />

                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" Grid.Column="0"  x:Name="Column1">
                            </Border>

                            <Border Grid.Row="0" Grid.Column="1" Style="{DynamicResource SHMonContainerSpacer}">
                            </Border>

                            <Border Grid.Row="0" Grid.Column="2"  x:Name="Column2" >
                            </Border>

                        </Grid>
                    </ScrollViewer>

                </Grid>

                <!-- 2 заголовки таблиц -->
                <Grid Grid.Row="2" Grid.Column="0">

                    <ScrollViewer Style="{DynamicResource SHMonContainerVScroll}" VerticalScrollBarVisibility="Visible" >
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="140" />
                                <ColumnDefinition Width="10" />
                                <ColumnDefinition Width="1*" />

                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" Grid.Column="0" Style="{DynamicResource SHMonContainerHeader}" >
                                <ScrollViewer  Style="{DynamicResource SHMonContainerHScroll}"  >
                                    <Grid Style="{DynamicResource SHMonContainer}">
                                        <Label Content="Станок" Style="{DynamicResource SHMonContainerHeaderLabel}"></Label>
                                    </Grid>
                                </ScrollViewer>
                            </Border>

                            <Border Grid.Row="0" Grid.Column="1" Style="{DynamicResource SHMonContainerSpacer}">
                            </Border>

                            <!-- временные метки -->
                            <Border Grid.Row="0" Grid.Column="2" Style="{DynamicResource SHMonContainerHeader}" >

                                <ScrollViewer Style="{DynamicResource SHMonContainerHScroll}"   x:Name="MonitorTimelineContainerScroll" Width="{Binding Path=ActualWidth, ElementName=Column2}" >
                                    <Grid x:Name="MonitorTimelineContainer"  Style="{DynamicResource SHMonContainer}" >
                                    </Grid>
                                </ScrollViewer>

                            </Border>

                        </Grid>
                    </ScrollViewer>

                </Grid>

                <!-- 3 таблицы данных   -->
                <Grid Grid.Row="3" Grid.Column="0">

                    <ScrollViewer Style="{DynamicResource SHMonContainerVScroll}" VerticalScrollBarVisibility="Visible" x:Name="ScrollViewer1" Height="{Binding Path=ActualHeight, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=UIElement}}">
                        <Grid >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="140" />
                                <ColumnDefinition Width="10" />
                                <ColumnDefinition />
                                <ColumnDefinition Width="14*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"  />
                                <RowDefinition Height="*"   />
                            </Grid.RowDefinitions>

                            <!-- названия терминалов -->
                            <Border Grid.Row="0" Grid.Column="0" Style="{DynamicResource SHMonContainerBody}" Margin="0,0,0,399" Grid.RowSpan="2">
                                <ScrollViewer   Style="{DynamicResource SHMonContainerHScroll}"  >
                                    <Grid x:Name="MonitorHeadersContainer" Style="{DynamicResource SHMonContainer}" VerticalAlignment="Top">
                                    </Grid>
                                </ScrollViewer>
                            </Border>

                            <Border Grid.Row="0" Grid.Column="1" Style="{DynamicResource SHMonContainerSpacer}" Margin="0,0,0,399" Grid.RowSpan="2">
                            </Border>

                            <!-- сегменты отгрузок -->
                            <Border Grid.Row="0" Grid.Column="2" Style="{DynamicResource SHMonContainerBody}" Grid.ColumnSpan="2" Margin="0,0,0,399" Grid.RowSpan="2">
                                <ScrollViewer  x:Name="MonitorDataContainerScroll"  Style="{DynamicResource SHMonContainerHScroll}" Width="{Binding Path=ActualWidth, ElementName=Column2}"  >
                                    <StackPanel Orientation="Vertical">
                                        <Grid  x:Name="MonitorDataContainer" Visibility="Visible" Style="{DynamicResource SHMonContainer}"  VerticalAlignment="Stretch"  >
                                        </Grid>
                                    </StackPanel>
                                </ScrollViewer>
                            </Border>

                        </Grid>
                    </ScrollViewer>

                </Grid>

                <!-- 4 скроллеры   -->
                <Grid Grid.Row="4" Grid.Column="0">

                    <ScrollViewer Style="{DynamicResource SHMonContainerVScroll}" VerticalScrollBarVisibility="Visible" >
                        <Grid >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="140" />
                                <ColumnDefinition Width="10" />
                                <ColumnDefinition Width="1*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"  />
                                <RowDefinition Height="*"   />
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" Grid.Column="2" Style="{DynamicResource SHMonContainerFooter}" >
                                <ScrollViewer  ScrollChanged="MonitorScrollChanged" HorizontalScrollBarVisibility="Visible"  x:Name="MonitorDataContainerAreaScroll" Style="{DynamicResource SHMonContainerHScroll}" Width="{Binding Path=ActualWidth, ElementName=Column2}" >
                                    <Grid Width="{Binding Path=ActualWidth, ElementName=MonitorTimelineContainer}">
                                    </Grid>
                                </ScrollViewer>
                            </Border>

                        </Grid>
                    </ScrollViewer>

                </Grid>

            </Grid>

            <Grid Grid.Row="1" Grid.Column="0" Name="Splash" Visibility="Collapsed" >
                <Border Background="#55ffffff" >
                </Border>
            </Grid>

        </Grid>
    </Border>

</UserControl>
