<UserControl 
    x:Class="Client.Interfaces.Production.ProductionTaskList"
    xmlns:local="clr-namespace:Client.Interfaces.Production"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"         
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"    
    xmlns:s="clr-namespace:System;assembly=mscorlib"

    mc:Ignorable="d" 
    d:DesignWidth="1450" d:DesignHeight="800" 
    Style="{DynamicResource UserControlCanvas}"            
>

    <UserControl.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>


    <Border Style="{DynamicResource DataGridContainerHolder}"  >
        <Grid Style="{DynamicResource DataGridContainer}">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"  Grid.Column="0" Style="{DynamicResource GridControlPanel}">
                <Border >
                    <Grid >

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="11.5*"/>
                            <ColumnDefinition Width="0.5*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>                            
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Name="TaskGridToolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left"  >

                            <Label Content="Дата:"  Style="{DynamicResource TopLabel}"/>
                            <controls:DatePicker x:Name="Today" Style="{DynamicResource DateEditStyle}" TextChanged="Today_TextChanged" />

                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                            <Button Name="RefreshButton"    Content="Показать"  Style="{DynamicResource Button}" Click="ShowButton_Click"   />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                            <Button Name="CreateButton" Click="CreateButton_Click" Tag="access_mode_full_access"  Content="Создать"    Style="{DynamicResource Button}" />
                            <Button Name="EditButton"   Click="EditButton_Click"   Tag="access_mode_full_access"  Content="Изменить"   Style="{DynamicResource Button}" Width="70" />
                            <Button Name="DeleteButton" Click="DeleteButton_Click" Tag="access_mode_full_access"  Content="Удалить"    Style="{DynamicResource Button}" />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                            <Button Name="ExportButton" Click="ExportButton_Click" Content="В Excel"   Style="{DynamicResource Button}" />

                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                            <Label Content="Поиск:"  Style="{DynamicResource TopLabel}" />
                            <TextBox Style="{DynamicResource FormFieldTopPanel}" Width="60" TextWrapping="NoWrap" Name="SearchText" Text=""  />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                            <Label Content="Завод:"  Style="{DynamicResource TopLabel}"/>
                            <controls:SelectBox x:Name="Factory" Style="{DynamicResource CustomFormField}" ListBoxMinWidth="170" Width="125" Autocomplete="False" SelectedItemChanged="Factory_SelectedItemChanged"  />


                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="1" Style="{DynamicResource FormHeaderButtonsPanel}"  HorizontalAlignment="Right" >
                            <Button Name="HelpButton"  Width="24" MinWidth="24"  Content="?"  Style="{DynamicResource Button}" Click="HelpButton_Click_1"  />
                        </StackPanel>

                    </Grid>
                </Border>
            </Grid>
            <Grid Grid.Row="1"  Grid.Column="0" Style="{DynamicResource GridControlPanel}">
                <Border>
                    <StackPanel Name="TaskFilterToolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left">

                        <Label Content="Статус:" Style="{DynamicResource TopLabel}" />
                        <controls:SelectBox  x:Name="Statuses" SelectedItemChanged="Statuses_SelectedItemChanged" Style="{DynamicResource CustomFormField}" ListBoxMinWidth="205" ListBoxMinHeight="280" Width="140" Autocomplete="False" />
                        <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>

                        <Label Content="Профиль:" Style="{DynamicResource TopLabel}" />
                        <controls:SelectBox x:Name="Profile" SelectedItemChanged="Profile_SelectedItemChanged"  Style="{DynamicResource CustomFormField}" ListBoxMinWidth="150" ListBoxMinHeight="255"  Width="50" Autocomplete="True"   VerticalAlignment="Bottom" />
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                        <Label Content="Картон:" Style="{DynamicResource TopLabel}" />
                        <controls:SelectBox x:Name="Cardboard" SelectedItemChanged="Cardboard_SelectedItemChanged"  Style="{DynamicResource CustomFormField}" ListBoxMinWidth="200" ListBoxMinHeight="255"  Width="100" Autocomplete="True"   VerticalAlignment="Bottom" />
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                        <Label Content="Формат:" Style="{DynamicResource TopLabel}" />
                        <controls:SelectBox x:Name="Format" SelectedItemChanged="Format_SelectedItemChanged"  Style="{DynamicResource CustomFormField}" ListBoxMinHeight="205" ListBoxMinWidth="120"  Width="60" Autocomplete="True"   VerticalAlignment="Bottom" />
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                        <Label Content="Станок:" Style="{DynamicResource TopLabel}" />
                        <controls:SelectBox  x:Name="Machine" SelectedItemChanged="Machine_SelectedItemChanged"  Style="{DynamicResource CustomFormField}"  ListBoxMinWidth="180" ListBoxMinHeight="250" Width="80" Autocomplete="False" />
                        <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>

                        <Label Content="Создатель:" Style="{DynamicResource TopLabel}" />
                        <controls:SelectBox x:Name="Creator" SelectedItemChanged="Creator_SelectedItemChanged"  Style="{DynamicResource CustomFormField}" ListBoxMinWidth="140" ListBoxMinHeight="170"  Width="100" Autocomplete="True"   VerticalAlignment="Bottom" />
                        <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>

                        <CheckBox Name="TandemCheckBox" Content="Тандем" Style="{DynamicResource CheckBoxTopPanel}" Click="TandemCheckBox_Click" />
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>


                    </StackPanel>
                </Border>
            </Grid>

            <Grid Grid.Row="2"  Grid.Column="0" >
                <Border Style="{DynamicResource DataGridMainHolder}" >
                    <controls:GridBox x:Name="TaskGrid" />
                </Border>
            </Grid>
            <Grid Grid.Row="3"  Grid.Column="0" Style="{DynamicResource GridControlPanel}" >
                <Border>
                    <StackPanel Orientation="Horizontal" Name="PositionGridToolbar">
                        
                        <Label Content="Общая длина заданий, м:" ToolTip="Общая длина невыполненных заданий" Style="{DynamicResource TopLabel}" />
                        <TextBox Name="TotalLength" ToolTip="Общая длина невыполенных заданий" Style="{DynamicResource FormFieldTopPanel}" HorizontalAlignment="Right" MinWidth="50" TextWrapping="NoWrap"  IsReadOnly="True" />
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                        <Label Content="Длина заданий, м:" ToolTip="Общая длина заданий в списке" Style="{DynamicResource TopLabel}" />
                        <TextBox Name="TotalLength2" ToolTip="Общая длина невыполенных заданий" Style="{DynamicResource FormFieldTopPanel}" HorizontalAlignment="Right" MinWidth="50" TextWrapping="NoWrap"  IsReadOnly="True"  />
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                        <Button Name="BindPosition" Tag="access_mode_full_access"    Content="Заявки"  Style="{DynamicResource Button}" Click="BindPosition_Click"   />
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                        <Label Name="DuplicatedQuantityLabel" Content="Похожие задания:" ToolTip="Число похожих заданий" Style="{DynamicResource TopLabel}" />
                        <TextBox Name="DuplicatedQuantity" ToolTip="Число похожих заданий" Style="{DynamicResource FormFieldTopPanel}" HorizontalAlignment="Right" MinWidth="50" TextWrapping="NoWrap"  IsReadOnly="True" />
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                    </StackPanel>
                </Border>
            </Grid>

            <Grid Grid.Row="4" Grid.Column="0" Style="{DynamicResource DataGridContainer}"  Height="124">
                <Border Style="{DynamicResource DataGridMainHolder}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="5" />
                            <ColumnDefinition Width="300" />
                        </Grid.ColumnDefinitions>

                        <Grid Grid.Row="0"  Grid.Column="0">
                            <Border Style="{DynamicResource DataGridMainHolder}" >
                                <controls:GridBox x:Name="PositionGrid" />
                            </Border>
                       </Grid>

                        <Grid Grid.Row="0"  Grid.Column="2">
                            <Border Style="{DynamicResource DataGridMainHolder}" >
                                <controls:GridBox x:Name="SourceGrid" />
                            </Border>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
            
            

        </Grid>
    </Border>

</UserControl>
