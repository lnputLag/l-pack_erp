<UserControl 
    x:Class="Client.Interfaces.Production.SelectBlank"
    xmlns:local="clr-namespace:Client.Interfaces.Production"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"         
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"    
    xmlns:s="clr-namespace:System;assembly=mscorlib"

    mc:Ignorable="d" 
    d:DesignWidth="1500" d:DesignHeight="200" 
    Style="{DynamicResource UserControlCanvas}"            
>

    <UserControl.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{DynamicResource DataGridContainerHolder}"  >
        <Grid Style="{DynamicResource DataGridContainer}">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"  Grid.Column="0" Style="{DynamicResource GridControlPanel}">
                <Border >
                    <Grid >

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="10*"/>
                            <ColumnDefinition Width="2*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Name="Toolbar"  Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left"  >

                            <Button Name="SaveButton"   Click="SaveButton_Click" Content="Выбрать"  Style="{DynamicResource FButtonPrimary}"    />
                            <Button Name="CancelButton" Click="CancelButton_Click"  Content="Отмена"     Style="{DynamicResource FButtonCancel}"    />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                            <TextBlock  Name="FormStatus"  TextAlignment="Left" Style="{DynamicResource FStatusBar}" HorizontalAlignment="Stretch" />

                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="1" Style="{DynamicResource FormHeaderButtonsPanel}"  HorizontalAlignment="Right" >
                            <Button x:Name="HelpButton" Click="HelpButton_Click"       ToolTip="Справка"     Style="{DynamicResource ButtonGlyph}" HorizontalAlignment="Right" Margin="0,0,0,0"      >
                                <ContentControl Style="{DynamicResource ButtonSMSymbolIcon}" Content="?" />
                            </Button>
                        </StackPanel>

                    </Grid>
                </Border>
            </Grid>

            <Border Grid.Row="1" Grid.Column="0" >

                <TabControl BorderThickness="0,1,0,0" Margin="0" Padding="0">

                    <TabItem Header="Заявки" Name="ApplicationTab" Style="{DynamicResource TabItemStyle}">
                        <Border Style="{DynamicResource DataGridContainerHolder}"  >
                            <Grid Style="{DynamicResource DataGridContainer}">

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"></RowDefinition>
                                    <RowDefinition Height="*"></RowDefinition>
                                </Grid.RowDefinitions>

                                <Grid Grid.Row="0"  Grid.Column="0" Style="{DynamicResource GridControlPanel}">
                                    <Border >
                                        <Grid >

                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="11*"/>
                                                <ColumnDefinition Width="1*"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"></RowDefinition>
                                            </Grid.RowDefinitions>

                                            <StackPanel Grid.Row="0" Grid.Column="0" Name="ApplicationGridToolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left"  >

                                                <Label Content="C:"  Style="{DynamicResource TopLabel}" Width="25" />
                                                <Grid Style="{DynamicResource FormFieldDateHolder}">
                                                    <!--<TextBox Name="FromDate"  MaxLength="10"   Style="{DynamicResource FormFieldDate}" IsReadOnly="True" TextChanged="FromDate_TextChanged" />
                                                    <DatePicker SelectedDate="{Binding Path=Text, Mode=OneWayToSource, ElementName=FromDate, Converter={StaticResource DateDate} }" Style="{DynamicResource FormFieldDP}"  />-->
                                                    <controls:DatePicker x:Name="FromDate" Style="{DynamicResource DateEditStyle}" TextChanged="FromDate_TextChanged">
                                                    </controls:DatePicker>
                                                </Grid>

                                                <Label Content="По:"  Style="{DynamicResource TopLabel}" Width="25" />
                                                <Grid Style="{DynamicResource FormFieldDateHolder}">
                                                    <!--<TextBox Name="ToDate"  MaxLength="10"   Style="{DynamicResource FormFieldDate}" IsReadOnly="True" TextChanged="ToDate_TextChanged" />
                                                    <DatePicker  SelectedDate="{Binding Path=Text, Mode=OneWayToSource, ElementName=ToDate, Converter={StaticResource DateDate} }"  Style="{DynamicResource FormFieldDP}" />-->
                                                    <controls:DatePicker x:Name="ToDate" Style="{DynamicResource DateEditStyle}" TextChanged="ToDate_TextChanged">
                                                    </controls:DatePicker>

                                                </Grid>

                                                <Button Name="ApplicationRefreshButton"  Content="Показать"  Style="{DynamicResource Button}" Click="ApplicationRefreshButton_Click"   />
                                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                                                <Label Content="Поиск:"  Style="{DynamicResource TopLabel}" />
                                                <TextBox Style="{DynamicResource FormFieldTopPanel}" Width="60" TextWrapping="NoWrap" Name="ApplicationSearchText" Text=""  />
                                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                                                <CheckBox Content="Нераскроенные"  Click="ApplicationFilter1_Click"  Name="ApplicationFilter1" Style="{DynamicResource CheckBoxTopPanel}"  />
                                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                                                <CheckBox Content="Z-картон"  Click="ApplicationFilterZ_Click"  Name="ApplicationFilterZ" Style="{DynamicResource CheckBoxTopPanel}"  />
                                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                                                <CheckBox Content="Первое производство" ToolTip="Изделия, ни разу не отгружавшиеся" Click="ApplicationFilterFirst_Click"  Name="ApplicationFilterFirst" Style="{DynamicResource CheckBoxTopPanel}"  />
                                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                                                <Label Content="Профиль:" Style="{DynamicResource TopLabel}" />
                                                <controls:SelectBox x:Name="ApplicationProfile"   Style="{DynamicResource CustomFormField}" ListBoxMinWidth="150" ListBoxMinHeight="255"  Width="50" Autocomplete="True"   VerticalAlignment="Bottom" />
                                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                                                <Label Content="Картон:" Style="{DynamicResource TopLabel}" />
                                                <controls:SelectBox x:Name="ApplicationCardboard"   Style="{DynamicResource CustomFormField}" ListBoxMinWidth="200" ListBoxMinHeight="255"  Width="100" Autocomplete="True"   VerticalAlignment="Bottom" />
                                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                                                <Label Content="Скрыть недокрой:"  Style="{DynamicResource TopLabel}" />
                                                <controls:SelectBox x:Name="HideUncutted"   Style="{DynamicResource CustomFormField}" Width="80" SelectedItemChanged="HideUncutted_SelectedItemChanged" />
                                                <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>
                                                <Button Name="ApplicationResetButton"  Content="Сброс" ToolTip="Сбросить параметры фильтрации" Style="{DynamicResource Button}" Click="ApplicationResetButton_Click"   />
                                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                                            </StackPanel>

                                            <StackPanel Grid.Row="0" Grid.Column="1" Style="{DynamicResource FormHeaderButtonsPanel}"  HorizontalAlignment="Right"  >

                                            </StackPanel>

                                        </Grid>
                                    </Border>
                                </Grid>

                                <Grid Grid.Row="1"  Grid.Column="0" >
                                    <Border Style="{DynamicResource DataGridMainHolder}" >
                                        <controls:GridBox x:Name="ApplicationGrid" />
                                    </Border>
                                </Grid>

                            </Grid>
                        </Border>
                    </TabItem>

                    <TabItem Header="Изделия" Name="GoodsTab" Style="{DynamicResource TabItemStyle}">
                        <Border Style="{DynamicResource DataGridContainerHolder}"  >
                            <Grid Style="{DynamicResource DataGridContainer}">

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"></RowDefinition>
                                    <RowDefinition Height="*"></RowDefinition>
                                </Grid.RowDefinitions>

                                <Grid Grid.Row="0"  Grid.Column="0" Style="{DynamicResource GridControlPanel}">
                                    <Border >
                                        <Grid >

                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="10*"/>
                                                <ColumnDefinition Width="2*"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"></RowDefinition>
                                            </Grid.RowDefinitions>

                                            <StackPanel Grid.Row="0" Grid.Column="0" Name="GoodsGridToolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left"  >

                                                <Label Content="Название/артикул:"  Style="{DynamicResource TopLabel}" />
                                                <TextBox Style="{DynamicResource FormFieldTopPanel}" TextChanged="GoodsSearchText_TextChanged" KeyUp="GoodsSearchText_KeyUp" Width="150" TextWrapping="NoWrap" Name="GoodsSearchText" Text=""  />
                                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                                                <!--
                                                <Label Content="Картон:" Style="{DynamicResource TopLabel}" />
                                                <controls:SelectBox x:Name="GoodsCardboard"   Style="{DynamicResource CustomFormField}" ListBoxMinWidth="120" ListBoxMinHeight="200"  Width="120" Autocomplete="True"   VerticalAlignment="Bottom" />
                                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                                                -->

                                                <Button Name="GoodsRefreshButton"  Content="Показать"  Style="{DynamicResource Button}" Click="GoodsRefreshButton_Click"   />
                                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                                                <Label Content="Профиль:" Style="{DynamicResource TopLabel}" />
                                                <controls:SelectBox x:Name="GoodsProfile"   Style="{DynamicResource CustomFormField}" ListBoxMinWidth="150" ListBoxMinHeight="255"  Width="50" Autocomplete="True"   VerticalAlignment="Bottom" />
                                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                                                <Label Content="Картон:" Style="{DynamicResource TopLabel}" />
                                                <controls:SelectBox x:Name="GoodsCardboard"   Style="{DynamicResource CustomFormField}" ListBoxMinWidth="200" ListBoxMinHeight="255"  Width="100" Autocomplete="True"   VerticalAlignment="Bottom" />
                                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                                            </StackPanel>

                                            <StackPanel Grid.Row="0" Grid.Column="1" Style="{DynamicResource FormHeaderButtonsPanel}"  HorizontalAlignment="Right"  >

                                            </StackPanel>

                                        </Grid>
                                    </Border>
                                </Grid>

                                <Grid Grid.Row="1"  Grid.Column="0" >
                                    <Border Style="{DynamicResource DataGridMainHolder}" >
                                        <controls:GridBox x:Name="GoodsGrid" />
                                    </Border>
                                </Grid>

                            </Grid>
                        </Border>
                    </TabItem>

                </TabControl>

            </Border>

        </Grid>
    </Border>

</UserControl>
