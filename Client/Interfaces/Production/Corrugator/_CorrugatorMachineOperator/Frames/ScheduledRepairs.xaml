<UserControl x:Class="Client.Interfaces.Production.Corrugator.CorrugatorMachineOperator.ScheduledRepairs"
    xmlns:local="clr-namespace:Client.Interfaces.Production"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"

    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"     
    mc:Ignorable="d"   
    d:DesignWidth="1200" d:DesignHeight="700"
    Style="{DynamicResource UserControlCanvas}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Style="{DynamicResource FormContainer}" Margin="1">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Style="{DynamicResource FormHeaderButtonsPanel}" >
            <Button Content="Закрыть"  Name="CloseButton" Click="CloseButton_Click" Style="{DynamicResource Button}" />
            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

            <Button Content="Добавить" Name="AddButton" Click="AddButton_Click" Style="{DynamicResource Button}" />
            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
            
            <Label Content="Узел" Style="{DynamicResource TopLabel}" />
            <ComboBox x:Name="Units"  Style="{DynamicResource FormFieldCombo}" Width="200" />
            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

            <Label Content="Поиск:"  Style="{DynamicResource TopLabel}" />
            <TextBox Style="{DynamicResource FormFieldTopPanel}" MinWidth="100" MaxWidth="800" Width="auto" TextWrapping="NoWrap" Name="SearchText" Text=""  />
        </StackPanel>

        <Border Grid.Row="1" Grid.Column="0" Style="{DynamicResource FormVerticalPanelSpacer}" ></Border>

        <Border Grid.Row="2" Style="{DynamicResource DataGridMainHolder}" >
            <TextBlock Text="Запланированные ремонты" Height="24" Padding="0,4,0,0" />
        </Border>
        
        <!--Список запланированных ремонтов-->
        <Border Grid.Row="3" Style="{DynamicResource DataGridMainHolder}" >
            <controls:GridBox x:Name="GridScheduledRepairs" />
        </Border>
        
        <Border Grid.Row="4" Style="{DynamicResource DataGridMainHolder}" >
            <TextBlock Text="Все ремонты" Height="24" Padding="0,4,0,0" />
        </Border>
        
        <!--Список всех ремонтов-->
        <Border Grid.Row="5" Style="{DynamicResource DataGridMainHolder}" >
            <controls:GridBox x:Name="GridAllRepairs" />
        </Border>

    </Grid>
</UserControl>
