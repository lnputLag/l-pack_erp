<UserControl 
    x:Class="Client.Interfaces.Production.Corrugator.CorrugatorMachineOperator.IdleEdit"
    xmlns:local="clr-namespace:Client.Interfaces.Production"
    xmlns:lib="clr-namespace:Client.Common"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"             
    mc:Ignorable="d" 
    Style="{DynamicResource UserControlCanvas}"
    >

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{DynamicResource FormContainerHolder}" Margin="1" >
        <Grid Style="{DynamicResource FormContainer}" >

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"  />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"  Grid.Column="0" Style="{DynamicResource FormControlPanel}" >
                <Grid ShowGridLines="false">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"  />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="0"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" >
                        <TextBlock Name="FormTitle"  TextAlignment="Left" Style="{DynamicResource FormHeaderTitleBar}"  />
                    </Border>

                    <Border Grid.Row="1" Grid.Column="0" BorderThickness="0" Grid.ColumnSpan="2">
                        <StackPanel Style="{DynamicResource FormHeaderButtonsPanel}" Name="FormToolbar" >
                            <Button Content="Сохранить" Name="AddButton"  Click="SaveButton_Click" Style="{DynamicResource FButtonPrimary}" VerticalAlignment="Top" />
                            <Button Content="Отмена"    Name="CancelButton" Click="CancelButton_Click" Style="{DynamicResource Button}" />
                        </StackPanel>
                    </Border>

                </Grid>
            </Grid>

            <Border Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Style="{DynamicResource FormVerticalPanelSpacer}"></Border>

            <Border Grid.Row="2" Grid.Column="0" >

                <Grid ShowGridLines="false" Grid.Row="0"  Grid.Column="0" Style="{DynamicResource FromGrid}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="800"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="*"></RowDefinition>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0"  Grid.Column="0" Style="{DynamicResource DataGridMainHolder}" >
                        <controls:GridBox x:Name="IdleReasonGrid" MinHeight="50" MaxHeight="500" Margin="5"/>
                    </Border>
                    
                    <Border Grid.Row="1"  Grid.Column="0" Style="{DynamicResource DataGridMainHolder}" >
                        <controls:GridBox x:Name="IdleReasonDetailGrid" MinHeight="50" MaxHeight="500" Margin="5" />
                    </Border>

                    <Border Grid.Row="2"  Grid.Column="0" Style="{DynamicResource DataGridMainHolder}" >
                        <TextBlock Margin="5,0,5,0" FontSize="18">Описание простоя:</TextBlock>
                    </Border>

                    <Border Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Style="{DynamicResource DataGridMainHolder}" >
                        <StackPanel Orientation="Horizontal" >
                            <TextBox Margin="5,0,5,0" Name="IdleReasonText" FontSize="18" MinWidth="430" MaxWidth="700" TextWrapping="Wrap" HorizontalAlignment="Left" Height="auto" Style="{DynamicResource FormField}" KeyDown="IdleReasonText_KeyDown" />
                            <Button Content="Скопировать" Margin="5 0 0 0" Height="30" VerticalAlignment="Bottom" Click="CopyButton_Click" Style="{DynamicResource Button}" />
                            <Button Content="Вставить" Margin="5 0 0 0" Height="30" VerticalAlignment="Bottom" Click="PasteButton_Click" Style="{DynamicResource Button}" />
                        </StackPanel>
                    </Border>

                </Grid>

            </Border>

            <Border Grid.Row="2" Grid.Column="1" >

                <Grid ShowGridLines="false" Grid.Row="0"  Grid.Column="0" Style="{DynamicResource FromGrid}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="600"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="*"></RowDefinition>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0"  Grid.Column="0" Style="{DynamicResource DataGridMainHolder}" >
                        <controls:GridBox x:Name="StanokUnitGrid" MinHeight="50" MaxHeight="400" Margin="5"/>
                    </Border>

                    <Border Grid.Row="1"  Grid.Column="0" Style="{DynamicResource DataGridMainHolder}" >
                        <controls:GridBox x:Name="DowntimeDefectTypeGrid" MinHeight="50" MaxHeight="500" Margin="5" />
                    </Border>

                    <Border Grid.Row="2"  Grid.Column="0" Style="{DynamicResource DataGridMainHolder}" >
                        <TextBlock Margin="5,0,5,0" FontSize="18">Комментарии технической службы:</TextBlock>
                    </Border>

                    <Border Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Style="{DynamicResource DataGridMainHolder}" >
                        <StackPanel Orientation="Horizontal" >
                            <TextBox  Margin="5,0,5,0" Name="MeasuresTakenText" FontSize="18" MinWidth="590" MaxWidth="800" TextWrapping="Wrap" HorizontalAlignment="Left" Height="auto" Style="{DynamicResource FormField}" KeyDown="IdleReasonText_KeyDown" />
                        </StackPanel>
                    </Border>

                </Grid>

            </Border>

        </Grid>
    </Border>

</UserControl>
