<controls:ControlBase x:Class="Client.Interfaces.Production.Corrugator.CorrugatorMachineOperator.CorrugatorMachinePlan"
              xmlns:local="clr-namespace:Client.Interfaces.Production"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"

    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"         
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"    
    xmlns:s="clr-namespace:System;assembly=mscorlib"

    mc:Ignorable="d" 
    d:DesignWidth="1200" d:DesignHeight="800"
    Style="{DynamicResource UserControlCanvas}"             
>

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Border x:Name="TaskPlaningBorder" BorderThickness="1" Style="{DynamicResource DataGridContainerHolder}"  >
        <Grid Style="{DynamicResource DataGridContainer}">

            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="GeneralColumnLeft" Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"  Grid.Column="0" Grid.ColumnSpan="3" Style="{DynamicResource GridControlPanel}" Panel.ZIndex="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Name="Toolbar" Background="White" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left" >

                    <Button Name="ShowButton"    Content="Обновить"  Style="{DynamicResource Button}" Click="ShowButton_Click"   />
                    <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>
                    
                    <Label Content="Время:" Style="{DynamicResource TopLabel}" />
                    <TextBox Name="PlanTimeTextBhs1" Text="" Width="30" Margin="0,0,5,0" PreviewTextInput="PlanTimeText_PreviewTextInput" />
                    <Button Name="SetNewTimeBhs1" Content="Задать" Style="{DynamicResource Button}" Click="SetNewTime_Click" />
                    <CheckBox Name="ChkAutoplanBhs1" Content="Автоплан БХС-1" Style="{DynamicResource CheckBoxTopPanel}" Click="AutoPlan_Checked"/>
                    <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"/>
                    
                    <Label Content="Время:" Style="{DynamicResource TopLabel}" />
                    <TextBox Name="PlanTimeTextBhs2" Text="" Width="30" Margin="0,0,5,0" PreviewTextInput="PlanTimeText_PreviewTextInput" />
                    <Button Name="SetNewTimeBhs2" Content="Задать" Style="{DynamicResource Button}" Click="SetNewTime_Click" />
                    <CheckBox Name="ChkAutoplanBhs2" Content="Автоплан БХС-2" Style="{DynamicResource CheckBoxTopPanel}" Click="AutoPlan_Checked"/>
                    <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"/>
                    
                    <Label Content="Время:" Style="{DynamicResource TopLabel}" />
                    <TextBox Name="PlanTimeTextFosber" Text="" Width="30" Margin="0,0,5,0" PreviewTextInput="PlanTimeText_PreviewTextInput" />
                    <Button Name="SetNewTimeFosber" Content="Задать" Style="{DynamicResource Button}" Click="SetNewTime_Click" />
                    <CheckBox Name="ChkAutoplanFosber" Content="Автоплан Фосбер" Style="{DynamicResource CheckBoxTopPanel}" Click="AutoPlan_Checked"/>
                </StackPanel>

               
            </Grid>


            <Border Grid.Row="2"  Grid.Column="0" Style="{DynamicResource DataGridMainHolder}" >
                <GroupBox Header="БХС 1">
                    <controls:GridBox4 x:Name="GridQueue2" />
                </GroupBox>
            </Border>
            <GridSplitter x:Name="SplitterHorizontal2" Grid.Row="3" Grid.Column="0" Height="5" Margin="1 0 0 0" HorizontalAlignment="Stretch" />
            
            <Border Grid.Row="4"  Grid.Column="0" Style="{DynamicResource DataGridMainHolder}" >
                <GroupBox Header="БХС 2">
                <controls:GridBox4 x:Name="GridQueue1"/>
                </GroupBox>
            </Border>
            <GridSplitter x:Name="SplitterHorizontal1" Grid.Row="5" Grid.Column="0"  Height="5" Margin="1 0 0 0" HorizontalAlignment="Stretch" />
            
            <Border Grid.Row="6"  Grid.Column="0" Style="{DynamicResource DataGridMainHolder}" >
                <GroupBox Header="Фосбер">
                    <controls:GridBox4 x:Name="GridQueue3" />
                </GroupBox>
            </Border>

            <Grid Grid.Row="2" Grid.Column="0" Grid.RowSpan="4" Name="GridSplashBlock" Visibility="Collapsed" Panel.ZIndex="10">
                <Border Background="#99ffffff">
                    <Border Margin="0,23,0,0" HorizontalAlignment="Center" VerticalAlignment="Top" BorderThickness="1" BorderBrush="#ffdedede" Width="350" Height="100" Background="#ffffffff">
                        <StackPanel Orientation="Vertical" >
                            <TextBlock HorizontalAlignment="Center" Margin="0,25,0,0" Foreground="#ff000000">Загрузка</TextBlock>
                            <ProgressBar Name="PositionGridSplashProgress" IsIndeterminate="True"  Minimum="0" Maximum="30" Value="0" Width="200" Height="15" Foreground="#ff0C79CA" Margin="0,10,0,0" />
                        </StackPanel>
                    </Border>
                </Border>
            </Grid>

        </Grid>
    </Border>
</controls:ControlBase>
