<UserControl 
    x:Class="Client.Interfaces.Production.MachineOperation"
    xmlns:local="clr-namespace:Client.Interfaces.Production"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"         
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"    
    xmlns:s="clr-namespace:System;assembly=mscorlib"

    mc:Ignorable="d" 
    d:DesignWidth="1035" d:DesignHeight="200" 
    Style="{DynamicResource UserControlCanvas}"    
>

    <UserControl.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>


    <Border Style="{DynamicResource DataGridContainerHolder}">
        <Grid Style="{DynamicResource DataGridContainer}" PreviewMouseWheel="Grid_PreviewMouseWheel">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="1*"></RowDefinition>
                <RowDefinition Height="1*"></RowDefinition>
                <RowDefinition Height="1*"></RowDefinition>
                <RowDefinition Height="1*"></RowDefinition>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="1" Grid.RowSpan="3" Name="ProgressContainer" Visibility="Collapsed" Panel.ZIndex="10">
                <controls:ProgressControl x:Name="Progress" Caption="Загрузка данных" />
            </Grid>

            <Grid Grid.Row="0"  Grid.Column="0" Style="{DynamicResource GridControlPanel}">
                <Border >
                    <Grid >

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="11.5*"/>
                            <ColumnDefinition Width="0.5*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>                            
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Name="GridToolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left"  >

                            <Label Content="Дата:"  Style="{DynamicResource TopLabel}"/>
                            <Grid Style="{DynamicResource FormFieldDateHolder}">
                                <!--<TextBox Name="Today"  MaxLength="10"  Style="{DynamicResource FormFieldDate}" TextChanged="DateTextChanged" IsReadOnly="True"  />
                                <DatePicker  SelectedDate="{Binding Path=Text, Mode=OneWayToSource, ElementName=Today, Converter={StaticResource DateDate} }"  Style="{DynamicResource FormFieldDP}"  Margin="0,1,0,0" >
                                </DatePicker>-->
                                <controls:DatePicker x:Name="Today" Style="{DynamicResource DateEditStyle}" TextChanged="DateTextChanged">
                                </controls:DatePicker>
                            </Grid>

                            <Button Name="RefreshButton"    Content="Показать"  Style="{DynamicResource Button}" Width="75" Click="RefreshButton_Click"   />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                            <!--
                            <Label Content="Поиск:"  Style="{DynamicResource TopLabel}" />
                            <TextBox Style="{DynamicResource FormFieldTopPanel}" Width="150" TextWrapping="NoWrap" Name="Search" Text=""  />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                            -->

                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="1" Style="{DynamicResource FormHeaderButtonsPanel}"  HorizontalAlignment="Right" >
                            <Button Name="HelpButton"  Width="24" MinWidth="24"  Content="?"  Style="{DynamicResource Button}" Click="HelpButton_Click" Margin="0" />
                        </StackPanel>

                    </Grid>
                </Border>
            </Grid>

            <Grid Grid.Row="1"  Grid.Column="0" >
                <Border Style="{DynamicResource DataGridMainHolder}" BorderBrush="#ff900000" BorderThickness="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <controls:GraphBox x:Name="Graph1"  />
                </Border>
            </Grid>

            <Grid Grid.Row="2"  Grid.Column="0" >
                <Border Style="{DynamicResource DataGridMainHolder}" BorderBrush="#ff900000" BorderThickness="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <controls:GraphBox x:Name="Graph2"  />
                </Border>
            </Grid>

            <Grid Grid.Row="3"  Grid.Column="0" >
                <Border Style="{DynamicResource DataGridMainHolder}" BorderBrush="#ff900000" BorderThickness="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <controls:GraphBox x:Name="Graph3"  />
                </Border>
            </Grid>

            <Grid Grid.Row="4"  Grid.Column="0" >
                <Border Style="{DynamicResource DataGridMainHolder}" BorderBrush="#ff900000" BorderThickness="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <controls:GraphBox x:Name="Graph4"  />
                </Border>
            </Grid>

        </Grid>
    </Border>

</UserControl>
