<UserControl 
    x:Class="Client.Interfaces.Production.RollReceiptViewer"
    xmlns:local="clr-namespace:Client.Interfaces.Shipments"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"         
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"    
    xmlns:s="clr-namespace:System;assembly=mscorlib"

    mc:Ignorable="d" 
    d:DesignWidth="800" d:DesignHeight="520" 
    Style="{DynamicResource UserControlCanvasTouch}"
>

    <UserControl.Resources>
        <ResourceDictionary>

            <Style x:Key="DocumentViewer" TargetType="DocumentViewer">
                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}" />
                <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" />
                <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type DocumentViewer}">
                            <Border BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}" Focusable="False">
                                <Grid KeyboardNavigation.TabNavigation="Local">
                                    <Grid.Background>
                                        <SolidColorBrush Color="{DynamicResource ControlLightColor}" />
                                    </Grid.Background>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <!--
                                    <ToolBar ToolBarTray.IsLocked="True" KeyboardNavigation.TabNavigation="Continue">
                                        <Button  Content="Печатать" Click="PrintButtonClick" />
                                        <Separator />
                                        <Button Command="NavigationCommands.IncreaseZoom"               CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}" Content=" + " />
                                        <Separator />
                                        <Button Command="NavigationCommands.DecreaseZoom"               CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}" Content=" - " />
                                        <Separator />
                                        <Button Command="NavigationCommands.Zoom"                       CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}" CommandParameter="100.0" Content="100%" />
                                        <Separator />
                                        <Button Command="DocumentViewer.FitToWidthCommand"              CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}" Content="По ширине" />
                                        <Separator />
                                        <Button Command="DocumentViewer.FitToMaxPagesAcrossCommand"     CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}" CommandParameter="1" Content="Уместить на странице" />

                                    </ToolBar>
                                    -->

                                    <ScrollViewer Grid.Row="1"  CanContentScroll="true"  HorizontalScrollBarVisibility="Auto" x:Name="PART_ContentHost" IsTabStop="true" >
                                        <ScrollViewer.Background>
                                            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                                <GradientStop Color="{DynamicResource ControlLightColor}"   Offset="0" />
                                                <GradientStop Color="{DynamicResource ControlMediumColor}"  Offset="1" />
                                            </LinearGradientBrush>
                                        </ScrollViewer.Background>
                                    </ScrollViewer>

                                    <ContentControl Grid.Row="2"    x:Name="PART_FindToolBarHost" Visibility="Collapsed"  />
                                </Grid>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>


    <Border Style="{DynamicResource TouchFrameBlock}"  >
        <Grid Style="{DynamicResource TouchFrame}">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"  Grid.Column="0" Grid.RowSpan="2" Name="Splash" Panel.ZIndex="20" Visibility="Hidden">
                <Border HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#ffffff">
                    <TextBlock FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center">Печать ярлыка</TextBlock>
                </Border>
            </Border>

            <Border Grid.Row="0"  Grid.Column="0" Style="{DynamicResource TouchFromGridBlock}" >
                <Border Style="{DynamicResource TouchFrameFormPanelBlock}" BorderBrush="#900000" BorderThickness="0" >
                    <Grid Style="{DynamicResource TouchFrameFormPanel}">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="8*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="4*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"></RowDefinition>
                        </Grid.RowDefinitions>

                        
                        <Border Grid.Row="0" Grid.Column="0" Style="{DynamicResource TouchFromGridBlock}" BorderBrush="#bbbbbb" BorderThickness="1"  >
                            <DocumentViewer Name="ReceiptViewer" Style="{StaticResource DocumentViewer}" HorizontalAlignment="Stretch" />
                        </Border>

                        <Border Grid.Row="0" Grid.Column="2" Style="{DynamicResource TouchFromGridBlock}" BorderBrush="#bbbbbb" BorderThickness="1" >
                            <StackPanel Orientation="Vertical">
                                <ScrollViewer VerticalScrollBarVisibility="Visible" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                                    <TextBox Name="ReceiptLog" HorizontalAlignment="Stretch" IsReadOnly="True" MinWidth="300" MinHeight="200" BorderThickness="0"></TextBox>
                                </ScrollViewer>

                                <StackPanel Orientation="Vertical" Width="170">
                                    <Button Name="InstallButton" Click="InstallButton_Click"  Content="Драйвер"     Style="{DynamicResource TouchFormButton}" Margin="0,0,0,10"  Visibility="Collapsed" />
                                    <Button Name="SetupButton" Click="SetupButton_Click"  Content="Настроить"     Style="{DynamicResource TouchFormButton}" Margin="0,0,0,10"   />
                                    <Button Name="OpenButton" Click="OpenButton_Click"  Content="Открыть"     Style="{DynamicResource TouchFormButton}"  Visibility="Collapsed" Margin="0,0,0,10" />
                                    <Button Name="RefreshButton" Click="RefreshButton_Click"  Content="Обновить"     Style="{DynamicResource TouchFormButton}"  Margin="0,0,0,10"  />
                                    <Button Name="PrintButton" Click="PrintButton_Click"  Content="Печать"     Style="{DynamicResource TouchFormButton}" Margin="0,0,0,10"   />
                                </StackPanel>
                                
                            </StackPanel>
                            
                        </Border>

                    </Grid>

                </Border>
            </Border>

            <Border Grid.Row="1"  Grid.Column="0" Style="{DynamicResource TouchFrameControlPanelBlock}" >
                <Grid Style="{DynamicResource TouchFrameControlPanel}" >

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="12*"/>
                        <ColumnDefinition Width="0*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Style="{DynamicResource TouchFrameControlPanelLeft}"   >
                        <TextBlock  Name="FormStatus" Text="Не все обязательные поля заполнены верно" TextAlignment="Left" Style="{DynamicResource TouchFormStatusBar}"  />
                    </StackPanel>
                    <StackPanel Grid.Row="0" Grid.Column="1" Style="{DynamicResource TouchFrameControlPanelRight}" >
                    </StackPanel>

                </Grid>
            </Border>

            <Border Grid.Row="2"  Grid.Column="0" Style="{DynamicResource TouchFrameControlPanelBlock}" >
                <Grid Style="{DynamicResource TouchFrameControlPanel}" >

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Name="Toolbar"  Style="{DynamicResource TouchFrameControlPanelLeft}"   >

                    </StackPanel>
                    <StackPanel Grid.Row="0" Grid.Column="1" Style="{DynamicResource TouchFrameControlPanelRight}" >

                        

                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                        <Button Name="CloseButton"   Click="CloseButton_Click" Content="Закрыть"  Style="{DynamicResource TouchFormButtonPrimary}" Margin="0" Width="150"  />
                        
                    </StackPanel>

                </Grid>
            </Border>

        </Grid>
    </Border>

</UserControl>

    