<UserControl 
    x:Class="Client.Interfaces.Production.ServiceWaste"
    xmlns:local="clr-namespace:Client.Interfaces.Production"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"         
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"    
    xmlns:s="clr-namespace:System;assembly=mscorlib"

    mc:Ignorable="d" 
    d:DesignWidth="520" d:DesignHeight="260" 
    Width="530" Height="260"
    Style="{DynamicResource UserControlCanvas}"            
>

    <UserControl.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>


    <Border Style="{DynamicResource DataGridContainerHolder}" Width="520" HorizontalAlignment="Center">
        <Grid Style="{DynamicResource DataGridContainer}">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>

            <!--
            <Border Grid.Row="0" Grid.Column="0" >
                <Grid ShowGridLines="false" Style="{DynamicResource FromGrid2Top}" >
                    <StackPanel Style="{DynamicResource FormHeaderButtonsPanelR}" >
                        <Button Name="HelpButton" ToolTip="Документация" Width="24" MinWidth="24"  Content="?"  Style="{DynamicResource Button}" Click="HelpButton_OnClick"  />
                    </StackPanel>
                </Grid>
            </Border>
            -->

            <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="10">
                <StackPanel Orientation="Vertical">
                    <TextBlock>
                        Импорт данных по отходам из файла. Выберите файл в специальном формате.
                    </TextBlock>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Grid.Column="0" Height="50">
                <Grid ShowGridLines="false" Style="{DynamicResource FromGrid2}" >

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.2*"/>
                        <ColumnDefinition Width="3*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Grid.Column="0" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="Файл *"  Style="{DynamicResource FormLabel}"  />
                    </Border>
                    <Border Grid.Row="0"  Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                        <StackPanel Orientation="Vertical">
                            <StackPanel Orientation="Horizontal">
                                <TextBox Name="File" Style="{DynamicResource FormField}" Width="341" TextWrapping="NoWrap" IsReadOnly="True" >
                                </TextBox>

                                <Button Name="FileSelectButton"   Click="FileSelectButton_Click" ToolTip="Выбрать"   Style="{DynamicResource ButtonGlyph}"  Margin="-1,0,0,0"  >
                                    <Image Style="{DynamicResource SelectImage}" />
                                </Button>
                            </StackPanel>

                            <TextBlock Foreground="#cccccc">Тип файла: XLSX</TextBlock>
                        </StackPanel>
                    </Border>

                </Grid>
            </Border>

            <Grid Grid.Row="2"  Grid.Column="0" Style="{DynamicResource GridControlPanel}">
                <Border>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Name="Toolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Right"  >
                            <ProgressBar Name="Progress" Width="150" Height="10" Margin="0,2,10,0" IsIndeterminate="True" Foreground="#ff0078D7" />
                            <TextBlock  Name="FormStatus" Text="Пожалуйста, выберите файл" TextAlignment="Left" Style="{DynamicResource FStatusBar2}" HorizontalAlignment="Stretch" Margin="0,0,10,0" />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                            <Button Name="SaveButton"  Click="SaveButton_Click" Content="Загрузить"  Style="{DynamicResource FButtonPrimary}"    />
                        </StackPanel>

                    </Grid>
                </Border>
            </Grid>

            <Grid Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" >
                <StackPanel Margin="5" >
                    <TextBox Name="ResultMetrics" MinHeight="120" FontFamily="Courier New" FontSize="12" VerticalScrollBarVisibility="Visible" TextWrapping="Wrap" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

                    </TextBox>
                </StackPanel>
            </Grid>

        </Grid>
    </Border>

</UserControl>
