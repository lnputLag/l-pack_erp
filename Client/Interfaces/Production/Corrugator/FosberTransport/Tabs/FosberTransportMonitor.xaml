<UserControl x:Class="Client.Interfaces.Production.FosberTransportMonitor"
    xmlns:local="clr-namespace:Client.Interfaces.Production"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"         
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"    
    xmlns:s="clr-namespace:System;assembly=mscorlib"

    mc:Ignorable="d" 
    d:DesignWidth="1000" d:DesignHeight="500"
    Style="{DynamicResource UserControlCanvas}"             
>
    <UserControl.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{DynamicResource DataGridContainerHolder}"  >
        <Grid Style="{DynamicResource DataGridContainer}">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="0"/>
                <ColumnDefinition Width="0"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="20"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <TextBlock Grid.Column="0" Grid.Row="2" VerticalAlignment="Center">Рулоны в буфере</TextBlock>
            <Grid Grid.Row="3"  Grid.Column="0" >
                <Border Style="{DynamicResource DataGridMainHolder}" >
                    <controls:GridBox x:Name="RollsGrid" />
                </Border>
            </Grid>

            <TextBlock Grid.Column="2" Grid.Row="2" VerticalAlignment="Center">Рулоны в транспортной системе</TextBlock>
            <Grid Grid.Row="3"  Grid.Column="2"  >
                <Border Style="{DynamicResource DataGridMainHolder}" >
                    <controls:GridBox x:Name="DB900Grid" />
                </Border>
            </Grid>

            <Grid Grid.Row="0"  Grid.Column="0" Grid.ColumnSpan="3" Style="{DynamicResource GridControlPanel}">
                <Border >
                    <Grid >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Name="ToolbarDiagram" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left"  >
                            <Button Name="RefreshButtonDiagram"    Content="Обновить"  Style="{DynamicResource Button}" Click="RefreshButtonDiagram_Click" VerticalAlignment="Bottom"  />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                            <CheckBox Name="DiagramAutoUpdate" Style="{DynamicResource CheckBoxTopPanel}" Click="DiagramAutoUpdate_Click">Автообновление</CheckBox>
                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="1" Style="{DynamicResource FormHeaderButtonsPanel}"  HorizontalAlignment="Right" >
                            <Button Name="HelpButton"  Width="24" MinWidth="24"  Content="?"  Style="{DynamicResource Button}" HorizontalAlignment="Right" Margin="0" Click="HelpButton_Click" />
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>

            <Border Grid.Row="1"  Grid.Column="0" Grid.ColumnSpan="3" Grid.RowSpan="1" BorderThickness="1" BorderBrush="LightGray">
                <Viewbox HorizontalAlignment="Stretch" VerticalAlignment="Stretch"   >
                    <local:FosberTransportDiagram x:Name="TransportDiagram" Margin="0" />
                </Viewbox>
            </Border>
        </Grid>
    </Border>

</UserControl>
