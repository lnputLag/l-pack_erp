<UserControl x:Class="Client.Interfaces.Production.StackerManuallyPrintLabelCreator"
    xmlns:local="clr-namespace:Client.Interfaces.Production"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"         
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"    
    xmlns:s="clr-namespace:System;assembly=mscorlib"
             
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="600"
             
    Width="300" Height="70"
    Style="{DynamicResource UserControlCanvas}"  
    HorizontalAlignment="Left" VerticalAlignment="Top">

    <UserControl.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{DynamicResource FormContainerHolder}" Margin="1" >
        <Grid Style="{DynamicResource FormContainer}" >

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Grid.Column="0">
                <Grid Style="{DynamicResource FromGrid}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Grid.Column="0" HorizontalAlignment="Center" Name="PalletNumberGrid">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>
                        
                        <Border Grid.Column="0"  Grid.Row="0" Style="{DynamicResource FormLabelContainer}">
                            <Label Content="Порядковый номер поддона :" Style="{DynamicResource FormLabel}" />
                        </Border>
                        <Border Grid.Column="1" Grid.Row="0" Style="{DynamicResource FormFieldContainer}">
                            <TextBox Name="PalletNumberTextBox" Text="101" Width="40" TextWrapping="NoWrap" Style="{DynamicResource FormField}" />
                        </Border>
                    </Grid>

                    <Grid Grid.Row="1" Grid.Column="0" HorizontalAlignment="Center" Name="QuantityPalletGrid">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Column="0" Grid.Row="1" Style="{DynamicResource FormLabelContainer}">
                            <Label Content="Количество новых ярлыков :" Style="{DynamicResource FormLabel}" />
                        </Border>
                        <Border Grid.Column="1" Grid.Row="1" Style="{DynamicResource FormFieldContainer}">
                            <TextBox Name="QuantityPalletTextBox" Text="101" Width="40" TextWrapping="NoWrap" Style="{DynamicResource FormField}" />
                        </Border>
                    </Grid>

                </Grid>
            </Border>

            <Border Grid.Row="1" Grid.Column="0" Style="{DynamicResource FormVerticalPanelSpacer}"></Border>

            <Grid Grid.Row="2"  Grid.Column="0" Style="{DynamicResource FormControlPanel}" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Name="FormToolbar" HorizontalAlignment="Right" Grid.Row="0" Grid.Column="0" Style="{DynamicResource FormHeaderButtonsPanel}" >
                    <Button Name="SaveButton" Content="Применить" Click="SaveButton_Click"  Style="{DynamicResource FButtonPrimary}"/>
                    <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                    <Button Name="CancelButton" Content="Отмена" Click="CancelButton_Click"  Style="{DynamicResource Button}"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</UserControl>

