<UserControl 
    x:Class="Client.Interfaces.Production.RollListForCompletedTasks"
    xmlns:local="clr-namespace:Client.Interfaces.Production"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"         
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"    
    xmlns:s="clr-namespace:System;assembly=mscorlib"

    mc:Ignorable="d" 
    d:DesignWidth="1980" d:DesignHeight="400" 
    Style="{DynamicResource UserControlCanvas}"  
>

    <UserControl.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{DynamicResource DataGridContainerHolder}"  >
        <Grid Style="{DynamicResource DataGridContainer}">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5.5*" />
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="5*" MinWidth="570" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*" MinHeight="200" MaxHeight="200"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"  Grid.Column="0" Style="{DynamicResource GridControlPanel}">
                <Border >
                    <Grid >

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="12*"/>
                            <ColumnDefinition Width="0*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                        </Grid.RowDefinitions>

                        <WrapPanel Grid.Column="0" Name="TaskGridToolbar">

                            <StackPanel Style="{DynamicResource FormHeaderButtonsPanel}" Margin="0,4,0,0">
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
                                <Label Content="С:" Style="{DynamicResource TopLabel}" VerticalAlignment="Center"/>
                                <Grid Style="{DynamicResource FormFieldDateHolder}">
                                    <!--<TextBox Name="FromDate"  MaxLength="10" TextChanged="FromDateTextChanged" Style="{DynamicResource FormFieldDate}" IsReadOnly="False" />
                                <DatePicker  SelectedDate="{Binding Path=Text, Mode=OneWayToSource, ElementName=FromDate, Converter={StaticResource DateDate} }"  Style="{DynamicResource FormFieldDP}" >
                                </DatePicker>-->

                                    <controls:DatePicker x:Name="FromDate" Style="{DynamicResource DateEditStyle}" TextChanged="FromDateTextChanged">
                                    </controls:DatePicker>
                                </Grid>
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>

                                <Label Content="По:" Style="{DynamicResource TopLabel}" VerticalAlignment="Center"/>
                                <Grid Style="{DynamicResource FormFieldDateHolder}">
                                    <!--<TextBox Name="ToDate"  MaxLength="10"  Style="{DynamicResource FormFieldDate}" TextChanged="ToDateTextChanged" IsReadOnly="False" IsEnabled="True" />
                                <DatePicker  SelectedDate="{Binding Path=Text, Mode=OneWayToSource, ElementName=ToDate, Converter={StaticResource DateDate} }"  Style="{DynamicResource FormFieldDP}"  >
                                </DatePicker>-->
                                    <controls:DatePicker x:Name="ToDate" Style="{DynamicResource DateEditStyle}" TextChanged="ToDateTextChanged">
                                    </controls:DatePicker>

                                </Grid>
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>

                                <Button Name="RefreshButton"  Content="Показать"  Style="{DynamicResource Button}" Click="RefreshButton_Click" VerticalAlignment="Bottom"   />
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
                            </StackPanel>

                            <StackPanel Style="{DynamicResource FormHeaderButtonsPanel}" Margin="0,4,0,0">
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
                                <Label Name="SearchTaskLabel" Content="Поиск:"  Style="{DynamicResource TopLabel}" VerticalAlignment="Center" />
                                <TextBox Name="SearchTask" Style="{DynamicResource FormFieldTopPanel}" Width="150" TextWrapping="NoWrap"  Text="" />
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
                            </StackPanel>

                            <StackPanel Style="{DynamicResource FormHeaderButtonsPanel}" Margin="0,4,0,0">
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
                                <Button  Name="CM1Button" Content="ГА1"  Style="{DynamicResource FButtonPrimary}" MinWidth="30" Width="70" Click="CM1Button_Click" HorizontalAlignment ="Stretch" />
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
                                <Button  Name="CM2Button" Content="ГА2"  Style="{DynamicResource Button}" MinWidth="30" Width="70" Click="CM2Button_Click" HorizontalAlignment ="Stretch" />
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
                                <Button  Name="CM3Button" Content="ГА3"  Style="{DynamicResource Button}" MinWidth="30" Width="70" Click="CM3Button_Click" HorizontalAlignment ="Stretch" />
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
                                <Button  Name="KshCM1Button" Content="ГА КШ"  Style="{DynamicResource Button}" MinWidth="30" Width="70" Click="KshCM1Button_Click" HorizontalAlignment ="Stretch" />
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
                            </StackPanel>

                            <StackPanel Style="{DynamicResource FormHeaderButtonsPanel}" Margin="0,4,0,0">
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
                                <Button Name="ExportButton"    Content="В Excel"  Style="{DynamicResource Button}" Width="70" Click="ExportButton_Click"  />
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
                            </StackPanel>

                        </WrapPanel>
                    </Grid>
                </Border>
            </Grid>

            <Grid Grid.Row="1"  Grid.Column="0" Grid.RowSpan="4" >
                <Border Style="{DynamicResource DataGridMainHolder}" >
                    <controls:GridBox x:Name="TaskGrid" />
                </Border>
            </Grid>



            <Grid Grid.Row="0"  Grid.Column="2" Style="{DynamicResource GridControlPanel}">
                <Border >
                    <Grid >

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <WrapPanel Grid.Row="0" Grid.Column="0" Name="RollsSelectedGridToolbar">
                            <StackPanel  Style="{DynamicResource FormHeaderButtonsPanel}" Margin="0,4,0,0">
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
                                <Button Name="AddRollButton"  Tag="access_mode_full_access"  Content="Добавить"  Style="{DynamicResource Button}" Width="70" Click="AddRollButton_Click" VerticalAlignment="Stretch" Height="Auto" />
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
                                <Button Name="DelRollButton"  Tag="access_mode_full_access"  Content="Удалить"  Style="{DynamicResource Button}" Width="70" Click="DelRollButton_Click" />
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
                            </StackPanel>

                            <StackPanel  Style="{DynamicResource FormHeaderButtonsPanel}" Margin="0,4,0,0">
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
                                <Button Grid.Column="0" Name="Reel1Button"    Content="Слой 1"  Style="{DynamicResource FButtonPrimary}" HorizontalAlignment ="Stretch" Width="70" Click="Reel1Button_Click" />
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
                                <Button Grid.Column="1" Name="Reel2Button"    Content="Слой 2"  Style="{DynamicResource Button}" HorizontalAlignment ="Stretch" Width="70" Click="Reel2Button_Click" />
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
                                <Button Grid.Column="2" Name="Reel3Button"    Content="Слой 3"  Style="{DynamicResource Button}" HorizontalAlignment ="Stretch" Width="70" Click="Reel3Button_Click" />
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
                                <Button Grid.Column="3" Name="Reel4Button"    Content="Слой 4"  Style="{DynamicResource Button}" HorizontalAlignment ="Stretch" Width="70" Click="Reel4Button_Click" />
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
                                <Button Grid.Column="4" Name="Reel5Button"    Content="Слой 5"  Style="{DynamicResource Button}" HorizontalAlignment ="Stretch" Width="70" Click="Reel5Button_Click" />
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
                            </StackPanel>
                        </WrapPanel>

                        <StackPanel Grid.Row="0" Grid.Column="1" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Right" Margin="0,4,0,0" VerticalAlignment="Top">
                            <Button Name="SettingsButton"    ToolTip="Настройка"   Style="{DynamicResource ButtonGlyph}" Click="SettingsButton_Click"  Margin="0,0,5,0"  >
                                <Image Style="{DynamicResource SettingsImage}" />
                            </Button>
                            <Button Grid.Column="5" Name="HelpButton"  Width="24" MinWidth="24"  Content="?"  Style="{DynamicResource Button}" Click="HelpButton_Click" />
                        </StackPanel>

                    </Grid>
                </Border>
            </Grid>

            <Grid Grid.Row="1"  Grid.Column="2" >
                <Border Style="{DynamicResource DataGridMainHolder}" >
                    <controls:GridBox x:Name="RollSelectedGrid" />
                </Border>
            </Grid>


            <Grid Grid.Row="2"  Grid.Column="2" Style="{DynamicResource GridControlPanel}">
                <Border >
                    <Grid >

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="12*"/>
                            <ColumnDefinition Width="0*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Name="RollsAllGridToolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left"  >
                            <Label Content="Поиск:"  Style="{DynamicResource TopLabel}" />
                            <TextBox Name="SearchRoll" Style="{DynamicResource FormFieldTopPanel}" Width="150" TextWrapping="NoWrap"  Text=""  />
                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="1" Style="{DynamicResource FormHeaderButtonsPanel}"  HorizontalAlignment="Right" >
                        </StackPanel>

                    </Grid>
                </Border>
            </Grid>

            <Grid Grid.Row="3"  Grid.Column="2" >
                <Border Style="{DynamicResource DataGridMainHolder}" >
                    <controls:GridBox x:Name="RollGrid" />
                </Border>
            </Grid>

        </Grid>
    </Border>

</UserControl>
