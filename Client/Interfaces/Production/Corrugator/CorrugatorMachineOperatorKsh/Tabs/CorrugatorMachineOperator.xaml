<UserControl x:Class="Client.Interfaces.Production.Corrugator.CorrugatorMachineOperatorKsh.CorrugatorMachineOperator"
    xmlns:local="clr-namespace:Client.Interfaces.Production.Corrugator.CorrugatorMachineOperatorKsh"

    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"         

    mc:Ignorable="d" 
    d:DesignWidth="1200" d:DesignHeight="800"
    Style="{DynamicResource UserControlCanvas}"             
>
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>


    <Border Style="{DynamicResource DataGridContainerHolder}"  >
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.35*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="0.35*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <!--Строка состояния-->
                <RowDefinition Height="30"></RowDefinition>
                <!--График скорости, заполненность буфера и СГП, простои-->
                <RowDefinition Height="170"></RowDefinition>
                <!--Кнопки-->
                <RowDefinition Height="auto"></RowDefinition>
                <!--Список ПЗ-->
                <RowDefinition Height="*"></RowDefinition>
                <!--Показатели и сырьё-->
                <RowDefinition Height="220"></RowDefinition>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0"  Grid.Column="0" Grid.ColumnSpan="5" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="left" >
                <Button Name="ButtonUpdate" Content="Обновить" Style="{DynamicResource Button}" Click="ButtonUpdate_Click" />
                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                
                <TextBlock Name="CurrentTime" Text="08:00"  VerticalAlignment="Bottom" FontSize="20" FontWeight="Bold" />
                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
 
                <TextBlock Name="FireStatus" Text="Пожар" Foreground="Red" VerticalAlignment="Bottom" FontSize="20" FontWeight="SemiBold" />
                <Border Style="{DynamicResource FormButtonsPanelSpacer}" />
                <TextBlock Name="StatusReadTimeStamp" Text="" Foreground="Black" VerticalAlignment="Bottom" FontSize="20" FontWeight="SemiBold" />
            </StackPanel>

            <StackPanel Grid.Row="0"  Grid.Column="0" Grid.ColumnSpan="5" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Right" >
                <CheckBox x:Name="ShowOldIdle" Content="Показать простои предыдущих смен" Style="{DynamicResource CheckBoxTopPanel}"
                                    Checked="ShowOldIdle_Checked" Unchecked="ShowOldIdle_Unchecked"/>
                
                <Button Name="FireAlarmButton" MinWidth="60" Content="Пожар" Margin="0" Style="{DynamicResource ButtonFire}" Click="FireAlarmButton_Click" />
                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                <Button Name="HelpButton" MinWidth="24" Content="?" Margin="0" Style="{DynamicResource Button}" Click="HelpButton_Click"  />
            </StackPanel>


            <Grid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="5" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="130"/>
                    <ColumnDefinition Width="400"/>
                </Grid.ColumnDefinitions>
                
                <Border Grid.Column="0" Style="{DynamicResource DataGridMainHolder}" BorderThickness="1" BorderBrush="Silver" >
                    <local:SpeedChart x:Name="SpeedChart"/>
                </Border>

                <Border Grid.Column="1" Style="{DynamicResource DataGridMainHolder}" BorderThickness="1" BorderBrush="Silver" Margin="5 0">
                    <local:ColumnChart x:Name="StorageStateChart" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
                </Border>

                <Border Grid.Column="2" Style="{DynamicResource DataGridMainHolder}" Name="IdleBorderGrid">
                    <local:IdleList x:Name="IdleGrid" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                </Border>
                
                <Border Grid.Column="2" Style="{DynamicResource DataGridMainHolder}" Name="SpeedProfileBorderGrid" Visibility="Collapsed">
                    <local:ProfileSpeed x:Name="ProfileSpeedGrid" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                </Border>
            </Grid>

            <Grid Grid.Row="2"  Grid.Column="0" Grid.ColumnSpan="5" Style="{DynamicResource GridControlPanel}">
                <Border >
                    <StackPanel Name="ToolbarDiagram" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left"  >
                        <Button IsEnabled="True" Name="ButtonTaskList"     Content="Показать все"  Style="{DynamicResource Button}" Click="ButtonListTaskTotal_Click" />
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                        <Button Tag="access_mode_full_access" Name="ButtonChangeRolls" Content="Сменить валы" Style="{DynamicResource Button}" Click="ButtonChangeRolls_Click" />
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                        <Button Visibility="Collapsed" Name="ButtonDeleteTask"  Content="Удалить"   Style="{DynamicResource Button}" Click="ButtonDeleteTask_Click" />
                        <Border Visibility="Collapsed" Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                        <!-- <Button Tag="access_mode_full_access" Name="ButtonUP"           Content="В начало"  ToolTip="Поместить в начало очереди" Style="{DynamicResource Button}" Click="ButtonTaskUP_Click" /> -->
                        <Button Tag="access_mode_full_access" Name="ButtonUp"           Content="Выше"      ToolTip="Перенести на строку выше" Style="{DynamicResource Button}" Click="ButtonTaskUp_Click" MinWidth="40" />
                        <Button Tag="access_mode_full_access" Name="ButtonDown"         Content="Ниже"      ToolTip="Перенести на строку ниже" Style="{DynamicResource Button}" Click="ButtonTaskDown_Click" MinWidth="40" />

                        <Button Tag="access_mode_full_access" Name="ButtonDelete"         Content="Удалить"      ToolTip="Удалить выбранное задание" Style="{DynamicResource Button}" Click="ButtonDeleteTask_Click" MinWidth="40" IsEnabled="False" />
                        <Button Tag="access_mode_full_access" Name="ButtonDeleteSelected" Content="Удалить выбранные" ToolTip="Удалить все выбранные задания" Style="{DynamicResource Button}" Click="ButtonDeleteSelectedTasks_Click" MinWidth="80" IsEnabled="False" />
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                        <Button Name="ButtonApply" Tag="access_mode_full_access" Content="Применить" Style="{DynamicResource Button}" Click="ButtonApply_Click" />

                        <Button Name="IsJSButton" Content="J.S" Style="{DynamicResource Button}" Click="ButtonCheckWhatCM_Click" MinWidth="40" />
                    </StackPanel>
                </Border>
                <Border >
                    <StackPanel Name="ToolbarDiagram2" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Right"  >
                        <Button Tag="access_mode_full_access" Name="AutoSyncControlBtn" Content="Автосинхронизация" Style="{DynamicResource FButtonPrimary}" Click="AutoSyncControlBtn_OnClick"/>
                        <Button Tag="access_mode_full_access" Name="ButtonProblemPallets"   Content="Проблемные поддоны" Style="{DynamicResource Button}" Click="ButtonProblemPallets_Click" />
                        <Button Tag="access_mode_full_access" Name="ButtonDefectReasons"    Content="Причины брака"      Style="{DynamicResource Button}" Click="ButtonDefectReasons_Click" />
                        <Button Visibility="Collapsed" Name="ButtonScheduledRepairs" Content="Плановые ремонты"   Style="{DynamicResource Button}" Click="ButtonScheduledRepairs_Click" />
                        <Button Tag="access_mode_full_access" Name="ButtonOperatorLog"      Content="Журнал оператора"   Style="{DynamicResource Button}" Click="ButtonLogbook_Click" />
                        <Button Tag="access_mode_full_access" Name="SpeedAnyProfile" Content="Скорость по профилю" Style="{DynamicResource Button}" Click="SpeedAnyProfile_Click" />
                    </StackPanel>
                </Border>
            </Grid>
            
            <!--Список ПЗ-->
            <Border Grid.Row="3"  Grid.Column="0" Grid.ColumnSpan="5" Style="{DynamicResource DataGridMainHolder}" >
                <local:MachineTaskQueue x:Name="TaskQueue"/>
            </Border>

            <!--Показатели задания-->
            <Grid Grid.Row="4"  Grid.Column="0" Margin="0, 5, 0, 0">
                <Border Style="{DynamicResource DataGridMainHolder}" BorderThickness="1" BorderBrush="Silver">
                    <local:TaskParameters x:Name="TaskData" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
                </Border>
            </Grid>

            <!--Показатели смены-->
            <Grid Grid.Row="4"  Grid.Column="4" Margin="0, 5, 0, 0">
                <Border Style="{DynamicResource DataGridMainHolder}" BorderThickness="1" BorderBrush="Silver">
                    <local:ShiftParameters x:Name="ShiftData" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
                </Border>
            </Grid>

            <!--Задания по времени и списки рулонов-->
            <Grid Grid.Row="4"  Grid.Column="2" Margin="0, 5, 0, 0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>

                <Border Grid.Column="0" Grid.Row="0" Background="#ffff88ad" BorderThickness="1" BorderBrush="Silver" Margin="0,0,0,5" ToolTip="Плановое начало выполнения ПЗ просрочено." Style="{DynamicResource DataGridMainHolder}" >
                    <TextBlock Name="TaskLength1" HorizontalAlignment="Center" VerticalAlignment="Center" ToolTip="Плановое начало выполнения ПЗ просрочено.">0</TextBlock>
                </Border>
                <Border Grid.Column="2" Grid.Row="0" Background="#FFFFC182" BorderThickness="1" BorderBrush="Silver" Margin="0,0,0,5" ToolTip="До планового начала выполнения ПЗ менее 2 часов." Style="{DynamicResource DataGridMainHolder}" >
                    <TextBlock Name="TaskLength2" HorizontalAlignment="Center" VerticalAlignment="Center" ToolTip="До планового начала выполнения ПЗ менее 2 часов." >0</TextBlock>
                </Border>
                <Border Grid.Column="4" Grid.Row="0" Background="#FFffe28e" BorderThickness="1" BorderBrush="Silver" Margin="0,0,0,5" ToolTip="До планового начала выполнения ПЗ менее 4 часов." Style="{DynamicResource DataGridMainHolder}" >
                    <TextBlock Name="TaskLength3" HorizontalAlignment="Center" VerticalAlignment="Center" ToolTip="До планового начала выполнения ПЗ менее 4 часов.">0</TextBlock>
                </Border>
                <Border Grid.Column="6" Grid.Row="0" Background="#FFcccccc" BorderThickness="1" BorderBrush="Silver" Margin="0,0,0,5" ToolTip="До планового начала выполнения ПЗ более 4 часов." Style="{DynamicResource DataGridMainHolder}" >
                    <TextBlock Name="TaskLength4" HorizontalAlignment="Center" VerticalAlignment="Center" ToolTip="До планового начала выполнения ПЗ более 4 часов.">0</TextBlock>
                </Border>
                <Border Grid.Column="8" Grid.Row="0" Background="White" BorderThickness="1" BorderBrush="Silver" Margin="0,0,0,5" ToolTip="Общая длина заданий в очереди." Style="{DynamicResource DataGridMainHolder}" >
                    <TextBlock Name="TaskLength5" HorizontalAlignment="Center" VerticalAlignment="Center" ToolTip="Общая длина заданий в очереди.">0</TextBlock>
                </Border>

                <local:ReelTable Grid.Column="0" Grid.Row="1" x:Name="RawGrid1"/>
                <local:ReelTable Grid.Column="2" Grid.Row="1" x:Name="RawGrid2"/>
                <local:ReelTable Grid.Column="4" Grid.Row="1" x:Name="RawGrid3"/>
                <local:ReelTable Grid.Column="6" Grid.Row="1" x:Name="RawGrid4"/>
                <local:ReelTable Grid.Column="8" Grid.Row="1" x:Name="RawGrid5"/>
            </Grid>


            <Border Grid.Row="1" Grid.Column="0"  Grid.RowSpan="9" Grid.ColumnSpan="7" Style="{DynamicResource DataGridMainHolder}" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Image x:Name="FireAlarmImage" Style="{DynamicResource FireAlarmImage}" Width="auto" Height="auto" Visibility="Collapsed"/>
            </Border>
        </Grid>
    </Border>

</UserControl>
