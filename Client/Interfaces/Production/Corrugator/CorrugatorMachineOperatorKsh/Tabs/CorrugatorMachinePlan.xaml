<controls:ControlBase x:Class="Client.Interfaces.Production.Corrugator.CorrugatorMachineOperatorKsh.CorrugatorMachinePlan"
              xmlns:local="clr-namespace:Client.Interfaces.Production"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"

    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"         
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"    
    xmlns:s="clr-namespace:System;assembly=mscorlib"

    mc:Ignorable="d" 
    d:DesignWidth="1200" d:DesignHeight="800"
    Style="{DynamicResource UserControlCanvas}"             
>

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Border x:Name="TaskPlaningBorder" BorderThickness="1" Style="{DynamicResource DataGridContainerHolder}"  >
        <Grid Style="{DynamicResource DataGridContainer}">

            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="GeneralColumnLeft" Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"  Grid.Column="0" Grid.ColumnSpan="3" Style="{DynamicResource GridControlPanel}" Panel.ZIndex="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Name="Toolbar" Background="White" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left" >

                    <Button Name="ShowButton"    Content="Обновить"  Style="{DynamicResource Button}" Click="ShowButton_Click"   />
                    <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>

                    <Label Content="Время:" Style="{DynamicResource TopLabel}" />
                    <TextBox Name="PlanTimeTextJS" Text="" Width="30" Margin="0,0,5,0" PreviewTextInput="PlanTimeText_PreviewTextInput" />
                    <Button Name="SetNewTimeJS" Content="Задать" Style="{DynamicResource Button}" Click="SetNewTime_Click" />
                    <CheckBox Name="ChkAutoplanJS" Content="Автоплан J.S" Style="{DynamicResource CheckBoxTopPanel}" Click="AutoPlan_Checked"/>
                    <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"/>
                </StackPanel>
            </Grid>


            <Border Grid.Row="2"  Grid.Column="0" Style="{DynamicResource DataGridMainHolder}" >
                <GroupBox Header="J.S">
                    <controls:GridBox4 x:Name="GridQueue" />
                </GroupBox>
            </Border>
        </Grid>
    </Border>
</controls:ControlBase>
