<controls:ControlBase x:Class="Client.Interfaces.Production.Cmn.Idles.IdlesTab"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"
                      
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d" 
    d:DesignHeight="450" d:DesignWidth="1024"
    Style="{DynamicResource UserControlCanvas}" >

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Style="{DynamicResource DataGridContainer}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <WrapPanel Grid.Row="0"  Grid.Column="0" Margin="0,5,0,0">
            <!--Получение данных-->
            <StackPanel Name="GridToolbar" Style="{DynamicResource FormHeaderButtonsPanel}" >
                <Label Content="Период:"  Style="{DynamicResource TopLabel}" />
                <controls:SelectBox  x:Name="TimeSpanSelectBox" Style="{DynamicResource CustomFormField}" Margin="0,0,5,0" ListBoxMinHeight="200"  Width="140" Autocomplete="False"  />
                <Button Content="Назад" Name="LeftShiftButton" Style="{DynamicResource Button}" MinWidth="45"/>                
                <Button Content="Вперед" Name="RightShiftButton" Style="{DynamicResource Button}" MinWidth="45"/>
                <Label Content="С:"  Style="{DynamicResource TopLabel}"/>
                <controls:DateTimePicker x:Name="FromDate" Style="{DynamicResource DateTimeEditStyle}"/>
                <Label Content="По:"  Style="{DynamicResource TopLabel}"/>
                <controls:DateTimePicker x:Name="ToDate" Style="{DynamicResource DateTimeEditStyle}"/>
                <Label Content="Станок:"  Style="{DynamicResource TopLabel}"/>
                <controls:SelectBox  x:Name="MachineSelectBox" Style="{DynamicResource CustomFormField}" Margin="0,0,5,0" ListBoxMinHeight="400"  Width="100" Autocomplete="False"  />
                <Button Content="Показать" Name="ShowButton"  Style="{DynamicResource Button}"/>
            </StackPanel>
        </WrapPanel>

        <StackPanel Grid.Row="0" Style="{DynamicResource FormHeaderButtonsPanel}"  HorizontalAlignment="Right" Width="24" >
            <Button Name="HelpButton" Tag="help" Width="24" MinWidth="24"  Content="?"  Style="{DynamicResource Button}" Margin="0"  />
        </StackPanel>

        <WrapPanel Grid.Row="1"  Grid.Column="0" Margin="0,5,0,0">
            <!--стандартные команды-->
            <StackPanel Style="{DynamicResource FormHeaderButtonsPanel}">
                <Button Name="CreateButton" Content="Создать" Style="{DynamicResource Button}"  />
                <Button Name="EditButton" Content="Изменить" Style="{DynamicResource Button}"  />
                <Button Name="DeleteButton"  Content="Удалить" Style="{DynamicResource Button}"/>
            </StackPanel>
            <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
            <!--поиск-->
            <StackPanel Style="{DynamicResource FormHeaderButtonsPanel}">
                <Label Content="Поиск:"  Style="{DynamicResource TopLabel}" />
                <TextBox Style="{DynamicResource FormFieldTopPanel}" Width="60" TextWrapping="NoWrap" Name="SearchText" Text=""  />
            </StackPanel>
            <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
            <!--фильтр-->
            <StackPanel Style="{DynamicResource FormHeaderButtonsPanel}" >
                <Label Content="Тип:"  Style="{DynamicResource TopLabel}"/>
                <controls:SelectBox  x:Name="ReasonSelectBox" Style="{DynamicResource CustomFormField}" Margin="0,0,0,0" ListBoxMinHeight="200"  Width="140" Autocomplete="False"  />
                <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
                <Label Content="Причина:"  Style="{DynamicResource TopLabel}"/>
                <controls:SelectBox  x:Name="ReasonDetailSelectBox" Style="{DynamicResource CustomFormField}" Margin="0,0,0,0" ListBoxMinHeight="200"  Width="140" Autocomplete="False"  />
                <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
            </StackPanel>
            <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
            <!--вспомогательные команды-->
            <StackPanel Style="{DynamicResource FormHeaderButtonsPanel}" >
                <Button Name="PrintButton" Content="Печатать"  Style="{DynamicResource Button}" />
                <Button Name="ExcelButton" Content="В Excel"  Style="{DynamicResource Button}" MinWidth="45" />
            </StackPanel>
        </WrapPanel>

        <Border Grid.Row="2"  Grid.Column="0" Style="{DynamicResource DataGridMainHolder}" Margin="0,5,0,0">
            <controls:GridBox4 x:Name="Grid" Visibility="Visible" />
        </Border>
    </Grid>
</controls:ControlBase>

