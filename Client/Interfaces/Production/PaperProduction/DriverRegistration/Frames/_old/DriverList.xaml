<UserControl 
    x:Class="Client.Interfaces.Production.DriverList"
    xmlns:local="clr-namespace:Client.Interfaces.Production"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"         
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"    
    xmlns:s="clr-namespace:System;assembly=mscorlib"

    mc:Ignorable="d" 
    d:DesignWidth="1024" d:DesignHeight="768" 

   Style="{DynamicResource UserControlCanvas}"    
>

    <UserControl.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{DynamicResource DataGridContainerHolder}" >
        <Grid Style="{DynamicResource DataGridContainer}">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="52"></RowDefinition>
                <RowDefinition Height="2*"></RowDefinition >
                <RowDefinition Height="Auto" MinHeight="100"></RowDefinition>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Grid.Column="0">
                <Grid >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="0.25*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Grid.Column="0">
                        <Label Content="Список машин на БДМ"  Name="LabelLisCar" Style="{DynamicResource TopLabel}" Width="500" Height="40" FontSize="20" FontWeight="SemiBold"/>
                    </Grid>

                    <Grid Grid.Row="0" Grid.Column="1">
                        <Label Content="Время"  Name="LabelTime" Style="{DynamicResource TopLabel}" Height="40" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,0"/>
                    </Grid>

                    <Grid Grid.Row="0" Grid.Column="2">
                        <Button Name="BurgerButton" Style="{DynamicResource MobileButton}" Width="50" Click="BurgerButton_Click"  Margin="10,0,0,0" >
                            <StackPanel Orientation="Horizontal"  HorizontalAlignment="Center" >
                                <Border Width="40">
                                    <Image Style="{DynamicResource RollReelMenuIcon}"  />
                                </Border>
                            </StackPanel>
                            <Button.ContextMenu >
                                <ContextMenu Name="BurgerMenu">
                                    <MenuItem Header="Выход"  Name="BurgerExit"  Click="BurgerExit_Click" FontSize="18" Height="32" Background="#ffffff" FontWeight="SemiBold" />
                                </ContextMenu>
                            </Button.ContextMenu>
                        </Button>

                    </Grid>

                </Grid>
            </Grid>

            <Grid Grid.Row="1" Grid.Column="0">
                <Border Name="ListCarContainer" Style="{DynamicResource DataGridMainHolder}">
                    <controls:GridBox x:Name="ListCarGrid" FontWeight="SemiBold" Margin="0,0,0,10"  />
                </Border>
            </Grid>

            <Grid Grid.Row="2" Grid.Column="0">
                <Button Name="DriverRegistrationButton" Width="800" Style="{DynamicResource MobileButton}"  Click="DriverRegistrationButton_Click" Content="НАЖМИТЕ ДЛЯ РЕГИСТРАЦИИ МАШИНЫ" />
                <Button Name="DriverRegistrationButton2" Width="60" Style="{DynamicResource MobileButton}" Content="x" Click="DriverRegistrationButton2_Click"/>
            </Grid>


        </Grid>
    </Border>
</UserControl>
