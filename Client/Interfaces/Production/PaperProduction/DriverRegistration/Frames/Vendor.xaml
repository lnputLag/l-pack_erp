<local:WizardFrame
    x:Class="Client.Interfaces.Production.Vendor"
    xmlns:local="clr-namespace:Client.Interfaces.Production"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"         
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"    
    xmlns:s="clr-namespace:System;assembly=mscorlib"

    mc:Ignorable="d" 
    d:DesignWidth="1024" d:DesignHeight="768" 
  
    Style="{DynamicResource UserControlCanvasTouch}"   
>
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{DynamicResource TouchFrameBlock}"  >
        <Grid Style="{DynamicResource TouchFrame}" >

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" ></RowDefinition>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto" ></RowDefinition>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"  Grid.Column="0" Style="{DynamicResource TouchFromGridTitleBlock}" Height="50" >                
                <StackPanel Name="GridToolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left" Height="50" Margin="0">
                    <TextBlock  Name="Title" Text="Выберите из списка поставщика макулатуры" TextAlignment="Left" Margin="0,-16,0,0" Style="{DynamicResource TouchFormTitleTextBlock}"  />
                  
                </StackPanel>
            </Border>

            <Border Grid.Row="1"  Grid.Column="0" Style="{DynamicResource TouchFromGridBlock}" BorderBrush="#900000" BorderThickness="0" >

                <Grid Style="{DynamicResource TouchFrame}" VerticalAlignment="Stretch" >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Grid.Column="0" Style="{DynamicResource TouchFromGridBlock}" BorderBrush="#900000" BorderThickness="0" Margin="0,5,0,5" >
                        <controls:GridBox x:Name="Grid" VerticalAlignment="Top" Margin="0,0,0,0" MinHeight="210" />
                    </Border>

                    <Border Grid.Row="1" Grid.Column="0" Style="{DynamicResource TouchFromGridBlock}"  BorderBrush="#009000" BorderThickness="0" Margin="0,5,0,8" >
                        <Grid Style="{DynamicResource TouchFrame}" VerticalAlignment="Stretch" >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"></RowDefinition>
                                <RowDefinition Height="Auto"></RowDefinition>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" Grid.Column="0" Style="{DynamicResource TouchFromGridBlock}" BorderBrush="#900000" BorderThickness="0" Margin="0,5,0,5" >
                                <StackPanel Name="GridToolbar2" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left" Height="50" Margin="0">

                                    <TextBlock  Name="Title2" Text="Поиск (введите буквы без ИП, ООО, АО)" TextAlignment="Left" Margin="0,-16,0,0" Style="{DynamicResource TouchFormTitleTextBlock}"  />
                                    <Border Style="{DynamicResource FormButtonsPanelButtonBigSpacer}"></Border>

                                    <TextBox Style="{DynamicResource FormFieldInputBig}" Width="200" Margin="0,-10,0,0" MaxLength="10" TextWrapping="NoWrap" Name="SearchText" Text=""  />

                                </StackPanel>

                            </Border>

                            <Border Grid.Row="1" Grid.Column="0" Style="{DynamicResource TouchFromGridBlock}" BorderBrush="#900000" BorderThickness="0" Margin="0,5,0,5" >
                                <controls:TouchKeyboardAlpha x:Name="Keyboard" ReceiverName="Vendor" VerticalAlignment="Stretch"  />
                            </Border>

                        </Grid>

                    </Border>

                </Grid>
                
            </Border>

            <Border Grid.Row="2"  Grid.Column="0" Style="{DynamicResource TouchFrameControlPanelBlockBig}" BorderBrush="#900000" BorderThickness="0">
                <Grid Style="{DynamicResource TouchFrameControlPanel}" >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Style="{DynamicResource TouchFrameControlPanelLeft}" >
                        <Button Name="HomeButton" Click="HomeButtonClick" Content="В начало"  Style="{DynamicResource TouchFormButtonBig}" Margin="20,0,0,0" />
                        <Border Style="{DynamicResource FormButtonsPanelButtonBigSpacer}"></Border>
                        <Button Grid.Column="0" Name="PriorButton" Click="PriorButtonClick"  Content="Назад" Style="{DynamicResource TouchFormButtonBig}"  />
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="1" Style="{DynamicResource TouchFrameControlPanelRight}" >
                        <Button Grid.Column="2" Name="NextButton"   Click="NextButtonClick" Content="Далее"  Style="{DynamicResource TouchFormButtonPrimaryBig}"  />
                    </StackPanel>
                    
                </Grid>
            </Border>
        </Grid>
    </Border>

</local:WizardFrame>

    