<controls:ControlBase
    x:Class="Client.Interfaces.Production.PaperMakingMachineOperator"
    xmlns:local="clr-namespace:Client.Interfaces.Production"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"

    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"         
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"    
    xmlns:s="clr-namespace:System;assembly=mscorlib"

    mc:Ignorable="d" 
    d:DesignWidth="1900" d:DesignHeight="1080"
    Style="{DynamicResource UserControlCanvas}"             
>

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>


    <Border Style="{DynamicResource DataGridContainerHolder}"  >
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.5*" Name="Col0"/>
                <ColumnDefinition Width="5" Name="Col1"/>
                <ColumnDefinition Width="0.4*" Name="Col2"/>
                <ColumnDefinition Width="1" Name="Col3"/>
                <ColumnDefinition Width="0.45*" Name="ColIdles"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <!--Строка состояния-->
                <RowDefinition Height="30"></RowDefinition>
                <!--График скорости-->
                <RowDefinition Height="150"></RowDefinition>
                <!--Кнопки-->
                <RowDefinition Height="auto"></RowDefinition>
                <!--Список ПЗ-->
                <RowDefinition Name="PanelPz" Height="200"></RowDefinition>
                <!--Произведено и простои-->
                <RowDefinition Name="PanelBottom" Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0"  Grid.Column="0" Grid.ColumnSpan="3" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="left" >

                <!--<Grid Style="{DynamicResource FormFieldDateHolder}">
                    <controls:DatePicker x:Name="Today" Style="{DynamicResource DateEditStyle}" TextChanged="Today_TextChanged">
                    </controls:DatePicker>
                </Grid>-->
               
                 <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>

                <TextBlock Name="FireStatus" Text="Сообщение о пожаре на БДМ2" Foreground="Red" VerticalAlignment="Bottom" FontSize="20" FontWeight="SemiBold" />
                <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>

            </StackPanel>

            <StackPanel Grid.Row="0"  Grid.Column="2" Grid.ColumnSpan="3"  Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Right" >
                <TextBlock Name="InfoDuty" Text="Дежурный СА:"  VerticalAlignment="Bottom" FontSize="20" FontWeight="Bold" />
                <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>
                <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>
                <TextBlock Name="CurrentMacine" Text="БДМ2"  VerticalAlignment="Bottom" FontSize="20" FontWeight="Bold" />
                <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>
                <TextBlock Name="CurrentTime" Text="08:00"  VerticalAlignment="Bottom" FontSize="20" FontWeight="Bold" />
                <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>
                <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>
                
                <Button Name="FireAlarmButton" MinWidth="60" Content="Пожар" Margin="0" Style="{DynamicResource ButtonFire}" Click="FireAlarmButton_Click" />
                <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>
                <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>

                <Button Name="BurgerMenuButton" ToolTip="Настройка" Style="{DynamicResource ButtonGlyph}" Click="BurgerMenuButton_Click" Margin="0,0,5,0" >
                    <StackPanel Orientation="Horizontal"  HorizontalAlignment="Center" >
                        <Border >
                            <Image Style="{DynamicResource SettingsImage}"  />
                        </Border>
                    </StackPanel>
                    <Button.ContextMenu >
                        <ContextMenu Name="BurgerMenu">
                            <MenuItem Header="Информация" Name="InfoMenu"    Click="InfoMenu_Click"    FontSize="18" Height="32" Background="#ffffff" FontWeight="SemiBold" />
                            <Separator />
                            <MenuItem Header="Обновить"   Name="RestartMenu" Click="RestartMenu_Click" FontSize="18" Height="32" Background="#ffffff" FontWeight="SemiBold" />
                            <MenuItem Header="Выход"      Name="ExitMenu"    Click="ExitMenu_Click"    FontSize="18" Height="32" Background="#ffffff" FontWeight="SemiBold" />
                        </ContextMenu>
                    </Button.ContextMenu>
                </Button>


                
                <Button Name="HelpButton" MinWidth="24" Content="?" Margin="0" Style="{DynamicResource Button}" Click="HelpButton_Click"  />
            </StackPanel>


            <Grid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="5" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Border Grid.Column="0" Style="{DynamicResource DataGridMainHolder}" >
                    <local:PaperMakingSpeedChart x:Name="SpeedChart"/>
                </Border>

                <Border Grid.Column="1" Style="{DynamicResource DataGridMainHolder}" BorderThickness="1" BorderBrush="Silver">
              
                </Border>

                <Border Grid.Column="2" Style="{DynamicResource DataGridMainHolder}">
                    <!--<local:PaperMakingIdleList x:Name="IdleGrid" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />-->
                </Border>
            </Grid>

            <Grid Grid.Row="2"  Grid.Column="0" Grid.ColumnSpan="5" Style="{DynamicResource GridControlPanel}">
                <Border >
                    <StackPanel Name="ToolbarDiagram" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left"  >
                        <Button IsEnabled="True" Name="RefreshButton"  Content="Обновить"  Style="{DynamicResource Button}" Click="RefreshButton_Click" />
                        <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>
                        <Button Name="ButtonCloseTask" Tag="access_mode_full_access" Content="Закрыть задание" Style="{DynamicResource Button}" Visibility="Visible" Click="ButtonCloseTask_Click" />
                        <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>
                        <Button Name="ButtonChangeTask" Content="Раскрыть список заданий" Style="{DynamicResource Button}" Click="ButtonChangeTask_Click" />
                        <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>
                        <Button Name="ButtonUpTask" Content="Вверх"  ToolTip="Перенести на строку выше" Style="{DynamicResource Button}" Visibility="Visible" Click="ButtonUpTask_Click" />
                        <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>
                        <Button Name="ButtonDownTask" Content="Вниз"  ToolTip="Перенести на строку ниже" Style="{DynamicResource Button}" Visibility="Visible" Click="ButtonDownTask_Click" />
                        <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>
                        <Button Name="ExcelButton" Content="B Excel" Click="ExcelButton_Click" Style="{DynamicResource Button}"  Visibility="Visible"  />
                    </StackPanel>
                </Border>

                <Border  >
                    <StackPanel Name="ToolbarInfo" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Right"  Orientation="Horizontal" >
                        <TextBlock  Text="Вес рулонов всего:" FontSize="20" MinWidth="80" HorizontalAlignment="Left" VerticalAlignment="Center" ></TextBlock>
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                        <TextBlock  Name="Prihod" Text="0" FontSize="22" MinWidth="80" HorizontalAlignment="Left" VerticalAlignment="Center" ></TextBlock>
                       
                        <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>
                        <TextBlock Text="Остаток сырья на складе" FontSize="20" MinWidth="80" HorizontalAlignment="Left" VerticalAlignment="Center" ></TextBlock>
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                        <TextBlock  Name="Ostatok" Text="0" FontSize="22" MinWidth="80" HorizontalAlignment="Left" VerticalAlignment="Center" ></TextBlock>
                    </StackPanel>
                </Border>
            </Grid>
            
            <!--Список ПЗ-->
            <Border Grid.Row="3"  Grid.Column="0" Grid.ColumnSpan="5" Style="{DynamicResource DataGridMainHolder}" >
                <local:PaperMakingMachineTaskQueue x:Name="TaskQueue"/>
            </Border>

            <!--Произведено-->
            <Grid Grid.Row="4"  Grid.Column="0" Grid.ColumnSpan="3" Margin="0, 5, 0, 0">
                <Border Style="{DynamicResource DataGridMainHolder}" >
                    <local:PaperMakingProduction x:Name="ProductionData" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
                </Border>
            </Grid>

            <!--Информация по простоям смены-->
            <Grid x:Name="Idles" Grid.Row="4"  Grid.Column="4" Margin="0, 5, 0, 0">
                <Border Style="{DynamicResource DataGridMainHolder}" BorderThickness="1" BorderBrush="Silver">
                    <local:PaperMakingIdleList x:Name="IdlesData" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
                </Border>
            </Grid>

            <!--Задания по времени и списки рулонов-->
            <Grid Grid.Row="4"  Grid.Column="2" Margin="0, 5, 0, 0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>

            </Grid>

            <Border Grid.Row="1" Grid.Column="0"  Grid.RowSpan="9" Grid.ColumnSpan="7" Style="{DynamicResource DataGridMainHolder}" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Image x:Name="FireAlarmImage" Style="{DynamicResource FireAlarmImage}" Width="auto" Height="auto" Visibility="Collapsed"/>
            </Border>
        </Grid>
    </Border>

</controls:ControlBase>
