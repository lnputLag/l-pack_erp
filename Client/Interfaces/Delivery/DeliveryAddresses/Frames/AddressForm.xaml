<controls:ControlBase
    xmlns:local="clr-namespace:Client.Interfaces.DeliveryAddresses"
    xmlns:lib="clr-namespace:Client.Common"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    
    xmlns:Wpf="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf" 
    x:Class="Client.Interfaces.DeliveryAddresses.AddressForm"
    
    mc:Ignorable="d"
    
    Width="800" Height="460"
    Style="{DynamicResource UserControlCanvas}"
    >

    <controls:ControlBase.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </controls:ControlBase.Resources>

    <Border Style="{DynamicResource FormContainerHolder}" >

        <Grid Style="{DynamicResource FormContainer}">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Style="{DynamicResource FormControlPanel}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Column="0" x:Name="FormToolbar" Style="{DynamicResource FormHeaderButtonsPanel}">
                    <Button x:Name="SaveButton" Content="Сохранить" Style="{DynamicResource FButtonPrimary}" />
                    <Button x:Name="CancelButton" Content="Отмена" Style="{DynamicResource Button}"/>
                    <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                    <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                    <TextBlock  Name="StatusBar" Text="Не все обязательные поля заполнены верно"  TextAlignment="Left" Style="{DynamicResource FStatusBar2}" HorizontalAlignment="Stretch" />
                </StackPanel>
            </Grid>

            <Border Grid.Row="1" Grid.Column="0" Style="{DynamicResource FormVerticalPanelSpacer}"/>

            <Border Grid.Row="2" Style="{DynamicResource DataGridMainHolder}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="5*"/>
                        <ColumnDefinition Width="5*"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="3*"/>
                            <ColumnDefinition Width="7*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <Border Grid.Row="0" Grid.Column="0" Style="{DynamicResource FormLabelContainer}">
                            <Label Content="Полный адрес: *"    Style="{DynamicResource FormLabel}"  />
                        </Border>
                        <Border Grid.Row="0"  Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                            <TextBox  Name="FullAddressTextBox"  MaxLength="512" TextWrapping="WrapWithOverflow" Style="{DynamicResource FormField}" HorizontalAlignment="Stretch" Height="45" />
                        </Border>

                        <Border Grid.Row="1" Grid.Column="0" Style="{DynamicResource FormLabelContainer}">
                            <Label Content="ФИАС:  "    Style="{DynamicResource FormLabel}"  />
                        </Border>
                        <Border Grid.Row="1"  Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                            <TextBox  Name="CodeTextBox"  Style="{DynamicResource FormField}" Width="200" HorizontalAlignment="Left"/>
                        </Border>

                        <Border Grid.Row="2" Grid.Column="0" Style="{DynamicResource FormLabelContainer}">
                            <Label Content="ОКАТО:  "    Style="{DynamicResource FormLabel}"  />
                        </Border>
                        <Border Grid.Row="2"  Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                            <TextBox  Name="OkatoTextBox"  Style="{DynamicResource FormField}" Width="200" HorizontalAlignment="Left"/>
                        </Border>

                        <Border Grid.Row="3" Grid.Column="0" Style="{DynamicResource FormLabelContainer}">
                            <Label Content="Индекс:  "    Style="{DynamicResource FormLabel}"  />
                        </Border>
                        <Border Grid.Row="3"  Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                            <TextBox  Name="ZipNumTextBox"  MaxLength="6" TextWrapping="WrapWithOverflow" Style="{DynamicResource FormField}" Width="200" HorizontalAlignment="Left"/>
                        </Border>

                        <Border Grid.Row="4" Grid.Column="0" Style="{DynamicResource FormLabelContainer}">
                            <Label Content="Страна:  "    Style="{DynamicResource FormLabel}"  />
                        </Border>
                        <Border Grid.Row="4"  Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                            <ComboBox Name="CountryComboBox" Style="{DynamicResource CustomFormField}" Width="200" HorizontalAlignment="Left" />
                        </Border>

                        <Border Grid.Row="5" Grid.Column="0" Style="{DynamicResource FormLabelContainer}">
                            <Label Content="Область:  "    Style="{DynamicResource FormLabel}"  />
                        </Border>
                        <Border Grid.Row="5"  Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                            <ComboBox Name="RegionComboBox" Style="{DynamicResource CustomFormField}" Width="200" HorizontalAlignment="Left" />
                        </Border>

                        <Border Grid.Row="6" Grid.Column="0" Style="{DynamicResource FormLabelContainer}">
                            <Label Content="Район:  "    Style="{DynamicResource FormLabel}"  />
                        </Border>
                        <Border Grid.Row="6"  Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                            <ComboBox Name="DistrictComboBox" Style="{DynamicResource CustomFormField}" Width="200" HorizontalAlignment="Left" />
                        </Border>

                        <Border Grid.Row="7" Grid.Column="0" Style="{DynamicResource FormLabelContainer}">
                            <Label Content="Город/н.п.:  "    Style="{DynamicResource FormLabel}"  />
                        </Border>
                        <Border Grid.Row="7"  Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                            <ComboBox Name="CityComboBox" Style="{DynamicResource CustomFormField}" Width="200" HorizontalAlignment="Left" />
                        </Border>

                        <Border Grid.Row="8" Grid.Column="0" Style="{DynamicResource FormLabelContainer}">
                            <Label Content="Улица:  "    Style="{DynamicResource FormLabel}"  />
                        </Border>
                        <Border Grid.Row="8"  Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                            <ComboBox Name="StreetComboBox" Style="{DynamicResource CustomFormField}" Width="200" HorizontalAlignment="Left" />
                        </Border>

                        <Border Grid.Row="9" Grid.Column="0" Style="{DynamicResource FormLabelContainer}">
                            <Label Content="Дом:  "    Style="{DynamicResource FormLabel}"  />
                        </Border>
                        <Border Grid.Row="9"  Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                            <ComboBox Name="BuildingComboBox" Style="{DynamicResource CustomFormField}" Width="200" HorizontalAlignment="Left" />
                        </Border>

                        <Border Grid.Row="10" Grid.Column="0" Style="{DynamicResource FormLabelContainer}">
                            <Label Content="Квартира:  "    Style="{DynamicResource FormLabel}"  />
                        </Border>
                        <Border Grid.Row="10"  Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                            <TextBox  Name="RoomTextBox"  MaxLength="9" Style="{DynamicResource FormField}" Width="200" HorizontalAlignment="Left"/>
                        </Border>

                        <Border Grid.Row="11" Grid.Column="0" Style="{DynamicResource FormLabelContainer}">
                            <Label Content="Широта: *"    Style="{DynamicResource FormLabel}"  />
                        </Border>
                        <StackPanel Grid.Row="11"  Grid.Column="1" Orientation="Horizontal">
                            <Border Style="{DynamicResource FormFieldContainer}">
                                <TextBox  x:Name="LatitudeTextBox"  MaxLength="9" Style="{DynamicResource FormField}" Width="200" HorizontalAlignment="Left"/>
                            </Border>
                            <Border Style="{DynamicResource FormFieldContainer}" Margin="0,0,0,0">
                                <Label Content="°" Style="{DynamicResource FormLabelNote}" HorizontalAlignment="Left" />
                            </Border>
                        </StackPanel>

                        <Border Grid.Row="12" Grid.Column="0" Style="{DynamicResource FormLabelContainer}">
                            <Label Content="Долгота: *" Style="{DynamicResource FormLabel}"  />
                        </Border>
                        <StackPanel Grid.Row="12"  Grid.Column="1" Orientation="Horizontal">
                            <Border Style="{DynamicResource FormFieldContainer}">
                                <TextBox  x:Name="LongitudeTextBox" MaxLength="9" Style="{DynamicResource FormField}" Width="200" HorizontalAlignment="Left"/>
                            </Border>
                            <Border Style="{DynamicResource FormFieldContainer}" Margin="0,0,0,0">
                                <Label Content="°" Style="{DynamicResource FormLabelNote}" HorizontalAlignment="Left" />
                            </Border>
                        </StackPanel>

                        <Border Grid.Row="13" Grid.Column="0" Style="{DynamicResource FormLabelContainer}">
                            <Label Content="Расстояние:  " Style="{DynamicResource FormLabel}"  />
                        </Border>
                        <StackPanel Grid.Row="13"  Grid.Column="1" Orientation="Horizontal">
                            <Border Style="{DynamicResource FormFieldContainer}">
                                <TextBox  x:Name="DistanceTextBox"  MaxLength="9" Style="{DynamicResource FormField}" Width="200" HorizontalAlignment="Left"/>
                            </Border>
                            <Border Style="{DynamicResource FormFieldContainer}" Margin="0,0,0,0">
                                <Label Content="км" Style="{DynamicResource FormLabelNote}" HorizontalAlignment="Left" />
                            </Border>
                        </StackPanel>

                        <!--<Border Grid.Row="15" Grid.Column="0" Style="{DynamicResource FormLabelContainer}">
                            <Label Content="Синхронизировать поля:  "    Style="{DynamicResource FormLabel}"  />
                        </Border>
                        <Border Grid.Row="15"  Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                            <CheckBox  Name="SyncCheckBox"  Style="{DynamicResource FormField}" Width="200" HorizontalAlignment="Left"/>
                        </Border>-->

                    </Grid>

                    <Wpf:WebView2 Grid.Column="1" Name="WebView" Source="about:blank" Margin="5,5,5,5"/>
                    
                </Grid>

            </Border>

        </Grid>

    </Border>


</controls:ControlBase>
