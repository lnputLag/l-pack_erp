<controls:ControlBase x:Class="Client.Interfaces.Preproduction.RigCalculationTask"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Client.Interfaces.Preproduction"
             xmlns:controls="clr-namespace:Client.Interfaces.Main"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="650"
             Style="{DynamicResource UserControlCanvas}"
                      Background="White">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{DynamicResource DataGridContainerHolder}" HorizontalAlignment="Left" VerticalAlignment="Stretch" Width="650">
        <Grid Style="{DynamicResource FromGrid}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>


            <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left"  >

                <Button Name="SaveButton"   Tag="Save" Content="Сохранить"  Style="{DynamicResource FButtonPrimary}"    Click="ButtonOnClick"  />
                <Button Name="CancelButton" Tag="Close" Content="Отмена"     Style="{DynamicResource FButtonCancel}" Click="ButtonOnClick"   />
                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                <Button Name="SimilarButton" Tag="Similar" Content="Похожие"     Style="{DynamicResource FButtonCancel}" Click="ButtonOnClick"   />
                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                <TextBlock  Name="FormStatus"  TextAlignment="Left" Style="{DynamicResource FStatusBar}" HorizontalAlignment="Stretch" />
            </StackPanel>

            <Border Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3" Style="{DynamicResource FormVerticalPanelSpacer}"></Border>
            <Border Grid.Row="2" Grid.Column="0" Style="{DynamicResource DataGridContainerHolder}">
                <Grid  Name="TaskBlock">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="180"/>
                        <ColumnDefinition Width="280"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Grid.Column="0" Style="{DynamicResource FormLabelContainer}">
                        <Label Name="CustomerLabel" Content="Покупатель: *" Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Row="0" Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                        <controls:SelectBox  x:Name="CustomerName" Style="{DynamicResource CustomFormField}" Autocomplete="True"/>
                    </Border>

                    <Border Grid.Row="1" Grid.Column="0"  Style="{DynamicResource FormLabelContainer}" >
                        <Label Content="Вид изделия: *" Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Row="1" Grid.Column="1"  Style="{DynamicResource FormFieldContainer}" >
                        <controls:SelectBox  x:Name="ProductClass"  Style="{DynamicResource CustomFormField}" Autocomplete="True" ListResizeable="False" />
                    </Border>

                    <Border Grid.Row="2" Grid.Column="0" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="Профиль: *" Style="{DynamicResource FormLabel}"  />
                    </Border>
                    <Border Grid.Row="2" Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                        <controls:SelectBox  x:Name="Profile"  Style="{DynamicResource CustomFormField}" Autocomplete="True" ListResizeable="False"  />
                    </Border>

                    <Border Grid.Row="3" Grid.Column="0"  Style="{DynamicResource FormLabelContainer}" >
                        <Label Content="Длина, мм: *" Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="90"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="90"/>
                                <ColumnDefinition Width="103"/>
                                <ColumnDefinition Width="90"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Row="0" Grid.Column="0" Style="{DynamicResource FormFieldContainer}">
                                <TextBox Name="SLength" Style="{DynamicResource FormField}"/>
                            </Border>

                            <Border Grid.Row="0" Grid.Column="1" Style="{DynamicResource FormLabelContainer}">
                                <Label Content="Ширина, мм: *" Style="{DynamicResource FormLabel}" />
                            </Border>
                            <Border Grid.Row="0" Grid.Column="2" Style="{DynamicResource FormFieldContainer}">
                                <TextBox Name="SWidth" Style="{DynamicResource FormField}"  />
                            </Border>
                            <Border Grid.Row="0" Grid.Column="3" Style="{DynamicResource FormLabelContainer}">
                                <Label Content="Высота, мм: *" Style="{DynamicResource FormLabel}" />
                            </Border>
                            <Border Grid.Row="0" Grid.Column="4" Style="{DynamicResource FormFieldContainer}">
                                <TextBox Name="SHeight" Style="{DynamicResource FormField}" Margin="0,0,3,3" />
                            </Border>
                        </Grid>
                    </Border>

                    <Border Grid.Row="4" Grid.Column="0"  Style="{DynamicResource FormLabelContainer}" >
                        <Label Content="FEFCO:  " Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Row="4" Grid.Column="1"  Style="{DynamicResource FormFieldContainer}" >
                        <controls:SelectBox  x:Name="Fefco"  Style="{DynamicResource CustomFormField}" Autocomplete="True" ListResizeable="False" />
                    </Border>

                    <Border Grid.Row="5" Grid.Column="0"  Style="{DynamicResource FormLabelContainer}" >
                        <Label Content="Размер партии:  " Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Row="5" Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                        <controls:SelectBox  x:Name="OrderQty"  Style="{DynamicResource CustomFormField}" Autocomplete="True" ListResizeable="False" />
                    </Border>

                    <Border Grid.Row="6" Grid.Column="0" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="Примечание:  " Style="{DynamicResource FormLabel}"  />
                    </Border>

                    <Border Grid.Row="6" Grid.Column="1" Grid.ColumnSpan="2">
                        <TextBox Name="Note" Style="{DynamicResource FormField}" MaxLength="511" Height="64" Margin="0,0,0,3" TextWrapping="Wrap" />
                    </Border>

                </Grid>
            </Border>

            <Border Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" Style="{DynamicResource FormVerticalPanelSpacer}"></Border>

            <Border Grid.Row="4" Grid.Column="0" Name="CalcBlock" Style="{DynamicResource DataGridContainerHolder}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="180"/>
                        <ColumnDefinition Width="130"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Grid.Column="0"  Style="{DynamicResource FormLabelContainer}" >
                        <Label Content="Рассчитать развертку:  " Style="{DynamicResource FormLabel}" />
                    </Border>

                    <Border Grid.Row="0" Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                        <TextBox x:Name="BlankStatus" Style="{DynamicResource CustomFormField}" IsReadOnly="True"/>
                    </Border>
                    <Border Grid.Row="0" Grid.Column="2" Style="{DynamicResource FormFieldContainer}">
                        <TextBox x:Name="BlankStatusId" Style="{DynamicResource CustomFormField}" Visibility="Collapsed"/>
                    </Border>

                    <Border Grid.Row="1" Grid.Column="0"  Style="{DynamicResource FormLabelContainer}" >
                        <Label Content="Габариты развертки:  " Style="{DynamicResource FormLabel}" />
                    </Border>

                    <Border Grid.Row="1" Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                        <TextBox Name="BlankLength" Style="{DynamicResource FormField}" TextChanged="BlankTextFieldChanged" />
                    </Border>

                    <Border Grid.Row="1" Grid.Column="2" Style="{DynamicResource FormFieldContainer}">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Label Content=" x  " />
                            <TextBox Name="BlankWidth" Style="{DynamicResource FormField}" Width="126"  TextChanged="BlankTextFieldChanged"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="2" Grid.Column="0"  Style="{DynamicResource FormLabelContainer}" >
                        <Label Content="Количество на штампе:  " Style="{DynamicResource FormLabel}" />
                    </Border>

                    <Border Grid.Row="2" Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                        <TextBox Name="QtyOnStamp" Style="{DynamicResource FormField}" TextChanged="BlankTextFieldChanged" />
                    </Border>

                    <Border Grid.Row="3" Grid.Column="0"  Style="{DynamicResource FormLabelContainer}" >
                        <Label Content="Рассчитать штамп:  " Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Row="3" Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                        <TextBox x:Name="StampStatus" Style="{DynamicResource CustomFormField}" IsReadOnly="True" />
                    </Border>
                    <Border Grid.Row="3" Grid.Column="2" Style="{DynamicResource FormFieldContainer}">
                        <TextBox x:Name="StampStatusId" Style="{DynamicResource CustomFormField}" Visibility="Collapsed"/>
                    </Border>

                    <Border Grid.Row="4" Grid.Column="0"  Style="{DynamicResource FormLabelContainer}" >
                        <Label Content="Стоимость штампа:  " Style="{DynamicResource FormLabel}" />
                    </Border>

                    <Border Grid.Row="4" Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                        <TextBox Name="StampPrice" Style="{DynamicResource FormField}" TextChanged="StampTextChanged"/>
                    </Border>

                    <Border Grid.Row="4" Grid.Column="2" Style="{DynamicResource FormFieldContainer}" Margin="20,0,0,0">
                        <controls:SelectBox x:Name="StampCurrency" Style="{DynamicResource CustomFormField}" SelectedItemChanged="StampCurrency_SelectedItemChanged"/>
                    </Border>

                    <Border Grid.Row="5" Grid.Column="0"  Style="{DynamicResource FormLabelContainer}" >
                        <Label Content="Файл чертежа:  " Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="3" Style="{DynamicResource FormFieldContainer}">
                        <StackPanel Orientation="Horizontal">
                            <TextBox Name="DrawingFileName" Style="{DynamicResource FormField}" IsReadOnly="True" Width="400" />
                            <TextBox Name="DrawingFile"  Style="{DynamicResource FormField}"  Visibility="Collapsed" />
                            <Button Name="DrawingFileSelectButton" Tag="DrawingSelect" ToolTip="Выбрать" Style="{DynamicResource ButtonGlyph}"  Margin="-1,0,0,0" Click="ButtonOnClick" >
                                <Image Style="{DynamicResource SelectImage}" />
                            </Button>
                            <Button Name="DrawingFileShowButton"  Tag="DrawingShow"  ToolTip="Посмотреть" Style="{DynamicResource ButtonGlyph}"  Margin="-1,0,0,0" IsEnabled="False" Click="ButtonOnClick">
                                <Image Style="{DynamicResource ViewImage}" />
                            </Button>
                            <Button Name="DrawingFileClearButton"  Tag="DrawingClear"  ToolTip="Очистить" Style="{DynamicResource ButtonGlyph}"  Margin="-1,0,0,0" IsEnabled="False" Click="ButtonOnClick">
                                <Image Style="{DynamicResource CloseImage}" />
                            </Button>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="6" Grid.Column="0"  Style="{DynamicResource FormLabelContainer}" >
                        <Label Content="Примечание конструктора:  " Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Row="6" Grid.Column="1" Grid.ColumnSpan="3" Style="{DynamicResource FormFieldContainer}">
                        <TextBox Name="ConstructorNote" Style="{DynamicResource FormField}" />
                    </Border>

                    <Border Grid.Row="7" Grid.Column="0"  Style="{DynamicResource FormLabelContainer}" >
                        <Label Content="Рассчитать клише:  " Style="{DynamicResource FormLabel}" />
                    </Border>

                    <Border Grid.Row="7" Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                        <TextBox x:Name="ClicheStatus" Style="{DynamicResource CustomFormField}" IsReadOnly="True" />
                    </Border>
                    <Border Grid.Row="7" Grid.Column="2" Style="{DynamicResource FormFieldContainer}">
                        <TextBox x:Name="ClicheStatusId" Style="{DynamicResource CustomFormField}" Visibility="Collapsed"/>
                    </Border>

                    <Border Grid.Row="8" Grid.Column="0"  Style="{DynamicResource FormLabelContainer}" >
                        <Label Content="Стоимость клише:  " Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Row="8" Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                        <TextBox Name="ClichePrice" Style="{DynamicResource FormField}" TextChanged="ClicheTextChanged" />
                    </Border>

                    <Border Grid.Row="8" Grid.Column="2" Style="{DynamicResource FormFieldContainer}" Margin="20,0,0,0">
                        <controls:SelectBox x:Name="ClicheCurrency" Style="{DynamicResource CustomFormField}" SelectedItemChanged="ClicheCurrency_SelectedItemChanged"/>
                    </Border>
                    <Border Grid.Row="9" Grid.Column="0"  Style="{DynamicResource FormLabelContainer}" >
                        <Label Content="Примечание дизайнера:  " Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Row="9" Grid.Column="1" Grid.ColumnSpan="3" Style="{DynamicResource FormFieldContainer}">
                        <TextBox Name="DesignerNote" Style="{DynamicResource FormField}" />
                    </Border>

                </Grid>
            </Border>

            <Border Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="3" Style="{DynamicResource FormVerticalPanelSpacer}"></Border>

            <Border Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="3" Style="{DynamicResource DataGridMainHolder}" Margin="0,0,0,10">
                <Grid Style="{DynamicResource DataGridContainer}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Style="{DynamicResource FormHeaderButtonsPanel}" Margin="0,5" >
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                        <Label Content="Файлы" Style="{DynamicResource TopLabel}"/>
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                        <Button Name="OpenFileButton"   Tag="OpenFile"   Content="Открыть"    Style="{DynamicResource Button}" Click="ButtonOnClick"  />
                        <Button Name="SaveFileButton"   Tag="SaveFile"   Content="Сохранить"  Style="{DynamicResource Button}" Click="ButtonOnClick"  />
                        <Button Name="AddFileButton"    Tag="AddFile"    Content="Добавить"   Style="{DynamicResource Button}" Click="ButtonOnClick"  />
                        <Button Name="DeleteFileButton" Tag="DeleteFile" Content="Удалить"    Style="{DynamicResource Button}" Click="ButtonOnClick"  />
                    </StackPanel>

                    <Border Grid.Row="1" Grid.Column="0" Style="{DynamicResource DataGridContainerHolder}" >
                        <controls:GridBox4 x:Name="AttachmentGrid" />
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Border>
</controls:ControlBase>
