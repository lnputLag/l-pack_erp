<controls:ControlBase x:Class="Client.Interfaces.Preproduction.SampleLaboratoryList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Client.Interfaces.Preproduction"
             xmlns:controls="clr-namespace:Client.Interfaces.Main"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="1280"
             Style="{DynamicResource UserControlCanvas}">

    <UserControl.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Style="{DynamicResource DataGridContainer}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0"  Grid.Column="0" Style="{DynamicResource GridControlPanel}">
            <Border>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="11.5*"/>
                        <ColumnDefinition Width="0.5*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Name="GridToolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left"  >
                        <Label Content="С:"  Style="{DynamicResource TopLabel}"/>
                        <Grid Style="{DynamicResource FormFieldDateHolder}">

                            <controls:DatePicker x:Name="FromDate" Style="{DynamicResource DateEditStyle}" TextChanged="Date_TextChanged">
                            </controls:DatePicker>
                        </Grid>

                        <Label Content="По:"  Style="{DynamicResource TopLabel}"/>
                        <Grid Style="{DynamicResource FormFieldDateHolder}">
                            <controls:DatePicker x:Name="ToDate" Style="{DynamicResource DateEditStyle}" TextChanged="Date_TextChanged">
                            </controls:DatePicker>
                        </Grid>
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                        <Button Name="RefreshButton" Tag="refresh" Content="Показать"  Style="{DynamicResource Button}" Click="ButtonOnClick"  />
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                        <Button Name="AddButton"     Tag="add;access_mode_full_access"     Content="Добавить"  Style="{DynamicResource Button}" Click="ButtonOnClick"   />
                        <Button Name="EditButton"    Tag="edit;access_mode_full_access"    Content="Изменить"  Style="{DynamicResource Button}" Click="ButtonOnClick"   />
                        <Button Name="DeleteButton"  Tag="delete;access_mode_full_access"  Content="Удалить"   Style="{DynamicResource Button}" Click="ButtonOnClick"   />
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                        <Button Name="AttachTKButton"  Tag="techmap;access_mode_full_access"  Content="Прикрепить ТК"   Style="{DynamicResource Button}" Click="ButtonOnClick"   />
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                        <Label Content="Поиск:"  Style="{DynamicResource TopLabel}" />
                        <TextBox Style="{DynamicResource FormFieldTopPanel}" Width="60" TextWrapping="NoWrap" Name="SearchText" Text=""  />
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                        <Label Content="Статус:"  Style="{DynamicResource TopLabel}" />
                        <controls:SelectBox x:Name="SampleStatus" Style="{DynamicResource CustomFormField}" Width="140" ListBoxMinHeight="220" Autocomplete="False" SelectedItemChanged="SampleStatus_SelectedItemChanged" />

                        <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>
                        <CheckBox Name="AllEmployeeCheckBox" Content="Все"  Style="{DynamicResource CheckBoxTopPanel}" Click="AllEmployeeCheckBox_Click" />
                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="1" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Right" >
                        <Button Name="HelpButton" Tag="Help" Width="24" MinWidth="24"  Content="?"  Style="{DynamicResource Button}" Margin="0" Click="ButtonOnClick" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Row="1"  Grid.Column="0">
            <Border Style="{DynamicResource DataGridMainHolder}" >
                <controls:GridBox3 x:Name="Grid" />
            </Border>
        </Grid>
    </Grid>
</controls:ControlBase>
