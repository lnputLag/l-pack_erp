<controls:ControlBase 
    x:Class="Client.Interfaces.Preproduction.TechnologicalMapSet"
    xmlns:local="clr-namespace:Client.Interfaces.Preproduction"
    xmlns:lib="clr-namespace:Client.Common"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"             
    
    mc:Ignorable="d"
    
    d:DesignHeight="800" d:DesignWidth="1200"
    Style="{DynamicResource UserControlCanvas}"    
    >

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Style="{DynamicResource DataGridContainer}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Style="{DynamicResource FormControlPanel}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>
            <StackPanel Grid.Column="0" Name="FormToolbar" Style="{DynamicResource FormHeaderButtonsPanel}">
                <Button Name="SaveButton" Content="Сохранить"  Tag="Save" Style="{DynamicResource FButtonPrimary}"/>
                <Button Name="CancelButton" Content="Отмена" Tag="Close" Style="{DynamicResource Button}"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Style="{DynamicResource FormHeaderButtonsPanel}"  HorizontalAlignment="Right" >
                <Button Name="HelpButton" ToolTip="Документация" Width="24" MinWidth="24"  Content="?"  Style="{DynamicResource Button}" Click="HelpButtonClick"  Margin="0,0,0,0" />
            </StackPanel>
        </Grid>
        <Border Grid.Row="1" Grid.Column="0" Style="{DynamicResource FormVerticalPanelSpacer}"></Border>
        <Grid Grid.Row="2" Style="{DynamicResource DataGridContainer}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="4*"/>
                <ColumnDefinition Width="0.8*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="5*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" MinHeight="30"></RowDefinition>
                <RowDefinition Height="Auto" MinHeight="30"></RowDefinition>
                <RowDefinition Height="Auto" MinHeight="30"></RowDefinition>
                <RowDefinition Height="Auto" MinHeight="30"></RowDefinition>
                <RowDefinition Height="Auto" MinHeight="30"></RowDefinition>
                <RowDefinition Height="Auto" MinHeight="30"></RowDefinition>
            </Grid.RowDefinitions>

            <Border Grid.Column="0" Grid.Row="0" >
                <Label Content="Главное изделие:  "  Style="{DynamicResource FormLabel}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Border>
            <Border Grid.Column="0" Grid.Row="1">
                <Label Content="Решетка 1:  "  Style="{DynamicResource FormLabel}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Border>
            <Border Grid.Column="0" Grid.Row="2">
                <Label Content="Решетка 2:  "  Style="{DynamicResource FormLabel}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Border>
            <Border Grid.Column="0" Grid.Row="3">
                <Label Content="Прокладка:  "  Style="{DynamicResource FormLabel}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Border>
            <Border Grid.Column="0" Grid.Row="4">
                <Label Content="Вкладыш:  "  Style="{DynamicResource FormLabel}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Border>
            <Border Grid.Column="0" Grid.Row="5">
                <Label Content="Лист:  "  Style="{DynamicResource FormLabel}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Border>
            <Border Grid.Column="2" Grid.Row="0">
                <Label Content="Кол-во:  "  Style="{DynamicResource FormLabel}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Border>
            <Border Grid.Column="2" Grid.Row="1">
                <Label Content="Кол-во:  "  Style="{DynamicResource FormLabel}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Border>
            <Border Grid.Column="2" Grid.Row="2">
                <Label Content="Кол-во:  "  Style="{DynamicResource FormLabel}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Border>
            <Border Grid.Column="2" Grid.Row="3">
                <Label Content="Кол-во:  "  Style="{DynamicResource FormLabel}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Border>
            <Border Grid.Column="2" Grid.Row="4">
                <Label Content="Кол-во:  "  Style="{DynamicResource FormLabel}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Border>
            <Border Grid.Column="2" Grid.Row="5">
                <Label Content="Кол-во:  "  Style="{DynamicResource FormLabel}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Border>
            <Border Grid.Column="3"  Grid.Row="0" Style="{DynamicResource FormFieldContainer}" VerticalAlignment="Center">
                <TextBox  Name="QtyBox" Width="50" MaxLength="3" HorizontalAlignment="Left" VerticalAlignment="Center" Style="{DynamicResource FormField}"/>
            </Border>
            <Border Grid.Column="3"  Grid.Row="1" Style="{DynamicResource FormFieldContainer}" VerticalAlignment="Center">
                <TextBox  Name="QtyPartition1" Width="50" MaxLength="3" HorizontalAlignment="Left" Style="{DynamicResource FormField}"/>
            </Border>
            <Border Grid.Column="3"  Grid.Row="2" Style="{DynamicResource FormFieldContainer}" VerticalAlignment="Center">
                <TextBox  Name="QtyPartition2" Width="50" MaxLength="3" HorizontalAlignment="Left" Style="{DynamicResource FormField}"/>
            </Border>
            <Border Grid.Column="3"  Grid.Row="3" Style="{DynamicResource FormFieldContainer}" VerticalAlignment="Center">
                <TextBox  Name="QtyGasket" Width="50" MaxLength="3" HorizontalAlignment="Left" VerticalAlignment="Center" Style="{DynamicResource FormField}"/>
            </Border>
            <Border Grid.Column="3"  Grid.Row="4" Style="{DynamicResource FormFieldContainer}" VerticalAlignment="Center">
                <TextBox  Name="QtyLiner" Width="50" MaxLength="3" HorizontalAlignment="Left" VerticalAlignment="Center" Style="{DynamicResource FormField}"/>
            </Border>
            <Border Grid.Column="3"  Grid.Row="5" Style="{DynamicResource FormFieldContainer}" VerticalAlignment="Center">
                <TextBox  Name="QtySheet" Width="50" MaxLength="3" HorizontalAlignment="Left" VerticalAlignment="Center" Style="{DynamicResource FormField}"/>
            </Border>
            <Grid Grid.Column="1" Grid.Row="0" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Border Grid.Column="0">
                    <TextBox  Name="BoxName" Style="{DynamicResource FormField}" HorizontalAlignment="Stretch" IsReadOnly="True" />
                </Border>
                <Border Grid.Column="1">
                    <Button Name="BoxSelectButton" Tag="Box" Click="SelectOnClick" Style="{DynamicResource ButtonGlyph}" Margin="0,0,0,0">
                        <Image Style="{DynamicResource SelectImage}"/>
                    </Button>
                </Border>
                <Border Grid.Column="2">
                    <Button Name="BoxDeleteButton"  Tag="Box" Click="DeleteOnClick"   Style="{DynamicResource ButtonGlyph}" Margin="0,0,0,0"  >
                        <Image Style="{DynamicResource CloseImage}"/>
                    </Button>
                </Border>
            </Grid>
            <Grid Grid.Column="1" Grid.Row="1" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Border Grid.Column="0">
                    <TextBox  Name="PartitionName" Style="{DynamicResource FormField}" HorizontalAlignment="Stretch" IsReadOnly="True" />
                </Border>
                <Border Grid.Column="1">
                    <Button Name="PartitionSelectButton" Tag="Partition" Click="SelectOnClick"  Style="{DynamicResource ButtonGlyph}" Margin="0,0,0,0"  >
                        <Image Style="{DynamicResource SelectImage}"/>
                    </Button>
                </Border>
                <Border Grid.Column="2">
                    <Button Name="PartitionDeleteButton" Tag="Partition" Click="DeleteOnClick"  Style="{DynamicResource ButtonGlyph}" Margin="0,0,0,0"  >
                        <Image Style="{DynamicResource CloseImage}"/>
                    </Button>
                </Border>
            </Grid>
            <Grid Grid.Column="1" Grid.Row="2" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Border Grid.Column="0">
                    <TextBox  Name="Partition2Name" Style="{DynamicResource FormField}" IsReadOnly="True" />
                </Border>
                <Border Grid.Column="1">
                    <Button Name="Partition2SelectButton"  ToolTip="Добавить" Visibility="Hidden"  Style="{DynamicResource ButtonGlyph}" Margin="0,0,0,0"  >
                        <Image Style="{DynamicResource SelectImage}"/>
                    </Button>
                </Border>
                <Border Grid.Column="2">
                    <Button Name="Partition2DeleteButton" Visibility="Hidden" Style="{DynamicResource ButtonGlyph}" Margin="0,0,0,0"  >
                        <Image Style="{DynamicResource CloseImage}"/>
                    </Button>
                </Border>
            </Grid>
            <Grid Grid.Column="1" Grid.Row="3" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Border Grid.Column="0">
                    <TextBox  Name="GasketName" Style="{DynamicResource FormField}" HorizontalAlignment="Stretch" IsReadOnly="True" />
                </Border>
                <Border Grid.Column="1">
                    <Button Name="GasketSelectButton" Tag="Gasket" Click="SelectOnClick" Style="{DynamicResource ButtonGlyph}" Margin="0,0,0,0"  >
                        <Image Style="{DynamicResource SelectImage}"/>
                    </Button>
                </Border>
                <Border Grid.Column="2">
                    <Button Name="GasketDeleteButton" Tag="Gasket" Click="DeleteOnClick" Style="{DynamicResource ButtonGlyph}" Margin="0,0,0,0"  >
                        <Image Style="{DynamicResource CloseImage}"/>
                    </Button>
                </Border>
            </Grid>
            <Grid Grid.Column="1" Grid.Row="4" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Border Grid.Column="0">
                    <TextBox  Name="LinerName" Style="{DynamicResource FormField}" HorizontalAlignment="Stretch" IsReadOnly="True" />
                </Border>
                <Border Grid.Column="1">
                    <Button Name="LinerSelectButton"  Tag="Liner" Click="SelectOnClick" Style="{DynamicResource ButtonGlyph}" Margin="0,0,0,0"  >
                        <Image Style="{DynamicResource SelectImage}"/>
                    </Button>
                </Border>
                <Border Grid.Column="2">
                    <Button Name="LinerDeleteButton" Tag="Liner" Click="DeleteOnClick" Style="{DynamicResource ButtonGlyph}" Margin="0,0,0,0"  >
                        <Image Style="{DynamicResource CloseImage}"/>
                    </Button>
                </Border>
            </Grid>
            <Grid Grid.Column="1" Grid.Row="5" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Border Grid.Column="0">
                    <TextBox  Name="SheetName" Style="{DynamicResource FormField}" HorizontalAlignment="Stretch" IsReadOnly="True"/>
                </Border>
                <Border Grid.Column="1">
                    <Button Name="SheetSelectButton" Tag="Sheet" Click="SelectOnClick"  Style="{DynamicResource ButtonGlyph}" Margin="0,0,0,0"  >
                        <Image Style="{DynamicResource SelectImage}"/>
                    </Button>
                </Border>
                <Border Grid.Column="2">
                    <Button Name="SheetDeleteButton" Tag="Sheet" Click="DeleteOnClick" Style="{DynamicResource ButtonGlyph}" Margin="0,0,0,0"  >
                        <Image Style="{DynamicResource CloseImage}"/>
                    </Button>
                </Border>
            </Grid>
        </Grid>

    </Grid>


</controls:ControlBase>