<UserControl x:Class="Client.Interfaces.Preproduction.CompositionList"
    xmlns:local="clr-namespace:Client.Interfaces.Preproduction"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"         
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"    
    xmlns:s="clr-namespace:System;assembly=mscorlib"
    mc:Ignorable="d" 
    d:DesignWidth="1900" d:DesignHeight="800"  
    Style="{DynamicResource UserControlCanvas}"  >

    <UserControl.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{DynamicResource DataGridContainerHolder}">
        <Grid Style="{DynamicResource DataGridContainer}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"  Grid.Column="0" Style="{DynamicResource GridControlPanel}">
                <Border>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="11.5*"/>
                            <ColumnDefinition Width="0.5*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"></RowDefinition>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Name="GridTkListToolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left">
                            <Button Name="ResreshButton"    Content="Обновить" Click="ResreshButton_Click"  Style="{DynamicResource Button}"  />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                            <Button Name="GetButton"    Content="Получить данные" Click="GetButton_Click"  Style="{DynamicResource Button}" />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="1" Style="{DynamicResource FormHeaderButtonsPanel}"  HorizontalAlignment="Right" >
                            <Button Name="HelpButton" ToolTip="Документация" Width="24" MinWidth="24"  Content="?"  Style="{DynamicResource Button}" Click="HelpButton_Click"  Margin="0,0,0,0" />
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>

            <Border Grid.Row="1" Grid.Column="0" Style="{DynamicResource DataGridMainHolder}" >
                <controls:GridBox x:Name="CartonGrid" />
            </Border>
            <!--
            <ScrollViewer Grid.Row="2" Grid.Column="0" HorizontalAlignment="Stretch" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Visible">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="130"/>
                            <ColumnDefinition Width="180"/>
                            <ColumnDefinition Width="180"/>
                            <ColumnDefinition Width="180"/>
                            <ColumnDefinition Width="180"/>
                            <ColumnDefinition Width="180"/>
                            <ColumnDefinition Width="180"/>
                            <ColumnDefinition Width="180"/>
                        </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Grid.Row="0" HorizontalAlignment="Stretch" BorderBrush="Black" BorderThickness="1,0,1,2">
                        <TextBlock Text="Картон/Формат" Style="{DynamicResource DIGridHeader}"/>
                    </Border>
                    <Border Grid.Column="0" Grid.Row="1" HorizontalAlignment="Stretch">
                        <Grid Name="HeaderGrid" Style="{DynamicResource DIGrid}" HorizontalAlignment="Stretch">

                        </Grid>
                    </Border>

                    <Border Grid.Column="1" Grid.Row="0" Style="{DynamicResource DIBorderGridHeaderDate}" >
                            <TextBlock Name="FirstGridHeader" Style="{DynamicResource DIGridHeader}"/>
                        </Border>
                        <Grid Grid.Column="1" Grid.Row="1" Name="FirstGrid" Style="{DynamicResource DIGrid}" HorizontalAlignment="Stretch">

                        </Grid>


                        <Border Grid.Column="2" Grid.Row="0" Style="{DynamicResource DIBorderGridHeaderDate}">
                            <TextBlock Name="SecondGridHeader" Style="{DynamicResource DIGridHeader}"/>
                        </Border>
                        <Grid Grid.Column="2" Grid.Row="1" Name="SecondGrid" Style="{DynamicResource DIGrid}">

                        </Grid>

                        <Border Grid.Column="3" Grid.Row="0" Style="{DynamicResource DIBorderGridHeaderDate}">
                            <TextBlock Name="ThirdGridHeader" Style="{DynamicResource DIGridHeader}"/>
                        </Border>
                        <Grid Grid.Column="3" Grid.Row="1" Name="ThirdGrid" Style="{DynamicResource DIGrid}">

                        </Grid>

                        <Border Grid.Column="4" Grid.Row="0" Style="{DynamicResource DIBorderGridHeaderDate}">
                            <TextBlock Name="FourthGridHeader" Style="{DynamicResource DIGridHeader}"/>
                        </Border>
                        <Grid Grid.Column="4" Grid.Row="1" Name="FourthGrid" Style="{DynamicResource DIGrid}">

                        </Grid>

                        <Border Grid.Column="5" Grid.Row="0" Style="{DynamicResource DIBorderGridHeaderDate}">
                            <TextBlock Name="FifthGridHeader" Style="{DynamicResource DIGridHeader}"/>
                        </Border>
                        <Grid Grid.Column="5" Grid.Row="1" Name="FifthGrid" Style="{DynamicResource DIGrid}">

                        </Grid>

                        <Border Grid.Column="6" Grid.Row="0" Style="{DynamicResource DIBorderGridHeaderDate}">
                            <TextBlock Name="SixthGridHeader" Style="{DynamicResource DIGridHeader}"/>
                        </Border>
                        <Grid Grid.Column="6" Grid.Row="1" Name="SixthGrid" Style="{DynamicResource DIGrid}">

                        </Grid>

                        <Border Grid.Column="7" Grid.Row="0" Style="{DynamicResource DIBorderGridHeaderDate}">
                            <TextBlock Name="SeventhGridHeader" Style="{DynamicResource DIGridHeader}"/>
                        </Border>
                        <Grid Grid.Column="7" Grid.Row="1" Name="SeventhGrid" Style="{DynamicResource DIGrid}">

                        </Grid>
                    </Grid>
                </ScrollViewer>
-->
        </Grid>
    </Border>
</UserControl>
