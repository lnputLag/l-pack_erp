<controls:ControlBase x:Class="Client.Interfaces.Preproduction.PreproductionConfirmOrderList"
    xmlns:local="clr-namespace:Client.Interfaces.Preproduction"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"         
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"    
    xmlns:s="clr-namespace:System;assembly=mscorlib"

    mc:Ignorable="d" 
    d:DesignWidth="1600" d:DesignHeight="800"  
    Style="{DynamicResource UserControlCanvas}"  >

    <UserControl.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>


    <Border Style="{DynamicResource DataGridContainerHolder}">
        <Grid Style="{DynamicResource DataGridContainer}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"  Grid.Column="0" Style="{DynamicResource GridControlPanel}">
                <Border>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="11*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"></RowDefinition>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Name="GridToolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left">
                            <Button Name="RefreshButton"    Content="Обновить"  Style="{DynamicResource Button}" />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                            <Button Name="ConfirmButton" IsEnabled="False" Content="Утвердить дату"  Style="{DynamicResource Button}" Click="ConfirmButton_Click"  />
                            <Button Name="ShowInCompositionListButton" Content="Отчёт по композициям"  Style="{DynamicResource Button}" />
                            <Button Name="ShowChatButton" Content="Чат"  Style="{DynamicResource Button}" />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                            <Button Name="ShowWorkcenterWorkloadButton" Content="Загруженность"  Style="{DynamicResource Button}" />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                            <Button Name="ChangeFactoryButton" Content="Изменить площадку"  Style="{DynamicResource Button}" />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                            <Label Content="Поиск:"  Style="{DynamicResource TopLabel}" />
                            <TextBox Style="{DynamicResource FormFieldTopPanel}" Width="100" TextWrapping="NoWrap" Name="SearchText" Text=""/>
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                            <controls:SelectBox x:Name="FactorySelectBox" SelectedItemChanged="FactorySelectBox_SelectedItemChanged"  Style="{DynamicResource CustomFormField}" ListBoxMinHeight="80" Width="120" Autocomplete="False"/>
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                            <controls:SelectBox x:Name="StatusSelectBox" SelectedItemChanged="StatusSelectBox_SelectedItemChanged"  Style="{DynamicResource CustomFormField}" ListBoxMinHeight="110" Width="150" Autocomplete="False"/>
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                            <controls:SelectBox x:Name="FullnessOrderSelectBox" SelectedItemChanged="FullnessOrderSelectBox_SelectedItemChanged"  Style="{DynamicResource CustomFormField}" ListBoxMinHeight="80" Width="120" Autocomplete="False"/>
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                            
                            <controls:SelectBox x:Name="ManagerSelectBox" SelectedItemChanged="ManagerSelectBox_SelectedItemChanged"  Style="{DynamicResource CustomFormField}" ListBoxMinHeight="110" Width="140" Autocomplete="False"/>
                            <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>

                            <Button     Content="В Excel"  Style="{DynamicResource Button}" Click="ExcelButton_Click"   />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>

                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="1" Style="{DynamicResource FormHeaderButtonsPanel}"  HorizontalAlignment="Right" >
                            <Button Name="HelpButton" ToolTip="Документация" Width="24" MinWidth="24"  Content="?"  Style="{DynamicResource Button}" Margin="0,0,0,0" />
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>

            <Grid Grid.Row="1"  Grid.Column="0">
                <Border Style="{DynamicResource DataGridMainHolder}" >
                    <controls:GridBox4 x:Name="OrderGrid" />
                </Border>
            </Grid>

            <Grid Grid.Row="2"  Grid.Column="0" >
                <Border Style="{DynamicResource DataGridMainHolder}" >
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel  Name="PositionGridToolbar" Grid.Row="0" Grid.Column="0" Margin="0,5" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left">
                            <TextBlock Height="24" Padding="0,4,0,0">Позиции заявки</TextBlock>
                            <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"/>
                            <Button Name="ShowTechnologicalMapButton"  Content="Открыть техкарту"  Style="{DynamicResource Button}" />
                            <Button Name="EditSchemeButton"  Content="Изменить схему"  Style="{DynamicResource Button}"/>
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>

                            <Button Name="SendToRackButton"  Content="На стеллажный склад"  Style="{DynamicResource Button}" />
                            <Button Name="NonSendToRackButton"  Content="На обычный склад"  Style="{DynamicResource Button}" />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
                        </StackPanel>

                        <controls:GridBox4 x:Name="PositionGrid" Grid.Row="1" Grid.Column="0"/>
                    </Grid>
                </Border>
            </Grid>

            <Grid Grid.Row="3"  Grid.Column="0">
                <Border Style="{DynamicResource DataGridMainHolder}" >
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel  Name="HistoryGridToolbar" Grid.Row="0" Grid.Column="0" Margin="0,5" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left">
                            <TextBlock Height="24" Padding="0,4,0,0">История изменений</TextBlock>
                            <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"/>
                        </StackPanel>

                        <controls:GridBox4 x:Name="HistoryGrid" Grid.Row="1" Grid.Column="0"/>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>

</controls:ControlBase>
