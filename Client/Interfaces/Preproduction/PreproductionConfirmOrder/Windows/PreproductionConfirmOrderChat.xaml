<controls:ControlBase x:Class="Client.Interfaces.Preproduction.PreproductionConfirmOrderChat"
    xmlns:local="clr-namespace:Client.Interfaces.Preproduction"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"         
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"    
    xmlns:s="clr-namespace:System;assembly=mscorlib"

    mc:Ignorable="d" 
    Width="800" MinHeight="500"
    Style="{DynamicResource UserControlCanvas}"  
>

    <UserControl.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{DynamicResource DataGridContainerHolder}">
        <Grid Style="{DynamicResource DataGridContainer}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Column="0" Grid.Row="0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="11*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"></RowDefinition>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left">
                        <Border Style="{DynamicResource FormLabelContainer}">
                            <Label Name="TitleLabel" Content="Чат по заявке № " Style="{DynamicResource FormLabel}"/>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Style="{DynamicResource FormHeaderButtonsPanelR}" >
                        <Button Name="HelpButton" Click="HelpButton_Click"  ToolTip="Документация" Width="24" MinWidth="24"  Content="?"  Style="{DynamicResource Button}"  />
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Row="1" Grid.Column="0" Style="{DynamicResource FormVerticalPanelSpacer}"/>

            <Border Grid.Row="2" Grid.Column="0" Style="{DynamicResource DataGridMainHolder}" >
                <ScrollViewer Name="СhatContainer" ScrollChanged="СhatContainer_ScrollChanged">
                    <Grid ShowGridLines="false" x:Name="ChatGrid" Style="{DynamicResource ChatContainer}" VerticalAlignment="Top">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="4*"/>
                            <ColumnDefinition Width="1*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                        </Grid.RowDefinitions>
                    </Grid>
                </ScrollViewer>
            </Border>

            <Border Grid.Row="3" Grid.Column="0" Style="{DynamicResource FormVerticalPanelSpacer}"/>

            <Border Grid.Row="4" Grid.Column="0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Column="0" Grid.Row="0" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="Текст сообщения:" HorizontalAlignment="Left" Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Column="0" Grid.Row="1" Style="{DynamicResource FormFieldContainer}">
                        <TextBox Name="MessageTextBox" Margin="5,0,5,0" TextChanged="MessageTextBox_TextChanged" Height="48" HorizontalAlignment="Stretch" TextWrapping="Wrap" Style="{DynamicResource FormField}"/>
                    </Border>
                </Grid>
            </Border>

            <Border Grid.Row="5" Grid.Column="0">
                <StackPanel Name="FormToolbar" Orientation="Vertical" HorizontalAlignment="Right" Margin="0,0,0,5">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Border Style="{DynamicResource FormFieldContainer}">
                            <CheckBox Name="EmailCheckBox" Style="{DynamicResource FormCheckBox}"/>
                        </Border>
                        <Border Style="{DynamicResource FormLabelContainer}">
                            <Label Content="email" HorizontalAlignment="Left" Style="{DynamicResource FormLabel}"/>
                        </Border>
                        <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>
                        <Border Style="{DynamicResource FormFieldContainer}">
                            <CheckBox Name="UnreadCheckBox" Style="{DynamicResource FormCheckBox}"/>
                        </Border>
                        <Border Style="{DynamicResource FormLabelContainer}">
                            <Label Content="Оставить непрочитанным" HorizontalAlignment="Left" Style="{DynamicResource FormLabel}"/>
                        </Border>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Name="SaveButton" IsEnabled="False"  Click="SaveButton_Click"  Content="Отправить"  Style="{DynamicResource FButtonPrimary}" />
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                        <Button Name="CancelButton" Click="CancelButton_Click"  Content="Отмена" Style="{DynamicResource FButtonCancel}" />
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>
    </Border>



</controls:ControlBase>
