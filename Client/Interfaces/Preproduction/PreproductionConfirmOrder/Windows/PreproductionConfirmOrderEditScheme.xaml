<controls:ControlBase x:Class="Client.Interfaces.Preproduction.PreproductionConfirmOrderEditScheme"
    xmlns:local="clr-namespace:Client.Interfaces.Preproduction"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"         
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"    
    xmlns:s="clr-namespace:System;assembly=mscorlib"

    mc:Ignorable="d" 
    Width="800" MinHeight="330"
    Style="{DynamicResource UserControlCanvas}"  
>

    <UserControl.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{DynamicResource DataGridContainerHolder}">
        <Grid Style="{DynamicResource DataGridContainer}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="5"/>
            </Grid.RowDefinitions>

            <Border Grid.Column="0" Grid.Row="0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="11*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"></RowDefinition>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left">
                    </StackPanel>

                    <StackPanel Grid.Column="1" Style="{DynamicResource FormHeaderButtonsPanelR}" >
                        <Button Name="HelpButton" Click="HelpButton_Click"  ToolTip="Документация" Width="24" MinWidth="24"  Content="?"  Style="{DynamicResource Button}"  />
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Row="1" Grid.Column="0" Style="{DynamicResource FormVerticalPanelSpacer}"/>

            <Border Grid.Column="0" Grid.Row="2">
                <Grid HorizontalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="*"/>

                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="4">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="1.5*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Column="0" Grid.Row="0" Style="{DynamicResource FormLabelContainer}">
                                <Label Content="Продукция:" Style="{DynamicResource FormLabel}" />
                            </Border>
                            <Border Grid.Column="1" Grid.Row="0" Style="{DynamicResource FormFieldContainer}">
                                <TextBox Name="ProductNameTextBox" Text="" MaxWidth="215" Width="215" HorizontalAlignment="Left" TextWrapping="NoWrap"  Style="{DynamicResource FormField}" IsReadOnly="True"/>
                            </Border>
                        </Grid>
                    </Border>

                    <Border Name="LastSchemeTextBoxLabelBorder" Visibility="Collapsed" Grid.Column="0" Grid.Row="1" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="Последняя схема:" ToolTip="Последняя использованная схема для выбранной продукции" Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Column="1" Grid.Row="1" Name="LastSchemeTextBoxBorder" Visibility="Collapsed" Style="{DynamicResource FormFieldContainer}">
                        <TextBox Name="LastSchemeTextBox" TextChanged="LastSchemeTextBox_TextChanged" HorizontalAlignment="Left" Text="" MaxWidth="215" Width="215" TextWrapping="NoWrap" ToolTip="Последняя использованная схема для выбранной продукции" Style="{DynamicResource FormField}" IsReadOnly="True"/>
                    </Border>
                    <Border Name="LastSchemeWorkloadGridLabelBorder" Visibility="Collapsed" Grid.Column="2" Grid.Row="1" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="Загруженность:" Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Column="3" Grid.Row="1" Name="LastSchemeWorkloadGridBorder" Visibility="Collapsed" Style="{DynamicResource FormFieldContainer}">
                        <controls:GridBox x:Name="LastSchemeWorkloadGrid" MinHeight="70" MaxHeight="70" Margin="0,0,5,0"/>
                    </Border>

                    <Border Grid.Column="0" Grid.Row="2" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="Текущая схема:" ToolTip="Текущая схема для выбранной позиции" Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Column="1" Grid.Row="2" Style="{DynamicResource FormFieldContainer}">
                        <TextBox Name="CurrentSchemeTextBox"  HorizontalAlignment="Left" Text="Г-Рт3-Пл.выс.-Cкл.мнт.Об.пач.Укл.г/я" MaxWidth="215" Width="215" TextWrapping="NoWrap" ToolTip="Текущая схема для выбранной позиции" Style="{DynamicResource FormField}" IsReadOnly="True"/>
                    </Border>
                    <Border Grid.Column="2" Grid.Row="2" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="Загруженность:" Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Column="3" Grid.Row="2" Style="{DynamicResource FormFieldContainer}">
                        <controls:GridBox x:Name="CurrentSchemeWorkloadGrid" MinHeight="70" MaxHeight="70" Margin="0,0,5,0"/>
                    </Border>


                    <Border Grid.Column="0" Grid.Row="3"  Style="{DynamicResource FormLabelContainer}">
                        <Label Content="Новая схема :" ToolTip="Изменённая схема для выбранной позиции" Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Column="1" Grid.Row="3" Style="{DynamicResource FormFieldContainer}">
                        <controls:SelectBox  x:Name="NewSchemeSelectBox" SelectedItemChanged="NewSchemeSelectBox_SelectedItemChanged" Style="{DynamicResource CustomFormField}" MaxWidth="215" Width="215" Autocomplete="True" HorizontalAlignment="Left" ToolTip="Изменённая схема для выбранной позиции" ListBoxMinWidth="180"/>
                    </Border>
                    <Border Grid.Column="2" Grid.Row="3" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="Загруженность:" Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Column="3" Grid.Row="3" Style="{DynamicResource FormFieldContainer}">
                        <controls:GridBox x:Name="NewSchemeWorkloadGrid" MinHeight="70" MaxHeight="70" Margin="0,0,5,0"/>
                    </Border>
                    
                </Grid>
            </Border>

            <Border Grid.Row="3" Grid.Column="0" Style="{DynamicResource FormVerticalPanelSpacer}"/>

            <Border Grid.Column="0" Grid.Row="4">
                <StackPanel Grid.Row="0" Grid.Column="0" Name="FormToolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Right"  >
                    <TextBlock  Name="FormStatus"  TextAlignment="Left" Style="{DynamicResource FStatusBar}" HorizontalAlignment="Stretch" />
                    <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                    <Button Name="SaveButton"   Click="SaveButton_Click"  Content="ОК"  Style="{DynamicResource FButtonPrimary}"    />
                    <Button Name="CancelButton" Click="CancelButton_Click"  Content="Отмена"     Style="{DynamicResource FButtonCancel}"    />
                </StackPanel>
            </Border>

        </Grid>
    </Border>



</controls:ControlBase>
