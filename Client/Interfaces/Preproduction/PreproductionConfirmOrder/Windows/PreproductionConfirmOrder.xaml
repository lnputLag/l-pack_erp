<controls:ControlBase x:Class="Client.Interfaces.Preproduction.PreproductionConfirmOrder"
    xmlns:local="clr-namespace:Client.Interfaces.Preproduction"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"         
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"    
    xmlns:s="clr-namespace:System;assembly=mscorlib"

    mc:Ignorable="d" 
    MinWidth="900" MinHeight="680"
    Style="{DynamicResource UserControlCanvas}"  
>

    <UserControl.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{DynamicResource DataGridContainerHolder}"  >
        <Grid Style="{DynamicResource DataGridContainer}">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Grid.Column="0" Style="{DynamicResource GridControlPanel}">
                <Border>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="11*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"></RowDefinition>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Name="GridToolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left">

                            <Border Style="{DynamicResource FormLabelContainer}">
                                <Label Content="Дата:" HorizontalAlignment="Left" Style="{DynamicResource FormLabel}"/>
                            </Border>
                            <Border HorizontalAlignment="Left">
                                <Grid Style="{DynamicResource FormFieldDateHolder}">


                                    <controls:DatePicker x:Name="DtFrom" Style="{DynamicResource DateEditStyle}" Text="20.10.2023" TextChanged="ConfirmedDateTextBox_TextChanged">
                                    </controls:DatePicker>
                                </Grid>
                            </Border>

                            <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>
                            
                            <Border Style="{DynamicResource FormFieldContainer}">
                                <CheckBox Name="ChangeDateCheckBox" Click="ChangeDateCheckBox_Click" Style="{DynamicResource FormCheckBox}"/>
                            </Border>
                            <Border Style="{DynamicResource FormLabelContainer}">
                                <Label Name="ChangeDateLabel" Content="Изменить дату" HorizontalAlignment="Left" Style="{DynamicResource FormLabel}"/>
                            </Border>

                            <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Style="{DynamicResource FormHeaderButtonsPanelR}" >
                            <Button Name="HelpButton" Click="HelpButton_Click" ToolTip="Документация" Width="24" MinWidth="24"  Content="?"  Style="{DynamicResource Button}"  />
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>

            <Border Grid.Row="1" Grid.Column="0" >
                <Grid ShowGridLines="false" Style="{DynamicResource FromGrid2}" >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Grid.Column="0">
                        <Border Style="{DynamicResource DataGridMainHolder}" >
                            <controls:GridBox x:Name="Grid" />
                        </Border>
                    </Grid>

                    <Grid Grid.Row="0" Grid.Column="0" Height="100"  VerticalAlignment="Top" Panel.ZIndex="10">
                        <controls:SplashControl x:Name="SplashControl"/>
                    </Grid>
                </Grid>
            </Border>

            <Grid Grid.Row="2" Grid.Column="0" Style="{DynamicResource GridControlPanel}">
                <Border>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Name="Toolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Right"  >
                            <TextBlock  Name="FormStatus"  TextAlignment="Left" Style="{DynamicResource FStatusBar}" HorizontalAlignment="Stretch" />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                            <Button Name="SaveButton"   Click="SaveButton_Click" Content="ОК"  Style="{DynamicResource FButtonPrimary}"    />
                            <Button Name="CancelButton" Click="CancelButton_Click"  Content="Отмена"     Style="{DynamicResource FButtonCancel}"    />
                        </StackPanel>

                    </Grid>
                </Border>
            </Grid>

        </Grid>
    </Border>

   
    
</controls:ControlBase>
