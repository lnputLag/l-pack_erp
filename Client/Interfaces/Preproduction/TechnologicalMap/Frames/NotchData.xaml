<UserControl 
    x:Class="Client.Interfaces.Preproduction.NotchData"
    xmlns:local="clr-namespace:Client.Interfaces.Preproduction"
    xmlns:lib="clr-namespace:Client.Common"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"             
    
    mc:Ignorable="d"
    
    d:DesignWidth="200" d:DesignHeight="90" 
    
    Width="200" Height="90"
    
    Style="{DynamicResource UserControlCanvas}"
    HorizontalAlignment="Left" VerticalAlignment="Top">

    <UserControl.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{DynamicResource FormContainerHolder}" Margin="1" >
        <Grid Style="{DynamicResource FormContainer}" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Style="{DynamicResource FormLabelContainer}">
                <Label Name="TitleOfNotchData" Content="Размер просечки :" Style="{DynamicResource FormLabel}" HorizontalAlignment="Center"/>
            </Border>

            <Border Grid.Column="0" Grid.Row="1" Style="{DynamicResource FormFieldContainer}">
                <TextBox Name="SizeNotchTextBox" MinWidth="120" HorizontalAlignment="Center" TextWrapping="NoWrap" Style="{DynamicResource FormField}"/>
            </Border>

            <Border Grid.Column="0" Grid.Row="2" VerticalAlignment="Bottom" Style="{DynamicResource FormFieldContainer}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Column="0" Grid.Row="0" Style="{DynamicResource FormButtonsPanelSpacer}"/>
                    <Border Grid.Column="1" Grid.Row="0">
                        <Button Content="ОК" Name="OkButton" Click="OkButton_Click" HorizontalAlignment="Stretch" Style="{DynamicResource Button}" />
                    </Border>

                    <Border Grid.Column="2" Grid.Row="0">
                        <Button Content="Отмена" Name="CancelButton" Click="CancelButton_Click" HorizontalAlignment="Stretch" Style="{DynamicResource Button}" />
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
