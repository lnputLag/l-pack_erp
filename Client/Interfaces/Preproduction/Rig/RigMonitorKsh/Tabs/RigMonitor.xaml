<controls:ControlBase x:Class="Client.Interfaces.Preproduction.Rig.RigMonitorKsh.Tabs.RigMonitor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:Client.Interfaces.Main"
             xmlns:local="clr-namespace:Client.Interfaces.Preproduction.Rig.RigMonitorKsh.Tabs"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1600" Style="{DynamicResource UserControlCanvas}">

    <UserControl.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{DynamicResource DataGridContainerHolder}">
        <Grid Style="{DynamicResource DataGridContainer}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Style="{DynamicResource GridControlPanel}">
                <Button x:Name="Refresh" Content="Обновить" Style="{DynamicResource Button}" />
            </Grid>

            <Border Grid.Row="1" Style="{DynamicResource FormVerticalPanelSpacer}"/>

            <Viewbox Grid.Row="2" Stretch="Uniform" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Grid Width="1820" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Name="MainGrid"/>
            </Viewbox>
        </Grid>
    </Border>

</controls:ControlBase>