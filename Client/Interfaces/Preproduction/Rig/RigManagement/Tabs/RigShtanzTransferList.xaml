<controls:ControlBase x:Class="Client.Interfaces.Preproduction.Rig.RigShtanzTransferList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Client.Interfaces.Preproduction.Rig"
             xmlns:controls="clr-namespace:Client.Interfaces.Main"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="1500"
             Style="{DynamicResource UserControlCanvas}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Style="{DynamicResource DataGridContainer}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>


        <Grid Grid.Row="0"  Grid.Column="0" Style="{DynamicResource GridControlPanel}">
            <Border>
                <StackPanel Grid.Row="0" Grid.Column="0" Name="GridToolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left">
                    <Button Name="RefreshButton"  Tag="Refresh"  Content="Обновить"  Style="{DynamicResource Button}" />
                    <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                    <Button Name="BindShipmentButton" Content="Привязать" Tag="BindShipment" Style="{DynamicResource Button}"/>

                    <Button Name="UnbindShipmentButton" Content="Отвязать" Tag="UnbindShipment" Style="{DynamicResource Button}" />
                    <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                    <Button Name="ToExcelButton" Content="В Excel" Tag="ExportToExcel" Style="{DynamicResource Button}" />
                    <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                    <Label Content="Поиск:"  Style="{DynamicResource TopLabel}" />
                    <TextBox Style="{DynamicResource FormFieldTopPanel}" Width="60" TextWrapping="NoWrap" Name="SearchText" Text=""  />

                    <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>
                    <Label Content="Менеджер:"  Style="{DynamicResource TopLabel}" />
                    <controls:SelectBox x:Name="ManagerName" Style="{DynamicResource CustomFormField}" Width="140" ListBoxMinHeight="220" Autocomplete="False" SelectedItemChanged="ManagerName_SelectedItemChanged" />
                    <TextBox Name="SelectedManagerCount" Text="Выбрано 2" Style="{DynamicResource FormFieldTopPanel}" Width="140" IsReadOnly="True" Visibility="Collapsed" />
                    <Button Name="SelectManagerButton"   ToolTip="Выбрать" Tag="SelectManager" Style="{DynamicResource ButtonGlyph}"  Margin="-1,0,0,0"  >
                        <Image Style="{DynamicResource SelectImage}" />
                    </Button>

                    <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                    <Label Content="Площадка:"  Style="{DynamicResource TopLabel}" />
                    <controls:SelectBox x:Name="Factory" Style="{DynamicResource CustomFormField}" Width="140" Autocomplete="False" SelectedItemChanged="Factory_SelectedItemChanged" />
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Row="1"  Grid.Column="0">
            <Border Style="{DynamicResource DataGridMainHolder}">
                <controls:GridBox4 x:Name="ShtanzGrid" />
            </Border>
        </Grid>

        <Grid Grid.Row="0"  Grid.Column="2" Style="{DynamicResource GridControlPanel}">
            <Border>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="8*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Name="PreformGridToolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left"  >
                        <Button Name="ShowTkButton"    Content="Показать ТК" Tag="ShowTk" Style="{DynamicResource Button}" />
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                    </StackPanel>

                    <StackPanel Grid.Row="0" Grid.Column="1" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Right" >
                        <Button Name="HelpButton"  Width="24" MinWidth="24"  Content="?" Tag="Help"  Style="{DynamicResource Button}" Margin="0" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>


        <Grid Grid.Row="1"  Grid.Column="2">
            <Border Style="{DynamicResource DataGridMainHolder}">
                <controls:GridBox4 x:Name="TechMapGrid" />
            </Border>
        </Grid>
    </Grid>
</controls:ControlBase>
