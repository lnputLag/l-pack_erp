<controls:ControlBase x:Class="Client.Interfaces.Preproduction.Rig.IncomeRigTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:controls="clr-namespace:Client.Interfaces.Main"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1280">
    <UserControl.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{DynamicResource DataGridContainerHolder}">
        <Grid Style="{DynamicResource DataGridContainer}">
            <Grid.RowDefinitions>
                <RowDefinition Height="0"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Grid.RowSpan="3" Name="ProgressContainer" Visibility="Collapsed" Panel.ZIndex="10" >
                <controls:ProgressControl x:Name="Progress" Caption="Выполняется операция" />
            </Grid>

            <Grid Grid.Row="1" Style="{DynamicResource GridControlPanel}">
                <Border>
                    <Grid>
                    <StackPanel HorizontalAlignment="Stretch" Name="FirstGridToolbar" Style="{DynamicResource FormHeaderButtonsPanel}">
                        <Button Name="UpdateButton" Content="Обновить" Style="{DynamicResource Button}"/>
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}" />
                        
                        <Button Name="AddButtonFirst" Content="Создать" Style="{DynamicResource Button}"/>
                        <Button Name="EditButtonFirst" Content="Изменить" Style="{DynamicResource Button}"/>
                        <Button Name="DeleteButtonFirst" Content="Удалить" Style="{DynamicResource Button}"/>
                        

                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
                        <Label Content="Поиск:" Style="{DynamicResource TopLabel}" />
                        <TextBox Style="{DynamicResource FormFieldTopPanel}" Width="100" TextWrapping="NoWrap" Name="SearchText" Text=""/>

                        <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}" />
                        <Label Content="Поставщик:" Style="{DynamicResource TopLabel}" />
                        <controls:SelectBox MinWidth="170" ListBoxMinHeight="170" x:Name="SelectProvider" Style="{DynamicResource CustomFormField}"/>
                    </StackPanel>

                    <StackPanel Grid.Row="0" Style="{DynamicResource FormHeaderButtonsPanel}"  HorizontalAlignment="Right" Width="24">
                        <Button x:Name="HelpButton" Tag="help" Width="24" MinWidth="24"  Content="?"  Style="{DynamicResource Button}"  />
                    </StackPanel>
                    </Grid>
                </Border>
            </Grid>

            <Grid Grid.Row="2" >
                <Border Style="{DynamicResource DataGridMainHolder}">
                    <controls:GridBox4 x:Name="IncomeGrid" />
                </Border>
            </Grid>

            <Grid Grid.Row="3" Style="{DynamicResource GridControlPanel}">
                <Border>
                    <StackPanel HorizontalAlignment="Stretch" Name="SecondGridToolbar" Style="{DynamicResource FormHeaderButtonsPanel}">
                        <TextBlock Height="24" Padding="0,4,0,0">Позиции в накладной</TextBlock>
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                        <Button Name="AddButtonSecond" Content="Создать" Style="{DynamicResource Button}"/>
                        <Button Name="EditButtonSecond" Content="Изменить" Style="{DynamicResource Button}"/>
                        <Button Name="DeleteButtonSecond" Content="Удалить" Style="{DynamicResource Button}"/>
                    </StackPanel>
                </Border>
            </Grid>

            <Grid Grid.Row="4">
                <Border Style="{DynamicResource DataGridMainHolder}">
                    <controls:GridBox4 x:Name="IncomePositionGrid" />
                </Border>
            </Grid>
            
            
        </Grid>

    </Border>
</controls:ControlBase>
