<controls:ControlBase  x:Class="Client.Interfaces.Preproduction.Rig.ConsumptionRigTab"
             
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:s="clr-namespace:System;assembly=mscorlib"
             xmlns:controls="clr-namespace:Client.Interfaces.Main"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1680">
    <UserControl.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{DynamicResource DataGridContainerHolder}">
        <Grid Style="{DynamicResource DataGridContainer}">
            <Grid.RowDefinitions>
                <RowDefinition Height="0"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Grid.RowSpan="3" Name="ProgressContainer" Visibility="Collapsed" Panel.ZIndex="10">
                <controls:ProgressControl x:Name="Progress" Caption="Выполняется операция" />
            </Grid>

            <Grid Grid.Row="1" Style="{DynamicResource GridControlPanel}">
                <Border>
                    <Grid>
                        <StackPanel Name="GridToolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Stretch">
                            <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}" />
                            <Label Content="C:"  Style="{DynamicResource TopLabel}" />
                            <controls:DatePicker x:Name="FromDate"  MaxLength="10"   Style="{DynamicResource DateEditStyle}"/>
                            <Label Content="По:"  Style="{DynamicResource TopLabel}"/>
                            <controls:DatePicker x:Name="ToDate"  MaxLength="10"   Style="{DynamicResource DateEditStyle}"/>
                            <Button Name="Refresh" Content="Показать" Style="{DynamicResource Button}" />
                             <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
                        
                            <Button Name="AddButton" Tag="add_rig_consumption" Content="Создать" Style="{DynamicResource Button}"/>
                            <Button Name="EditButton" Content="Изменить" Style="{DynamicResource Button}"/>
                            <Button Name="WriteOff" Content="Списать" Style="{DynamicResource Button}"/>
                            <Button Name="CancelWriteOff" Content="Отмена списания" Style="{DynamicResource Button}"/>
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>

                            <Button Name="ShowTK" Content="Показать ТК" Style="{DynamicResource Button}"/>
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>

                            <CheckBox Name="SelectContrlAgent" Content="Выбрать все" Style="{DynamicResource CheckBoxTopPanel}"/>
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>

                            <Label Content="Поиск:" Style="{DynamicResource TopLabel}"/>
                            <TextBox Style="{DynamicResource FormFieldTopPanel}" Width="100" TextWrapping="NoWrap" Name="SearchText" Text=""/>
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                            
                            <CheckBox Name="SelectGreenRow" Content="Все формы" Style="{DynamicResource CheckBoxTopPanel}"/>
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>

                            <Label Content="Тип:"  Style="{DynamicResource TopLabel}" />
                            <controls:SelectBox x:Name="RigType" Style="{DynamicResource CustomFormField}" Width="150" Autocomplete="True" CompareMode="Contains" />
                             <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
                            
                            <Label Content="Завод:"  Style="{DynamicResource TopLabel}" />
                            <controls:SelectBox x:Name="FactoryType" Style="{DynamicResource CustomFormField}" Width="120" Autocomplete="True" CompareMode="Contains" />
                            <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"/>

                            <Button Name="Excel" Content="В Excel" Style="{DynamicResource Button}"/>
                        </StackPanel>

                        <StackPanel Grid.Row="0" Style="{DynamicResource FormHeaderButtonsPanel}"  HorizontalAlignment="Right" Width="24" >
                            <Button x:Name="HelpButton" Tag="help" Width="24" MinWidth="24"  Content="?"  Style="{DynamicResource Button}"  />
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>

            <Grid Grid.Row="2" Grid.RowSpan="2" MinHeight="200">
                <Border Style="{DynamicResource DataGridMainHolder}">
                    <controls:GridBox4 x:Name="RigConsumtionGrid"/>
                </Border>
            </Grid>
        </Grid>
    </Border>
</controls:ControlBase >
