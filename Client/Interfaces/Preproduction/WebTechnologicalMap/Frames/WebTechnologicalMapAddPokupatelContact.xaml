<controls:ControlBase 
    x:Class="Client.Interfaces.Preproduction.WebTechnologicalMapAddPokupatelContact"
    xmlns:local="clr-namespace:Client.Interfaces.Preproduction"
    xmlns:lib="clr-namespace:Client.Common"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"             
    
    xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
    
    mc:Ignorable="d" 
    Width="400" Height="210"
    Style="{DynamicResource UserControlCanvas}"
    HorizontalAlignment="Left" VerticalAlignment="Top"     
    >

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{DynamicResource FormContainerHolder}" Margin="5">
        <Grid Style="{DynamicResource DataGridContainer}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Style="{DynamicResource FormControlPanel}">
                <Grid >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Column="0" Name="FormToolbar" Style="{DynamicResource FormHeaderButtonsPanel}">
                        <Button Name="SaveButton" Content="Сохранить"  Tag="Save" Style="{DynamicResource FButtonPrimary}" />
                        <Button Name="CancelButton" Content="Отмена" Tag="Close" Style="{DynamicResource Button}"/>
                    </StackPanel>
                </Grid>
            </Grid>
            <Border Grid.Row="1" Grid.Column="0" Style="{DynamicResource FormVerticalPanelSpacer}"></Border>
            <Border Grid.Row="2" Style="{DynamicResource DataGridMainHolder}">
                <Grid Style="{DynamicResource DataGridContainer}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="3*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>


                    <Border Grid.Column="0" Grid.Row="0" >
                        <Label Content="Имя :"  Style="{DynamicResource FormLabel}" HorizontalAlignment="Right" VerticalAlignment="Top"/>
                    </Border>
                    
                    <Border Grid.Column="1" Grid.Row="0" Style="{DynamicResource FormFieldContainer}">
                        <TextBox Name="PokupatelName" HorizontalAlignment="Stretch" TextWrapping="Wrap" Style="{DynamicResource FormField}"/>
                    </Border>

                    <Grid Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="0.5*"/>
                            <ColumnDefinition Width="0.5*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Grid.Row="0">
                            <Label Content="Телефон :"  Style="{DynamicResource FormLabel}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                        </Border>
                        <Border Grid.Column="1" Grid.Row="0" Style="{DynamicResource FormFieldContainer}">
                            <dxe:TextEdit Name="PhoneTextBox" 
                              Mask="\+7 \(([0-9]{3})\) ([0-9]{3})-([0-9]{2})-([0-9]{2})" 
                              MaskType="RegEx" 
                              MaskUseAsDisplayFormat="True" />
                        </Border>
                        
                        <Border Grid.Column="2" Grid.Row="0">
                            <Label Content="Доб. :"  Style="{DynamicResource FormLabel}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                        </Border>
                        <Border Grid.Column="3" Grid.Row="0" Style="{DynamicResource FormFieldContainer}">
                            <TextBox Name="ExtensionPhone" HorizontalAlignment="Stretch" TextWrapping="NoWrap" Style="{DynamicResource FormField}"/>
                        </Border>

                    </Grid>

                    <Border Grid.Column="0" Grid.Row="2">
                        <Label Content="Почта :"  Style="{DynamicResource FormLabel}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                    </Border>
                    <Border Grid.Column="1" Grid.Row="2" Style="{DynamicResource FormFieldContainer}">
                        <TextBox Name="PokupatelEmail" HorizontalAlignment="Stretch" TextWrapping="NoWrap" Style="{DynamicResource FormField}"/>
                    </Border>

                    <Grid Grid.Column="0" Grid.Row="3" Grid.ColumnSpan="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*"/>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition Width="1.5*"/>
                        </Grid.ColumnDefinitions>


                        <Border Grid.Column="1" Grid.Row="0">
                            <StackPanel  Orientation="Horizontal" HorizontalAlignment="Left">
                                <Border Style="{DynamicResource FormFieldContainer}">
                                    <CheckBox Name="CallFlagCheckBox" Style="{DynamicResource FormCheckBox}"/>
                                </Border>
                                <Border Style="{DynamicResource FormLabelContainer}">
                                    <Label Content="Контакт для созвона" HorizontalAlignment="Left" Style="{DynamicResource FormLabel}"/>
                                </Border>
                            </StackPanel>
                        </Border>
                    </Grid>
                    
                    <Border Grid.Column="0" Grid.Row="4">
                        <Label Content="Примечание :"  Style="{DynamicResource FormLabel}" HorizontalAlignment="Right" VerticalAlignment="Top"/>
                    </Border>
                    <Border Grid.Column="1" Grid.Row="4" Style="{DynamicResource FormFieldContainer}">
                        <TextBox Name="PokupatelNote" HorizontalAlignment="Stretch" Height="40"  TextWrapping="Wrap" Style="{DynamicResource FormField}"/>
                    </Border>

                    <TextBox Name="PokupatelPhone" Visibility="Collapsed"/>

                </Grid>
            </Border>

        </Grid>
    </Border>


</controls:ControlBase>
