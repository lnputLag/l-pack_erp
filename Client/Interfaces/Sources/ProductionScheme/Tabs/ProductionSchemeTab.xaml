<controls:ControlBase  x:Class="Client.Interfaces.Sources.ProductionSchemeTab"
        xmlns:local="clr-namespace:Client.Interfaces.Sources"
        xmlns:controls="clr-namespace:Client.Interfaces.Main"
                       
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                       
        mc:Ignorable="d"
        d:DesignHeight="800" d:DesignWidth="1200"
        Style="{DynamicResource UserControlCanvas}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Name ="FormToolbar" Style="{DynamicResource DataGridContainer}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="2.5*"/>
            <RowDefinition Height="4*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{DynamicResource DataGridMainHolder}" >
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Grid.Column="0" Name="GoodsGridToolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left" Margin="0,5,0,5">
                        <Label Content="Товар"  Style="{DynamicResource TopLabel}" Margin="0 0 10 0"/>
                        <Label Content="Артикул/наименование:"  Style="{DynamicResource TopLabel}" />
                        <TextBox Name="GoodsGridSearch" Style="{DynamicResource FormFieldTopPanel}" MinWidth="120" MaxWidth="180"  TextWrapping="NoWrap"  Text="" TextChanged="GoodsGridSearchTextChanged"/>
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                        <Button Name="GoodsGridSearchButton"      Content="Показать"     Style="{DynamicResource Button}" Click="GoodsGridSearchButtonClick"/>
                        <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                    </StackPanel>
                    <StackPanel Grid.Row="0" Grid.Column="1" Style="{DynamicResource FormHeaderButtonsPanel}"  HorizontalAlignment="Right" >
                        <Button Name="HelpButton" ToolTip="Документация" Width="24" MinWidth="24"  Content="?"  Style="{DynamicResource Button}" Click="HelpButtonClick"  Margin="0,0,0,0" />
                    </StackPanel>
                </Grid>

                <Border Grid.Row="1" Style="{DynamicResource DataGridMainHolder}" Height="Auto">
                    <controls:GridBox4 x:Name="GoodsGrid"/>
                </Border>

            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{DynamicResource DataGridMainHolder}" Margin="0,5,0,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Style="{DynamicResource GridControlPanel}" Height="24" Margin="0,5,0,5" VerticalAlignment="Top">
                    <StackPanel Name="ProductionSchemeToolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left">
                        <Label Content="Схемы производства"  Style="{DynamicResource TopLabel}" Margin="0 0 10 0"/>
                        <Button Name="CreateProductionSchemeButton"      Content="Добавить"  Style="{DynamicResource Button}" />
                        <Button Name="EditProductionSchemeButton"        Content="Изменить"    Style="{DynamicResource Button}" />
                        <Button Name="DeleteProductionSchemeButton"      Content="Удалить"     Style="{DynamicResource Button}" />
                        <Button Name="CreateAlternativeProductionSchemeButton"      Content="Создать альт. схемы для добивочной заготовки"     Style="{DynamicResource Button}" />
                    </StackPanel>
                </Grid>
                <Border Grid.Row="1" Style="{DynamicResource DataGridMainHolder}">
                    <controls:GridBox4 x:Name="ProductionSchemeGrid" />
                </Border>

            </Grid>
        </Border>


    </Grid>

</controls:ControlBase>
