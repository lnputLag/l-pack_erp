<UserControl 
    x:Class="Client.Interfaces.Service.PhotoScreen"
    xmlns:local="clr-namespace:Client.Interfaces.Service"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"         
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"    
    xmlns:s="clr-namespace:System;assembly=mscorlib"

    mc:Ignorable="d" 
    d:DesignWidth="900" d:DesignHeight="600" 
    Style="{DynamicResource UserControlCanvas}"    
>

    <UserControl.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>


    <Border Style="{DynamicResource DataGridContainerHolder}"  >
        <Grid Style="{DynamicResource DataGridContainer}" Margin="0,0,0,5">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="11*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"  Grid.Column="0" Grid.ColumnSpan="3" Style="{DynamicResource GridControlPanel}">
                <Border >
                    <Grid >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="11.5*"/>
                            <ColumnDefinition Width="0.5*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Name="GridToolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left"  >

                            <Label Content="С:"  Style="{DynamicResource TopLabel}"/>
                            <Grid Style="{DynamicResource FormFieldDateHolder}">
                                <!--<TextBox Name="FromDate"  MaxLength="19" Width="140" Style="{DynamicResource FormFieldDate}" IsReadOnly="False" TextChanged="FromDate_TextChanged" />
                                <DatePicker  SelectedDate="{Binding Path=Text, Mode=OneWayToSource, ElementName=FromDate, Converter={StaticResource DateDateTime} }"  Style="{DynamicResource FormFieldDP}"  Margin="0,1,0,0">
                                </DatePicker>-->

                                <controls:DateTimePicker x:Name="FromDate" Style="{DynamicResource DateTimeEditStyle}" TextChanged="FromDate_TextChanged">
                                </controls:DateTimePicker>
                            </Grid>

                            <Label Content="По:"  Style="{DynamicResource TopLabel}"/>
                            <Grid Style="{DynamicResource FormFieldDateHolder}">
                                <!--<TextBox Name="ToDate" MaxLength="19" Width="140"  Style="{DynamicResource FormFieldDate}" IsReadOnly="False" TextChanged="ToDate_TextChanged" IsEnabled="True" />
                                <DatePicker  SelectedDate="{Binding Path=Text, Mode=OneWayToSource, ElementName=ToDate, Converter={StaticResource DateDateTime} }"  Style="{DynamicResource FormFieldDP}"  Margin="0,1,0,0">
                                </DatePicker>-->

                                <controls:DateTimePicker x:Name="ToDate" Style="{DynamicResource DateTimeEditStyle}" TextChanged="ToDate_TextChanged">
                                </controls:DateTimePicker>
                            </Grid>

                            <Button Name="RefreshButton"    Content="Показать"  Style="{DynamicResource Button}" Width="70" Click="RefreshButton_Click" VerticalAlignment="Bottom"   />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                            <Label Content="Поиск:"  Style="{DynamicResource TopLabel}" />
                            <TextBox Style="{DynamicResource FormFieldTopPanel}" Width="60" TextWrapping="NoWrap" Name="SearchText" Text=""  />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                            <Button Name="ExportButton" Click="ExportButton_Click_1"  Content="Экспорт" ToolTip="Сохранить в файле" Style="{DynamicResource Button}" Width="55"  VerticalAlignment="Bottom"   />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="1" Style="{DynamicResource FormHeaderButtonsPanel}"  HorizontalAlignment="Right" >
                            <Button Name="HelpButton"  Width="24" MinWidth="24"  Content="?" Margin="0" Style="{DynamicResource Button}" Click="HelpButton_Click" />
                        </StackPanel>

                    </Grid>
                </Border>
            </Grid>

            <Grid Grid.Row="1"  Grid.Column="0" >
                <Border Style="{DynamicResource DataGridMainHolder}" >
                    <controls:GridBox x:Name="ScreenShotGrid" />
                </Border>
            </Grid>

            <Grid Grid.Row="1"  Grid.Column="2" >
                <Border Style="{DynamicResource DataGridMainHolder}" >
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="29"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="29"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                            <RowDefinition Height="*"></RowDefinition>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0"  Grid.Column="1"  Grid.ColumnSpan="3">
                            <Border Height="29">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Name="ScreenshotImageToolbar">
                                    <Button Name="ZoomInButton"  Width="24" MinWidth="24"  Content="+" FontSize="18" Padding="0,-2,0,0" Style="{DynamicResource Button}" Click="ZoomInButton_Click" />
                                    <Button Name="ZoomOutButton"  Width="24" MinWidth="24"  Content="−" FontSize="18" Padding="0,-2,0,0"  Style="{DynamicResource Button}" Click="ZoomOutButton_Click" />
                                    <Button Name="ZoomInfButton"  Width="35" MinWidth="35"  Content="1:1"   Style="{DynamicResource Button}" Click="ZoomInfButton_Click" />
                                </StackPanel>
                            </Border>
                        </Grid>
                        <Grid Grid.Row="1"  Grid.Column="0" >
                            <Border>
                                <Button Name="ArrowLeftButton" Height="100"  Margin="0" ToolTip="Предыдущий скриншот" HorizontalAlignment="Left"  Style="{DynamicResource ButtonGlyph}" Click="ArrowLeftButton_Click"   >
                                    <Image Style="{DynamicResource ArrowLeft2Image}" />
                                </Button>
                            </Border>                            
                        </Grid>
                        <Grid Grid.Row="1"  Grid.Column="1" >
                            <Border  Name="ScreenshotImageContainer" Background="#ffffffff" BorderBrush="#ffcccccc" BorderThickness="1" >
                                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                                    <Image Name="ScreenshotImage" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0,0,0,0"  />
                                </ScrollViewer>                                
                            </Border>
                        </Grid>
                        <Grid Grid.Row="1"  Grid.Column="2" >
                            <Border>
                                <Button Name="ArrowRightButton" Height="100"  Margin="0" ToolTip="Следующий скриншот"  HorizontalAlignment="Right" Style="{DynamicResource ButtonGlyph}" Click="ArrowRightButton_Click"  >
                                    <Image Style="{DynamicResource ArrowRight2Image}" />
                                </Button>
                            </Border>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>

        </Grid>
    </Border>

</UserControl>
