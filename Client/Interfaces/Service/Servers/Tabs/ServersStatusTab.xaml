<controls:ControlBase 
    x:Class="Client.Interfaces.Service.Servers.ServersStatusTab"
    xmlns:local="clr-namespace:Client.Interfaces.Service.Servers"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"         

    mc:Ignorable="d" 
    d:DesignWidth="1380" d:DesignHeight="200" 
    Style="{DynamicResource UserControlCanvas}"    
>

    <UserControl.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{DynamicResource DataGridContainerHolder}"  >
        <Grid Style="{DynamicResource DataGridContainer}">
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="0" />
                <ColumnDefinition Width="0" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>                
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Grid.Column="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0"  Grid.Column="0" Style="{DynamicResource GridControlPanel}">
                    <Border >
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="11*"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"></RowDefinition>
                            </Grid.RowDefinitions>
                            <StackPanel Grid.Row="0" Grid.Column="0" Name="ServerGridToolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left" Width="770" >
                                <TextBlock Height="24" Padding="0,4,0,0">Истансы</TextBlock>
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                                <Button Name="ServerGridRefreshButton"    Content="Обновить"  Style="{DynamicResource Button}"  />
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                                <Button Name="ServerCommandButton" Content="Команда" MinWidth="45"  Style="{DynamicResource Button}"  />
                                <Button Name="ServerStatusButton" Content="Статус" MinWidth="45"  Style="{DynamicResource Button}"  />
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                                <Label Content="Поиск:"  Style="{DynamicResource TopLabel}" />
                                <TextBox Name="ServerGridSearch" Style="{DynamicResource FormFieldTopPanel}" MinWidth="120" MaxWidth="180"  TextWrapping="NoWrap"  Text=""  />
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                            </StackPanel>
                            <StackPanel Grid.Row="0" Grid.Column="1" Style="{DynamicResource FormHeaderButtonsPanel}"  HorizontalAlignment="Right" Width="24" >
                                
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
                <Grid Grid.Row="1"  Grid.Column="0" >
                    <Border Style="{DynamicResource DataGridMainHolder}">
                        <controls:GridBox4 x:Name="ServerGrid" />
                    </Border>
                </Grid>
            </Grid>

            <Grid Grid.Row="0" Grid.Column="2">
                

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0"  Grid.Column="0"  Style="{DynamicResource GridControlPanel}">
                    <Border >
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="11*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"></RowDefinition>
                                <RowDefinition Height="Auto"></RowDefinition>
                            </Grid.RowDefinitions>
                            
                            <StackPanel Grid.Row="0" Grid.Column="1" Style="{DynamicResource FormHeaderButtonsPanel}"  HorizontalAlignment="Right" Width="24" >
                                <Button Name="HelpButton" Tag="help" Width="24" MinWidth="24"  Content="?"  Style="{DynamicResource Button}" Margin="0"  />
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
                <Grid Grid.Row="1"  Grid.Column="0" Visibility="Collapsed">
                    <Border Style="{DynamicResource DataGridMainHolder}">

                        <StackPanel Orientation="Vertical" Name="DebugPanel">
                            <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,0,5" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left" Width="770" >
                                <TextBlock Height="24" Width="40" Padding="0,4,0,0">UPD</TextBlock>
                                <TextBox Name="UpdInterval" Height="24" Width="40" Padding="0,4,0,0" Margin="5,0,5,0">300</TextBox>
                                <Button Name="UpdRun"  Click="UpdRun_Click" Content="1"  Style="{DynamicResource Button}" MinWidth="24" />
                                <Button Name="UpdStop" Click="UpdStop_Click" Content="0"  Style="{DynamicResource Button}" MinWidth="24" />
                            </StackPanel>
                            <StackPanel Grid.Row="1" Grid.Column="0" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left" Width="770" >
                                <TextBlock Height="24" Width="40" Padding="0,4,0,0">LOAD</TextBlock>
                                <TextBox Name="LoadInterval" Height="24" Width="40" Padding="0,4,0,0" Margin="5,0,5,0">1000</TextBox>
                                <Button Name="LoadRun"  Click="LoadRun_Click" Content="1"  Style="{DynamicResource Button}" MinWidth="24" />
                                <Button Name="LoadStop" Click="LoadStop_Click" Content="0"  Style="{DynamicResource Button}" MinWidth="24" />
                            </StackPanel>
                            <TextBox Name="Log" Text="sdfasdf " HorizontalAlignment="Stretch" Foreground="#ff555555" VerticalAlignment="Stretch" FontFamily="consolas" FontSize="11" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Visible" TextWrapping="Wrap" />
                        </StackPanel>

                        
                    </Border>
                </Grid>
            </Grid>
            

        </Grid>
    </Border>

</controls:ControlBase>
