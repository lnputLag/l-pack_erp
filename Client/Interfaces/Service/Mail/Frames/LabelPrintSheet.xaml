<UserControl 
    x:Class="Client.Interfaces.Service.Mail.LabelPrintSheet"
    xmlns:local="clr-namespace:Client.Interfaces.Service.Mail"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"
        
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"     
    mc:Ignorable="d"   
    Width="27cm" Height="16cm"
    Style="{DynamicResource UserControlCanvasStatic}"          
>
    <!--
    Width="1024" Height="600"
    Width="21cm" Height="29cm"
    -->
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{DynamicResource DataGridContainerHolder}"  >
        <Grid Style="{DynamicResource DataGridContainer}">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="10*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Style="{DynamicResource GridControlPanel}">
                <Border >
                    <Grid HorizontalAlignment="Left" >

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Name="GridToolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left" Width="770" >
                            
                            <Button Tag="Close" Content="Закрыть" Style="{DynamicResource Button}" Click="GridToolbarButtonOnClick" />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                            <Button Tag="Print" Content="Печать" Style="{DynamicResource FButtonPrimary}" Click="GridToolbarButtonOnClick" />
                            <Button Tag="Setup" Content="Настройка" Style="{DynamicResource Button}" Click="GridToolbarButtonOnClick"/>
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                        </StackPanel>

                    </Grid>
                </Border>
            </Grid>

            <!--
            <Grid Grid.Row="1"  Grid.Column="0" Style="{DynamicResource GridControlPanel}">
                <Border >
                    <Grid HorizontalAlignment="Stretch" >

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="79"/>
                            <ColumnDefinition Width="11*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left"  >
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}" Margin="10,0,0,0"></Border>
                            <TextBlock  Name="FormStatus" Text="Страницы:"  TextAlignment="Left" Style="{DynamicResource FNoteBar2}" HorizontalAlignment="Stretch" />
                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="1" Name="PagesToolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left"  >

                            <Button Tag="1" Content="1" Style="{DynamicResource ButtonSmall}" Click="PagesToolbarButtonOnClick"  />
                            <Button Tag="2" Content="2" Style="{DynamicResource ButtonSmall}" Click="PagesToolbarButtonOnClick"  />
                            <Button Tag="3" Content="3" Style="{DynamicResource ButtonSmall}" Click="PagesToolbarButtonOnClick"  />

                        </StackPanel>

                    </Grid>
                </Border>
            </Grid>
            -->

            <Border Grid.Row="1"  Grid.Column="0" Style="{DynamicResource DataGridMainHolder}" >
                <controls:GridBox x:Name="Grid" />
            </Border>

            <Grid Grid.Row="1"  Grid.Column="2" >
                <Border Style="{DynamicResource DataGridMainHolder}" BorderBrush="#ff000000" BorderThickness="0" Padding="0" Margin="0,0,0,0" Background="#99F0F0F0">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                        
                        <Border x:Name="PrintCanvas" Style="{DynamicResource DataGridMainHolder}" Background="#ffffffff" BorderBrush="#ffcccccc" Width="21cm" Height="29.7cm" BorderThickness="1" Padding="0,0,0,0" Margin="0" HorizontalAlignment="Left">
                            <Grid x:Name="LabelContainer" VerticalAlignment="Top"  HorizontalAlignment="Stretch" Background="#ffffffff" >

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1*" />
                                    <ColumnDefinition Width="1*" />
                                    <ColumnDefinition Width="1*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"></RowDefinition>
                                    <RowDefinition Height="Auto"></RowDefinition>
                                </Grid.RowDefinitions>

                                <Grid Grid.Row="0"  Grid.Column="0">
                                    <local:LabelBlock ></local:LabelBlock>
                                </Grid>
                                <Grid Grid.Row="0"  Grid.Column="1">
                                    <local:LabelBlock ></local:LabelBlock>
                                </Grid>
                                <Grid Grid.Row="0"  Grid.Column="2">
                                    <local:LabelBlock ></local:LabelBlock>
                                </Grid>

                                <Grid Grid.Row="1"  Grid.Column="0">
                                    <local:LabelBlock ></local:LabelBlock>
                                </Grid>
                                <Grid Grid.Row="1"  Grid.Column="1">
                                    <local:LabelBlock ></local:LabelBlock>
                                </Grid>
                                <Grid Grid.Row="1"  Grid.Column="2">
                                    <local:LabelBlock ></local:LabelBlock>
                                </Grid>

                            </Grid>
                        </Border>
                        
                    </ScrollViewer>
                </Border>
            </Grid>


        </Grid>
    </Border>

</UserControl>
