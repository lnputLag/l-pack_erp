<Window
        x:Class="Client.Interfaces.Service.Printing.PrintingPreview"
        xmlns:local="clr-namespace:Client.Interfaces.Service.Printing"
        xmlns:controls="clr-namespace:Client.Interfaces.Main"
    
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"            
        mc:Ignorable="d"
    
        d:DesignWidth="1200" d:DesignHeight="700" 
        Width="1200" Height="700"
        HorizontalAlignment="Left" VerticalAlignment="Top" 
    >

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <!--
        форма
        1 строка: блок кнопок, 30 пикс.
        2 строка: горизонтальный разделитель
        3 строка: блок с полями формы, 
    
    -->
    <Border Style="{DynamicResource FormContainerHolder}" Margin="1" >
        <Grid Style="{DynamicResource FormContainer}" >

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"  Grid.Column="0"  Style="{DynamicResource FormControlPanel}" >
                <Grid ShowGridLines="false">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition  />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="1" Grid.Column="0" BorderBrush="#ff00cccc" BorderThickness="0" >
                        <StackPanel Grid.Row="1" Grid.Column="0" Style="{DynamicResource FormHeaderButtonsPanel}" Name="FormToolbar" >
                            <Button Content="Печатать"    Name="PrintButton" Tag="print" Click="ButtonOnClick" Style="{DynamicResource Button}" />
                            <Button Content="Сохранить"    Name="SaveDocumentButton" Tag="save_document" Click="ButtonOnClick"  Style="{DynamicResource Button}" />
                            <Button Content="Закрыть"    Name="CancelButton" Tag="cancel" Click="ButtonOnClick" Style="{DynamicResource Button}" />
                        </StackPanel>
                    </Border>

                </Grid>
            </Grid>

            <Grid Grid.Row="0"  Grid.Column="1"  Style="{DynamicResource FormControlPanel}" >
                <Grid ShowGridLines="false">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition  />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="1" Grid.Column="0" BorderBrush="#ff00cccc" BorderThickness="0" >
                        <StackPanel Grid.Row="1" Grid.Column="0" Style="{DynamicResource FormHeaderButtonsPanel}"  >
                            <Button Content="-"     Tag="zoom_dec"  Click="ButtonOnClick" Style="{DynamicResource Button}" MinWidth="25"/>
                            <Button Content="100%"  Tag="zoom_rst" Click="ButtonOnClick" Style="{DynamicResource Button}" MinWidth="45"/>
                            <Button Content="+"     Tag="zoom_inc"  Click="ButtonOnClick" Style="{DynamicResource Button}" MinWidth="25"/>
                        </StackPanel>
                    </Border>

                </Grid>
            </Grid>

            <Border Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Style="{DynamicResource FormVerticalPanelSpacer}"></Border>

            <Border Grid.Row="2" Grid.Column="0" >

                <Grid ShowGridLines="false" Grid.Row="0"  Grid.Column="0" Style="{DynamicResource FromGrid}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="3.2*"/>
                        <ColumnDefinition Width="9*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                    </Grid.RowDefinitions>


                    <Border Grid.Row="0" Grid.Column="0" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="Профиль: *" Style="{DynamicResource FormLabel}"  />
                    </Border>
                    <Border Grid.Row="0"  Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                        <controls:SelectBox  x:Name="ProfileSelect" Style="{DynamicResource CustomFormField}" Width="200" Autocomplete="False" HorizontalAlignment="Left" />
                    </Border>

                    <Border Grid.Row="1" Grid.Column="0"  Grid.ColumnSpan="2" Style="{DynamicResource FormLabelContainer}">
                        
                    </Border>

                    <Border Grid.Row="2" Grid.Column="0" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="Имя: *" Style="{DynamicResource FormLabel}"  />
                    </Border>
                    <Border Grid.Row="2"  Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                        <TextBox  Name="Name" MaxLength="64" Text="label_printer" TextWrapping="NoWrap" Style="{DynamicResource FormField}" HorizontalAlignment="Left" Width="200"  />
                    </Border>

                    <Border Grid.Row="3" Grid.Column="0" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="Принтер: *" Style="{DynamicResource FormLabel}"  />
                    </Border>
                    <Border Grid.Row="3"  Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                        <StackPanel Orientation="Horizontal">
                            <TextBox  Name="PrinterName" MaxLength="64" Text="label_printer" TextWrapping="NoWrap" Style="{DynamicResource FormField}" HorizontalAlignment="Left" Width="177"  />
                            <Button Click="PrinterNameOnClick"  ToolTip="Выбрать"   Style="{DynamicResource ButtonGlyph}"  Margin="-1,0,0,0"  >
                                <Image Style="{DynamicResource SelectImage}" />
                            </Button>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="4" Grid.Column="0" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="Описание:  "    Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Row="4"  Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                        <TextBox  Name="Description"  MaxLength="512" TextWrapping="WrapWithOverflow" Style="{DynamicResource FormField}" HorizontalAlignment="Left" Width="200" Height="45" />
                    </Border>

                    <Border Grid.Row="5" Grid.Column="0" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="Копии: *" Style="{DynamicResource FormLabel}"  />
                    </Border>
                    <Border Grid.Row="5"  Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                        <TextBox  Name="Copies" MaxLength="64" Text="1" TextWrapping="NoWrap" Style="{DynamicResource FormField}" HorizontalAlignment="Left" Width="50"  />
                    </Border>

                    <Border Grid.Row="6" Grid.Column="0" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="Бумага: *" Style="{DynamicResource FormLabel}"  />
                    </Border>
                    <Border Grid.Row="6"  Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                        <StackPanel Orientation="Horizontal">
                            <TextBox  Name="Width" MaxLength="64" Text="210" TextWrapping="NoWrap" Style="{DynamicResource FormField}" HorizontalAlignment="Left" Width="50"  />
                            <TextBlock Margin="5,4,5,0">x</TextBlock>
                            <TextBox  Name="Height" MaxLength="64" Text="297" TextWrapping="NoWrap" Style="{DynamicResource FormField}" HorizontalAlignment="Left" Width="50"  />
                            <TextBlock Margin="5,4,5,0">мм</TextBlock>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="2" Style="{DynamicResource FormLabelContainer}" Margin="5,0,5,0">
                        <TextBlock  Name="Status" Text="Не все обязательные поля заполнены верно"  TextAlignment="Left" Style="{DynamicResource FStatusBar2}" HorizontalAlignment="Stretch" />
                    </Border>

                    <!--
                    <Border Grid.Row="8"  Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                        <Button Content="Сохранить"  Tag="save" Click="ButtonOnClick" Style="{DynamicResource Button}" HorizontalAlignment="Right" />
                    </Border>
                    -->

                    <Border Grid.Row="8"  Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                        <!--
                        <Button Content="border"  Tag="border" Click="ButtonOnClick" Style="{DynamicResource Button}" HorizontalAlignment="Right" />
                        -->
                    </Border>

                </Grid>

            </Border>

            <Border Grid.Row="2" Grid.Column="1" >
                <ScrollViewer Name="PrintPreviewContainer">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" Grid.Column="0" Panel.ZIndex="1">
                            <Border Name="PrintPreviewCanvas" MinWidth="400" MinHeight="570" BorderBrush="#ffcccccc" BorderThickness="1" >
                                <Border Name="PrintPreviewBody" MinWidth="100" MinHeight="570" BorderBrush="#ffcccccc" BorderThickness="1" Margin="10" HorizontalAlignment="Left" VerticalAlignment="Top">

                                </Border>
                            </Border>
                        </Grid>
                        <Grid Grid.Row="0" Grid.Column="0" Panel.ZIndex="10">
                            <Border Name="PrintPreviewCanvas0" MinWidth="400" MinHeight="570" BorderBrush="#ffcccccc" BorderThickness="1" >
                                <Border Name="PrintPreviewBody0" MinWidth="100" MinHeight="570" BorderBrush="#ffcc0000" BorderThickness="1" Margin="10" HorizontalAlignment="Left" VerticalAlignment="Top">

                                </Border>
                            </Border>
                        </Grid>
                    </Grid>                    
                </ScrollViewer>
            </Border>

            

        </Grid>
    </Border>

</Window>
