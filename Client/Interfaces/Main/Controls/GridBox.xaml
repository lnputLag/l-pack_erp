<UserControl
    x:Class="Client.Interfaces.Main.GridBox"
    xmlns:local="clr-namespace:Client.Interfaces.Main"

    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
    xmlns:gif="http://wpfanimatedgif.codeplex.com"

    mc:Ignorable="d"
    d:DesignWidth="400"
    d:DesignHeight="200"
    Style="{DynamicResource UserControlCanvas}"
>

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Grid.RowSpan="2" Grid.Column="0" Name="Splash" Visibility="Collapsed" Panel.ZIndex="20">
            <Border Background="#55ffffff" >
            </Border>
        </Grid>
        <Grid Grid.Row="0" Grid.Column="0" x:Name="GridMultilineHeaderArea" Visibility="Collapsed" Height="23" Panel.ZIndex="8">
            <Border Style="{DynamicResource MainGridMultiColumnContainerOuter}" >
                <ScrollViewer x:Name="GridMultilineHeaderScroll" Style="{DynamicResource MainGridMultiColumnContainerScroll}" HorizontalScrollBarVisibility="Hidden">
                    <StackPanel Orientation="Horizontal"  Style="{DynamicResource MainGridMultiColumnContainerHolder}"  >
                        <Grid >
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"  />
                                <ColumnDefinition Width="Auto"  />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"></RowDefinition>
                            </Grid.RowDefinitions>
                            <Border Grid.Row="0" Grid.Column="0" Name="MainGridMultiColumnHeaderCorner" Style="{DynamicResource MainGridMultiColumnHeaderCorner}" >
                            </Border>
                            <Border Grid.Row="0" Grid.Column="1" x:Name="GridMultilineHeader"  Margin="0" Padding="0" Style="{DynamicResource MainGridMultiColumnContainer}"></Border>
                        </Grid>

                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
        <Grid Grid.Row="1" Grid.Column="0" Name="GridHolder" Visibility="Visible" Margin="0,0,0,0">
            <Border Margin="0,0,0,0" BorderThickness="1,1,1,1" BorderBrush="#FFcccccc" RenderOptions.ClearTypeHint="Enabled"  >
                <DataGrid  RenderOptions.EdgeMode="Unspecified" Name="Grid" ScrollViewer.ScrollChanged="Scroller_ScrollChanged" MouseDoubleClick="Grid_MouseDoubleClick" Style="{DynamicResource DataGridMain}"  HorizontalScrollBarVisibility="Auto" >
                </DataGrid>
            </Border>
        </Grid>
        <Grid Grid.Row="2" Grid.Column="0"  x:Name="GridTotalsArea" Visibility="Collapsed" Margin="0,0,0,0">
            <Border BorderThickness="1,0,1,1" BorderBrush="#FFcccccc" RenderOptions.ClearTypeHint="Enabled"  RenderOptions.EdgeMode="Aliased"  >
                <StackPanel Orientation="Horizontal" Margin="0,0,0,0" Style="{DynamicResource MainGridTotalsColumnContainerHolder}" >
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"  />
                            <ColumnDefinition Width="Auto"  />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"></RowDefinition>
                        </Grid.RowDefinitions>
                    </Grid>
                    <Border Grid.Row="0" Grid.Column="0" Style="{DynamicResource MainGridTotalsColumnHeaderCorner}" BorderThickness="0,0,0,0" BorderBrush="#FFcccccc" />
                    <Border Grid.Row="0" Grid.Column="1" x:Name="GridTotals" Style="{DynamicResource MainGridTotalsColumnContainer}" BorderThickness="1,0,0,0" BorderBrush="#FFcccccc" Background="#ffffffff">

                    </Border>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>

</UserControl>
