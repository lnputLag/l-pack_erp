<UserControl 
    x:Class="Client.Interfaces.Main.GraphBox"             
    xmlns:local="clr-namespace:Client.Interfaces.Main"    
             
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"     
    xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"    
    xmlns:gif="http://wpfanimatedgif.codeplex.com"
    
    mc:Ignorable="d" 
    d:DesignWidth="400"    
    d:DesignHeight="200"    
    MinWidth="200"
    MinHeight="100"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch"
    Style="{DynamicResource UserControlCanvas}"         
>

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid ShowGridLines="False" PreviewMouseWheel="Grid_PreviewMouseWheel" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="20" />
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Grid.RowSpan="3"  Name="Splash" Visibility="Collapsed" Panel.ZIndex="20">
            <Border Background="#55ffffff" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            </Border>
        </Grid>

        <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Grid.RowSpan="2" Name="CanvasCell" Panel.ZIndex="10" SizeChanged="Grid_SizeChanged">
            <ScrollViewer  x:Name="CanvasScroll" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden">
                <Border Name="CanvasBorder" HorizontalAlignment="Left" VerticalAlignment="Stretch" BorderThickness="0" BorderBrush="#ff990000" RenderOptions.ClearTypeHint="Auto" RenderOptions.EdgeMode="Unspecified">
                    <Grid Name="Canvas" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" RenderOptions.ClearTypeHint="Auto" RenderOptions.EdgeMode="Unspecified" Width="20" >
                    </Grid>
                </Border>
            </ScrollViewer>
            <Grid.ContextMenu>
                <ContextMenu>
                    <MenuItem  Name="RefreshButton"         Header="Обновить"   Click="RefreshButton_Click"  ></MenuItem>
                    <Separator Name="DebugButtonSeparator"></Separator>
                    <MenuItem  Name="DebugButton" Header="Отладка">
                        <MenuItem  Name="ClearButton"            Header="clear"             Click="ClearButton_Click_1" />
                        <MenuItem  Name="RendeButtonr"           Header="render"            Click="Render_Click" />
                        <MenuItem  Name="RenderContainerButton"  Header="render prepare"    Click="RenderContainerButton_Click" />
                        <MenuItem  Name="RenderAxisButton"       Header="render axis"       Click="RenderAxisButton_Click" />
                        <MenuItem  Name="RenderGridButton"       Header="render grid"       Click="RenderGridButton_Click" />
                        <MenuItem  Name="RenderDataButton"       Header="render data"       Click="RenderDataButton_Click" />
                        <MenuItem  Name="RenderScrollsButton"    Header="render scroll"     Click="RenderScrollsButton_Click" />
                        <MenuItem  Name="LogsButton"             Header="show log"          Click="LogsButton_Click" />
                        <MenuItem  Name="Logs2Button"            Header="clear log"         Click="Logs2Button_Click" />
                        <MenuItem  Name="ExportDSButton"         Header="export ds"         Click="ExportDSButton_Click" />
                    </MenuItem>
                </ContextMenu>
            </Grid.ContextMenu>
        </Grid>

        <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Grid.RowSpan="2" Name="Canvas2Cell" Panel.ZIndex="1">
            <ScrollViewer  x:Name="Canvas2Scroll" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden">
                <Border Name="Canvas2Border" HorizontalAlignment="Left" VerticalAlignment="Stretch" BorderThickness="0" BorderBrush="#ff009900" RenderOptions.ClearTypeHint="Auto" RenderOptions.EdgeMode="Unspecified" >
                    <Canvas Name="Canvas2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" RenderOptions.ClearTypeHint="Auto" RenderOptions.EdgeMode="Unspecified" Width="20" >
                    </Canvas>
                </Border>
            </ScrollViewer>
        </Grid>

        <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Grid.RowSpan="2" Name="Canvas3Cell" Panel.ZIndex="20" Visibility="Collapsed">
            <ScrollViewer  x:Name="Canvas3Scroll" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden">
                <Border Name="Canvas3Border" HorizontalAlignment="Left" VerticalAlignment="Stretch" BorderThickness="0" BorderBrush="#ff990000" RenderOptions.ClearTypeHint="Auto" RenderOptions.EdgeMode="Unspecified">
                    <Grid Name="Canvas3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" RenderOptions.ClearTypeHint="Auto" RenderOptions.EdgeMode="Unspecified" Width="20" >
                    </Grid>
                </Border>
            </ScrollViewer>
        </Grid>

        <Grid Grid.Row="0" Grid.Column="0" Name="Canvas4Cell" Panel.ZIndex="15" Visibility="Visible">
            <ScrollViewer  x:Name="Canvas4Scroll" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden">
                <Border Name="Canvas4Border" HorizontalAlignment="Left" VerticalAlignment="Stretch" Background="#ffffff" BorderThickness="0" BorderBrush="#ff990000" RenderOptions.ClearTypeHint="Auto" RenderOptions.EdgeMode="Unspecified">
                    <Grid Name="Canvas4" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" RenderOptions.ClearTypeHint="Auto" RenderOptions.EdgeMode="Unspecified" Width="20" >
                    </Grid>
                </Border>
            </ScrollViewer>
        </Grid>

        <Grid Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Name="Canvas5Cell" Panel.ZIndex="12" Visibility="Visible" >
            <ScrollViewer  x:Name="Canvas5Scroll" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled" >
                <Border Name="Canvas5Border" HorizontalAlignment="Left" VerticalAlignment="Top" Height="22"  BorderThickness="0" BorderBrush="#ff990000" RenderOptions.ClearTypeHint="Auto" RenderOptions.EdgeMode="Unspecified">
                    <Grid Name="Canvas5"  HorizontalAlignment="Stretch" VerticalAlignment="Top" Height="22" RenderOptions.ClearTypeHint="Auto" RenderOptions.EdgeMode="Unspecified" Width="20" >
                    </Grid>
                </Border>
            </ScrollViewer>
        </Grid>

        <Grid Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" Visibility="Visible" >
            <ScrollViewer  HorizontalScrollBarVisibility="Visible"  x:Name="GraphHorizontalScroll" ScrollChanged="GraphHorizontalScroll_ScrollChanged" Style="{DynamicResource SHMonContainerHScroll}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
                <Border Name="GraphHorizontalScrollSpacer" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" BorderThickness="0" BorderBrush="#ff009900"></Border>
            </ScrollViewer>
        </Grid>

    </Grid>
</UserControl>
