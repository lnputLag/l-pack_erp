<UserControl x:Class="Client.Interfaces.Main.CloseableTabHeader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"        
    Padding="0,0,0,0" Margin="0,0,0,0"    
             
>
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="0,0,0,0"  HorizontalAlignment="Stretch" ShowGridLines="False" >
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />            
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Grid.Column="0" Name="ContainerIcon" Visibility="Collapsed" Width="23">
            <ContentControl ToolTip="Назад к списку" Grid.Row="0" Grid.Column="0" Name="IconHome" Style="{DynamicResource MenuTabGlyphIcon}" Margin="0,-2,0,0" Content="{StaticResource MenuTabIcon}" HorizontalAlignment="left" />
        </Border>

        <Border Grid.Row="0" Grid.Column="1" Padding="6,0,0,0" Name="ContainerTitle" >
            <Label Content="Вкладка" HorizontalAlignment="Left" Margin="0,-1,0,0" Padding="0,0,0,0" Name="LabelTabTitle" VerticalAlignment="Center"  />
        </Border>

        <Border Grid.Row="0" Grid.Column="2" Name="ContainerCloseButton" >
            <Border Name="ContainerInnerCloseButton" Width="20">
                <Button  Name="CloseButton" BorderBrush="#00efefef" Background="Transparent" BorderThickness="0" Margin="0,0,0,0" Padding="0,0,0,0" Width="20"  Visibility="Visible" ToolTip="Закрыть" HorizontalAlignment="Center">
                    <ContentControl Width="20" Height="20" Margin="0" Padding="0" HorizontalAlignment="Left" Style="{DynamicResource MenuTabGlyphIcon}" Content="{StaticResource CloseTabIcon}" />
                </Button>
            </Border>
            
        </Border>        
       
    </Grid>

</UserControl>
