<UserControl 
    x:Class="Client.Interfaces.Main.SelectBox"             
    xmlns:local="clr-namespace:Client.Interfaces.Main"    
             
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"     
    mc:Ignorable="d" 
    d:DesignWidth="200"    
    Style="{DynamicResource UserControlCanvas}"         
>

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/SelectBox.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Style="{DynamicResource TopContainer}"  >
                    
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"  />
            <ColumnDefinition Width="23" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" Grid.Column="0" Name="ValueTextBox" Style="{DynamicResource ValueTextBox}" LostFocus="ValueTextBox_LostFocus" />

        <Button Grid.Row="0" Grid.Column="1" Name="ShowDropdownButton" Style="{DynamicResource ShowDropdownButton}" Width="22" Height="22" Margin="1,-1,0,0" RenderOptions.EdgeMode="Aliased" SnapsToDevicePixels="True">
            <Image Style="{DynamicResource ChevronDownImage}" Margin="0,0,0,0" Width="22" Height="22" RenderOptions.EdgeMode="Aliased" SnapsToDevicePixels="True" />
        </Button>

        <Popup Name="DropDownBlock"  Style="{DynamicResource DropDownBlock}">
            <Border Style="{DynamicResource DropDownBorder}" BorderThickness="1" BorderBrush="#ffcccccc">
                <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#ffffffff">
                    <ListBox Name="DropDownListBox" ItemContainerStyle="{DynamicResource DropDownListBox}" Visibility="Collapsed" />
                    <DataGrid Name="DropDownGridBox"  Style="{DynamicResource DataGridMain}" AutoGenerateColumns="False" Visibility="Collapsed" VerticalScrollBarVisibility="Visible" >
                    </DataGrid>
                    <Thumb HorizontalAlignment="Right" VerticalAlignment="Bottom" Width="16" Height="16" Name="DropDownThumb" Style="{DynamicResource DropDownThumb}" DragStarted="OnDragStarted" DragDelta="OnDragDelta" DragCompleted="OnDragCompleted" />
                </Grid>
            </Border>
        </Popup>
        
    </Grid>

</UserControl>
