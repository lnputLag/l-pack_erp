<UserControl x:Class="Client.Interfaces.Main.ScannerInputControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Client.Interfaces.Main"
             mc:Ignorable="d" >
    
    <Border d:Background="White">
        <Grid Margin="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <Border x:Name="ScannerImageBorder" Grid.Column="0" Grid.Row="0">
                <Image x:Name="ScannerImage" Style="{DynamicResource ScannerImage}"/>
            </Border>

            <Border x:Name="BarcodeTextBoxBorder" Grid.Column="1" Grid.Row="0">
                <TextBox x:Name="BarcodeTextBox" Width="100" TextWrapping="NoWrap" Style="{DynamicResource FormField}"/>
            </Border>
        </Grid>
    </Border>
</UserControl>
