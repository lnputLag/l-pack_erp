<Window 
    x:Class="Client.Interfaces.Main.LoginWindow"
    xmlns:local="clr-namespace:Client.Interfaces.Main"        
             
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"    
    
    mc:Ignorable="d" 
    VerticalAlignment="Center"
    HorizontalAlignment="Center"
    Title="Л-Пак ERP. Версия: 11.4.11.22. Сервер: 192.168.3.204" 
    Width="520"     
    Height="161" 
    ResizeMode="NoResize"
    Topmost="False"
    Style="{DynamicResource WindowCanvas}"         
>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <!--
        простая форма
        1 строка: блок с полями, все остальное пространство
        2 строка: кнопки, 30 пикс
    -->
    <Border Style="{DynamicResource FormContainerMHolder}" >
        <Grid Style="{DynamicResource FormContainer}" >

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>                
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="25"></RowDefinition>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"  Grid.Column="0" Style="{DynamicResource FromGrid}">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="7*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Grid.Column="0" Style="{DynamicResource FormLabelContainer}">
                    <Label Content="Логин: *"  Name="LoginLabel"   Style="{DynamicResource FormLabel}" />
                </Border>
                <Border Grid.Row="0"  Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                    <TextBox  Name="LoginTextBox" TextWrapping="NoWrap" Style="{DynamicResource FormField}"  MaxLength="64"  TabIndex="1" Text="test_ab"  />
                </Border>

                <Border Grid.Row="1" Grid.Column="0" Style="{DynamicResource FormLabelContainer}">
                    <Label Content="Пароль: *"    Style="{DynamicResource FormLabel}" />
                </Border>
                <Border Grid.Row="1"  Grid.Column="1" Style="{DynamicResource FormFieldContainer}">
                    <PasswordBox  Name="PasswordTextBox"  Password="1234" TabIndex="2"  Style="{DynamicResource FormField}" KeyUp="Password_KeyUp" />
                </Border>

                <Border Grid.Row="2" Grid.Column="1" Style="{DynamicResource FormLabelContainer}">
                    <CheckBox Name="SaveLogin" Content="Сохранить логин" Style="{DynamicResource CheckBoxTopPanel}"/>
                </Border>

            </Grid>

            <Grid Grid.Row="1"  Grid.Column="0" Style="{DynamicResource FormControlPanel}" >

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid  Grid.Row="0" Grid.Column="0">
                    <TextBlock  Name="FormStatus" TextAlignment="Right" Style="{DynamicResource FStatusBar2}" Text="Неверный логин"  />
                </Grid>

                <StackPanel Grid.Row="0" Grid.Column="2" Style="{DynamicResource FormHeaderButtonsPanel}"  >
                    <Button Content="Войти" Margin="0,0,0,0"   Style="{DynamicResource FButtonPrimary}" ToolTip="Войти (Ctrl+Enter)" Click="Enter_OnClick" TabIndex="3" />
                </StackPanel>

            </Grid>

        </Grid>
    </Border>



</Window>
