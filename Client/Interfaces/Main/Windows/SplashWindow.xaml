<Window 
    x:Class="Client.Interfaces.Main.SplashWindow"
    xmlns:local="clr-namespace:Client.Interfaces.Main"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"        
    
    mc:Ignorable="d"
    VerticalAlignment="Center"
    HorizontalAlignment="Center"
    Title="" 
    Width="520"     
    Height="250" 
    ResizeMode="NoResize"
    Topmost="True"
    Style="{DynamicResource WindowCanvas}"     
>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <!--
        простая форма
        1 строка: блок с полями, все остальное пространство
        2 строка: кнопки, 30 пикс
    -->
    <Border Style="{DynamicResource FormContainerMHolder}" >
        <Grid Style="{DynamicResource FormContainer}" >

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"  Grid.Column="0" Style="{DynamicResource FromGrid}" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="178"/>
                    <ColumnDefinition Width="Auto" MinWidth="107"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                    <RowDefinition Height="Auto"></RowDefinition>
                </Grid.RowDefinitions>
                <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
                    <Image 
                         Source="/l-pack_erp;component/Assets/Images/l-pack_erp_splash2.png" 
                         RenderOptions.BitmapScalingMode="Linear"
                         VerticalAlignment="Top" Margin="0,0,0,0" 
                         HorizontalAlignment="Left" 
                         Width="319" Height="49"   
                         >
                    </Image>
                </Border>
                <Border Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3">
                    <StackPanel Orientation="Vertical" Margin="28,20,28,20">
                        <TextBlock Name="TextWaiting" Text="Пожалуйста, подождите."  FontSize="12" LineHeight="14" Margin="0" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                        <TextBlock Name="TextDescription" Text="Программа запускается." FontSize="12" LineHeight="14" Margin="0,2,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" />
                        <TextBlock Name="TextStatus" Text="" FontSize="12" LineHeight="14" Margin="0,2,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" />

                        <Border Name="ProgressContainer" Visibility="Collapsed" Margin="0,12,0,0" HorizontalAlignment="Stretch">
                            <Grid x:Name="ProgressBarLayout" Width="430" HorizontalAlignment="Left">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Name="TextProgress" Text="" FontSize="12" LineHeight="14" Margin="0,0,0,6" HorizontalAlignment="Stretch" VerticalAlignment="Top" Foreground="#333333" TextWrapping="Wrap" />
                                <ProgressBar Grid.Row="1" Name="UpdateProgressBar" Height="18" Minimum="0" Maximum="100" Value="0" Foreground="#0C79CA" BorderBrush="#CCCCCC" BorderThickness="1" HorizontalAlignment="Stretch" MinWidth="430" MaxWidth="430" />
                            </Grid>
                        </Border>
                    </StackPanel>
                </Border>
            </Grid>

        </Grid>
    </Border>

</Window>
