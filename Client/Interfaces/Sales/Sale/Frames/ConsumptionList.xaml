<UserControl 
    x:Class="Client.Interfaces.Sales.ConsumptionList"
    xmlns:local="clr-namespace:Client.Interfaces.Sales"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"         
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"    
    xmlns:s="clr-namespace:System;assembly=mscorlib"

    mc:Ignorable="d" 
    d:DesignWidth="1600" d:DesignHeight="800"  
    Style="{DynamicResource UserControlCanvas}"  >

    <UserControl.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>


    <Border Style="{DynamicResource DataGridContainerHolder}">
        <Grid Style="{DynamicResource DataGridContainer}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Grid.Column="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0"  Grid.Column="0" Style="{DynamicResource GridControlPanel}">
                    <Border>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="11.5*"/>
                                <ColumnDefinition Width="0.5*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"></RowDefinition>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Grid.Column="0" Name="FormToolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left">
                                <Button Name="SaveButton" Tag="access_mode_full_access"   Content="Сохранить"  Style="{DynamicResource Button}" Click="SaveButton_Click"/>
                                <Button Name="CloseButton"    Content="Отмена"  Style="{DynamicResource Button}" Click="CloseButton_Click"/>
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                                <TextBlock  Name="StatusTextBox" TextAlignment="Left" Style="{DynamicResource FStatusBar2}" HorizontalAlignment="Stretch" />
                            </StackPanel>

                            <StackPanel Grid.Row="0" Grid.Column="1" Style="{DynamicResource FormHeaderButtonsPanel}"  HorizontalAlignment="Right" >
                                <Button Name="HelpButton" ToolTip="Документация" Width="24" MinWidth="24"  Content="?"  Style="{DynamicResource Button}" Click="HelpButton_Click"  Margin="0,0,0,0" />
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>

                <Border Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,5,0,5" Style="{DynamicResource FormVerticalPanelSpacer}"/>
                
                <Grid Grid.Row="2" Grid.Column="0" Name="MainForm">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Grid.Column="0" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="Счёт:" Padding="0" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,0,7,0" />
                    </Border>
                    <Border Grid.Row="0" Grid.Column="1">
                        <Grid Style="{DynamicResource FormFieldDateHolder}">
                            <controls:DatePicker x:Name="ReceiptDateTextBox" Style="{DynamicResource DateEditStyle}" TextChanged="ReceiptDateTextBox_TextChanged" Text="20.10.2023">
                            </controls:DatePicker>
                        </Grid>
                    </Border>
                    <Border Grid.Column="2" Grid.Row="0" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="ТН:" Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Column="3" Grid.Row="0" Style="{DynamicResource FormFieldContainer}">
                        <TextBox Name="WaybillTextBox" TextWrapping="NoWrap" Style="{DynamicResource FormField}" Width="60"/>
                    </Border>
                    <Border Grid.Column="4" Grid.Row="0" Style="{DynamicResource FormFieldContainer}">
                        <CheckBox Name="DefectCheckBox" Checked="DefectCheckBox_Checked" Unchecked="DefectCheckBox_Unchecked" HorizontalAlignment="Right"  Style="{DynamicResource FormCheckBox}"/>
                    </Border>
                    <Border Grid.Column="5" Grid.Row="0" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="Брак" HorizontalAlignment="Left" Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Column="6" Grid.Row="0" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="Покупатель:" Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Column="7" Grid.Row="0" Style="{DynamicResource FormFieldContainer}">
                        <StackPanel Orientation="Horizontal">
                            <TextBox Name="BuyerNameTextBox" TextWrapping="NoWrap" Style="{DynamicResource FormField}" Width="300"/>
                            <Button Name="SelectBuyerButton" Tag="access_mode_full_access" Click="SelectBuyerButton_Click"  ToolTip="Выбрать покупателя" Style="{DynamicResource ButtonGlyph}"  Margin="0,0,0,0"  >
                                <Image Style="{DynamicResource SelectImage}" />
                            </Button>
                            <TextBox Name="BuyerIdTextBox" TextChanged="BuyerIdTextBox_TextChanged" TextWrapping="NoWrap" Visibility="Collapsed" Style="{DynamicResource FormField}" Width="60"/>
                        </StackPanel>
                    </Border>
                    <Border Grid.Column="8" Grid.Row="0" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="Смена:" Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Column="9" Grid.Row="0" Style="{DynamicResource FormFieldContainer}">
                        <controls:SelectBox  x:Name="ShiftSelectBox" Style="{DynamicResource CustomFormField}" Width="140" Autocomplete="True" HorizontalAlignment="Left" />
                    </Border>
                    <Border Grid.Column="10" Grid.Row="0" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="Комментарий:" Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Column="11" Grid.Row="0" Grid.RowSpan="2" Style="{DynamicResource FormFieldContainer}">
                        <TextBox Name="CommentTextBox" Width="200" Height="48" TextWrapping="Wrap" Style="{DynamicResource FormField}" />
                    </Border>
                    

                    <Border Grid.Row="1" Grid.Column="0" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="УПД:" Padding="0" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,0,7,0" />
                    </Border>
                    <Border Grid.Row="1" Grid.Column="1">
                        <Grid Style="{DynamicResource FormFieldDateHolder}">
                            <!--<TextBox Name="InvoiceDateTextBox" MaxLength="10" IsReadOnly="True" Style="{DynamicResource FormFieldDate}" Text="20.10.2023" />
                            <DatePicker Name="InvoiceDateDatePicker" SelectedDate="{Binding Path=Text, Mode=TwoWay, ElementName=InvoiceDateTextBox, Converter={StaticResource DateDate} }"  Style="{DynamicResource FormFieldDP}"  Margin="0,2,0,0">
                            </DatePicker>-->

                            <controls:DatePicker x:Name="InvoiceDateTextBox" Style="{DynamicResource DateEditStyle}" Text="20.10.2023">
                            </controls:DatePicker>
                        </Grid>
                    </Border>
                    <Border Grid.Column="2" Grid.Row="1" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="УПД:" Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Column="3" Grid.Row="1" Style="{DynamicResource FormFieldContainer}">
                        <TextBox Name="InvoiceTextBox" TextWrapping="NoWrap" Style="{DynamicResource FormField}" Width="60"/>
                    </Border>
                    <Border Grid.Column="4" Grid.Row="1" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="Продавец:" Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Column="5" Grid.Row="1" Style="{DynamicResource FormFieldContainer}">
                        <controls:SelectBox  x:Name="SellerSelectBox" SelectedItemChanged="SellerSelectBox_SelectedItemChanged"  Style="{DynamicResource CustomFormField}" ListBoxMinHeight="65" Width="235" Autocomplete="True" HorizontalAlignment="Left" />
                    </Border>
                    <Border Grid.Column="6" Grid.Row="1" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="Грузополучатель:" Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Column="7" Grid.Row="1" Style="{DynamicResource FormFieldContainer}">
                        <StackPanel Orientation="Horizontal">
                            <TextBox Name="ConsigneeNameTextBox" TextWrapping="NoWrap" Style="{DynamicResource FormField}" Width="300"/>
                            <Button Name="SelectConsigneeButton" Tag="access_mode_full_access" Click="SelectConsigneeButton_Click"  ToolTip="Выбрать грузополучателя" Style="{DynamicResource ButtonGlyph}"  Margin="0,0,0,0"  >
                                <Image Style="{DynamicResource SelectImage}" />
                            </Button>
                            <TextBox Name="ConsigneeIdTextBox" TextWrapping="NoWrap" Visibility="Collapsed" Style="{DynamicResource FormField}" Width="60"/>
                        </StackPanel>
                    </Border>
                    <Border Grid.Column="8" Grid.Row="1" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="Площадка:" Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Column="9" Grid.Row="1" Style="{DynamicResource FormFieldContainer}">
                        <controls:SelectBox  x:Name="FactorySelectBox" Style="{DynamicResource CustomFormField}" Width="140" Autocomplete="True" HorizontalAlignment="Left" />
                    </Border>


                    <Border Grid.Row="2" Grid.Column="0" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="Отгружено:" Padding="0" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,0,7,0" />
                    </Border>
                    <Border Grid.Row="2" Grid.Column="1">
                        <Grid Style="{DynamicResource FormFieldDateHolder}">
                            <!--<TextBox Name="ShippingDateTextBox" TextChanged="ShippingDateTextBox_TextChanged" MaxLength="10" IsReadOnly="True" Style="{DynamicResource FormFieldDate}" Text="20.10.2023" />
                            <DatePicker Name="ShippingDateDatePicker" SelectedDate="{Binding Path=Text, Mode=TwoWay, ElementName=ShippingDateTextBox, Converter={StaticResource DateDate} }"  Style="{DynamicResource FormFieldDP}"  Margin="0,2,0,0">
                            </DatePicker>-->

                            <controls:DatePicker x:Name="ShippingDateTextBox" Style="{DynamicResource DateEditStyle}" Text="20.10.2023" TextChanged="ShippingDateTextBox_TextChanged">
                            </controls:DatePicker>
                        </Grid>
                    </Border>
                    <Border Grid.Column="2" Grid.Row="2" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="ТТН:" Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Column="3" Grid.Row="2" Style="{DynamicResource FormFieldContainer}">
                        <TextBox Name="ConsignmentNoteTextBox" TextWrapping="NoWrap" Style="{DynamicResource FormField}" Width="60"/>
                    </Border>
                    <Border Grid.Column="4" Grid.Row="2" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="Грузоотправитель:" Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Column="5" Grid.Row="2" Style="{DynamicResource FormFieldContainer}">
                        <controls:SelectBox  x:Name="ShipperSelectBox" Style="{DynamicResource CustomFormField}" ListBoxMinHeight="85" Width="235" Autocomplete="True" HorizontalAlignment="Left" />
                    </Border>
                    <Border Grid.Column="6" Grid.Row="2" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="Договор:" Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Column="7" Grid.Row="2" Style="{DynamicResource FormFieldContainer}">
                        <controls:SelectBox  x:Name="ContractSelectBox" Style="{DynamicResource CustomFormField}" SelectedItemChanged="ContractSelectBox_SelectedItemChanged"  Width="324" Autocomplete="True" HorizontalAlignment="Left" />
                    </Border>
                    <Border Grid.Column="8" Grid.Row="2" Style="{DynamicResource FormLabelContainer}">
                        <Label Content="Корректировка:" Style="{DynamicResource FormLabel}" />
                    </Border>
                    <Border Grid.Column="9" Grid.Row="2" Grid.ColumnSpan="2">
                        <StackPanel Orientation="Horizontal">
                            <Border Grid.Column="9" Grid.Row="2" Style="{DynamicResource FormFieldContainer}">
                                <TextBox Name="AdjustmentNumberTextBox" TextWrapping="NoWrap" IsReadOnly="True" Style="{DynamicResource FormField}" Width="60"/>
                            </Border>
                            <Border Grid.Column="10" Grid.Row="2" Style="{DynamicResource FormLabelContainer}">
                                <Label Content="от:" Style="{DynamicResource FormLabel}" />
                            </Border>
                            <Border Grid.Column="11" Grid.Row="2" Style="{DynamicResource FormFieldContainer}">
                                <TextBox Name="AdjustmentDateTextBox" TextWrapping="NoWrap" IsReadOnly="True" Style="{DynamicResource FormField}" Width="70"/>
                            </Border>
                        </StackPanel>
                    </Border>
                   
                </Grid>
            </Grid>

            <Border Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,5,0,5" Style="{DynamicResource FormVerticalPanelSpacer}"/>

            <Grid Grid.Row="2"  Grid.Column="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0"  Grid.Column="0" Style="{DynamicResource GridControlPanel}">
                    <Border>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="11.5*"/>
                                <ColumnDefinition Width="0.5*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="auto"></RowDefinition>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Grid.Column="0" Name="GridToolbar" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left">
                                <Button Name="RefreshButton"    Content="Обновить"  Style="{DynamicResource Button}" Click="RefreshButton_Click"/>
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                                <Button Name="AddConsumptionButton" Tag="access_mode_full_access"  Content="Добавить" ToolTip="Добавить позицию"  Style="{DynamicResource Button}" Click="AddConsumptionButton_Click"/>
                                <Button Name="EditConsumptionButton" Tag="access_mode_full_access"   Content="Изменить" ToolTip="Изменить выбранную позицию"  Style="{DynamicResource Button}" Click="EditConsumptionButton_Click"/>
                                <Button Name="DeleteConsumptionButton" Tag="access_mode_full_access"   Content="Удалить" ToolTip="Удалить выбранные позиции"  Style="{DynamicResource Button}" Click="DeleteConsumptionButton_Click"/>
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                                <Button Name="ConfirmSelectedConsumptionButton" Tag="access_mode_full_access"   Content="Провести" ToolTip="Провести выбранные позиции" Style="{DynamicResource Button}" Click="ConfirmSelectedConsumptionButton_Click"/>
                                <Button Name="UnconfirmSelectedConsumptionButton" Tag="access_mode_full_access"   Content="Отменить проведение" ToolTip="Отменить проведение выбранных позиций"  Style="{DynamicResource Button}" Click="UnconfirmSelectedConsumptionButton_Click"/>
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                                <Button Name="MoveToOtherDocumentSelectedConsumptionButton" Tag="access_mode_full_access"   Content="Перенести" ToolTip="Перенести в другую накладную выбранные позиции"  Style="{DynamicResource Button}" Click="MoveToOtherDocumentSelectedConsumptionButton_Click"/>
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                                <Button Name="AddVirtualConsumptionButton" Tag="access_mode_special" Content="Виртуальный поддон" ToolTip="Добавить виртуальный поддон" Style="{DynamicResource Button}" Click="AddVirtualConsumptionButton_Click" />
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                                <Button Name="AdjustmentButton"    Content="Корректировка"  Style="{DynamicResource Button}" Click="AdjustmentButton_Click"/>
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                                <Button Name="PrintDocumentButton"    Content="Печать" Style="{DynamicResource Button}" Click="PrintDocumentButton_Click"/>
                                <Button Name="UploadWebDocumentButton"    Content="Выгрузить в Web"  Style="{DynamicResource Button}" Click="UploadWebDocumentButton_Click" />
                                <Button Name="PrintLabelButton"    Content="Ярлык" ToolTip="Распечатать ярлыки на выбранные позиции" Style="{DynamicResource Button}" Click="PrintLabelButton_Click"/>
                                <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                                <Button Name="UpdatePayDebtBuyerButton" Tag="access_mode_special"   Content="Пересчитать оплату" Style="{DynamicResource Button}" Click="UpdatePayDebtBuyerButton_Click" />

                                <Label Content="Поиск:"  Style="{DynamicResource TopLabel}" />
                                <TextBox Style="{DynamicResource FormFieldTopPanel}" Width="60" TextWrapping="NoWrap" Name="SearchText" Text=""/>
                                <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>

                <Grid Grid.Row="1"  Grid.Column="0">
                    <Border Style="{DynamicResource DataGridMainHolder}" >
                        <controls:GridBox4 x:Name="Grid" />
                    </Border>
                </Grid>
            </Grid>
        </Grid>
    </Border>

</UserControl>
