<controls:ControlBase x:Class="Client.Interfaces.Sales.ConsumptionEdit"
             xmlns:local="clr-namespace:Client.Interfaces.Sales"
             xmlns:controls="clr-namespace:Client.Interfaces.Main"
             xmlns:main="clr-namespace:Client.Interfaces.Main"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             MinHeight="650" MinWidth="750"
             Style="{DynamicResource UserControlCanvas}">

    <UserControl.Resources>
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{DynamicResource DataGridContainerHolder}">
        <Grid Style="{DynamicResource DataGridContainer}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <!--FormToolbar-->
            <Grid Grid.Row="0" Grid.Column="0" Style="{DynamicResource GridControlPanel}">
                <Border>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="11*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"></RowDefinition>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left">
                            <Button Name="RefreshButton"    Content="Обновить"  Style="{DynamicResource Button}" Margin="5,0,5,0" Click="RefreshButton_Click"/>
                            <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>

                            <Label Name="SearchLabel" Content="Поиск:"  Style="{DynamicResource TopLabel}" />
                            <TextBox Style="{DynamicResource FormFieldTopPanel}" Width="60" TextWrapping="NoWrap" Name="SearchText" Text=""/>
                            <Border Name="SearchBorder" Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Style="{DynamicResource FormHeaderButtonsPanelR}" >
                            <Button Name="HelpButton" Click="HelpButton_Click" ToolTip="Документация" Width="24" MinWidth="24"  Content="?"  Style="{DynamicResource Button}"  />
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>

            <Border Grid.Row="1" Grid.Column="0" Style="{DynamicResource FormVerticalPanelSpacer}"/>

            <!--MainGrid-->
            <Grid Grid.Row="2" Grid.Column="0" Margin="5,0,5,0" HorizontalAlignment="Center" Name="MainGrid">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Column="0" Grid.Row="0" Style="{DynamicResource FormLabelContainer}">
                    <Label Content="Продукция:" Style="{DynamicResource FormLabel}" />
                </Border>
                <Border Grid.Column="1" Grid.ColumnSpan="3" Grid.Row="0" Style="{DynamicResource FormFieldContainer}">
                    <TextBox Name="ProductNameTextBox" IsReadOnly="True" TextWrapping="NoWrap" Style="{DynamicResource FormField}" />
                </Border>

                <Border Grid.Column="0" Grid.Row="1" Style="{DynamicResource FormLabelContainer}">
                    <Label Content="Артикул:" Style="{DynamicResource FormLabel}" />
                </Border>
                <Border Grid.Column="1" Grid.Row="1" Style="{DynamicResource FormFieldContainer}">
                    <TextBox Name="ProductCodeTextBox" IsReadOnly="True" TextWrapping="NoWrap" Style="{DynamicResource FormField}" Width="120"/>
                </Border>

                <Border Grid.Column="0" Grid.Row="2" Style="{DynamicResource FormLabelContainer}">
                    <Label Content="Код:" Style="{DynamicResource FormLabel}" />
                </Border>
                <Border Grid.Column="1" Grid.Row="2" Style="{DynamicResource FormFieldContainer}">
                    <TextBox Name="ProductKodTextBox" IsReadOnly="True" TextWrapping="NoWrap" Style="{DynamicResource FormField}" Width="120"/>
                </Border>

                <Border Grid.Column="2" Grid.Row="1" Style="{DynamicResource FormLabelContainer}">
                    <Label Content="Количество:" Style="{DynamicResource FormLabel}" />
                </Border>
                <Border Grid.Column="3" Grid.Row="1" Style="{DynamicResource FormFieldContainer}">
                    <TextBox Name="QuantityTextBox" TextWrapping="NoWrap" Style="{DynamicResource FormField}" Width="120"/>
                </Border>

                <Border Grid.Column="2" Grid.Row="2" Style="{DynamicResource FormLabelContainer}">
                    <Label Content="Цена:" Style="{DynamicResource FormLabel}" />
                </Border>
                <Border Grid.Column="3" Grid.Row="2" Style="{DynamicResource FormFieldContainer}">
                    <TextBox Name="PriceTextBox" TextWrapping="NoWrap" Style="{DynamicResource FormField}" Width="120"/>
                </Border>

                <Border x:Name="AddressLabelBorder" Grid.Column="0" Grid.Row="3" Style="{DynamicResource FormLabelContainer}">
                    <Label Content="Адрес:" Style="{DynamicResource FormLabel}" />
                </Border>
                <Border x:Name="AddressSelectBoxBorder" Grid.Column="1" Grid.Row="3" Grid.ColumnSpan="3" Style="{DynamicResource FormFieldContainer}">
                    <controls:SelectBox  x:Name="AddressSelectBox" Style="{DynamicResource CustomFormField}" Autocomplete="False" />
                </Border>

                <Border Name="PriceForAllBorder" Grid.Column="0" Grid.Row="4" Grid.ColumnSpan="4">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Border Grid.Column="0" Grid.Row="3" Style="{DynamicResource FormFieldContainer}">
                            <CheckBox Name="PriceForAllCheckBox" HorizontalAlignment="Right"  Style="{DynamicResource FormCheckBox}"/>
                        </Border>
                        <Border Grid.Column="1" Grid.Row="3" Style="{DynamicResource FormLabelContainer}">
                            <Label Name="PriceForAllLabel" Content="Применить цену ко всем позициям" HorizontalAlignment="Left" Style="{DynamicResource FormLabel}" />
                        </Border>
                    </StackPanel>
                </Border>

            </Grid>

            <Border Name="PositionGridBorder" Grid.Row="3" Grid.Column="0" Margin="5,0,5,0" Style="{DynamicResource DataGridMainHolder}" >
                <controls:GridBox3 x:Name="ProductGrid" />
            </Border>

            <Border Grid.Row="4" Grid.Column="0" Style="{DynamicResource FormVerticalPanelSpacer}"/>

            <Grid Grid.Row="5" Grid.Column="0" Style="{DynamicResource GridControlPanel}">
                <Border>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"></RowDefinition>
                        </Grid.RowDefinitions>

                        <StackPanel Name="FormToolbar" Grid.Row="0" Grid.Column="0" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Right">
                            <Button Name="SaveButton" Content="Сохранить" Click="SaveButton_Click"  Style="{DynamicResource FButtonPrimary}" />
                            <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
                            <Button Name="CancelButton" Content="Отмена" Click="CancelButton_Click"  Style="{DynamicResource Button}" />
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Border>

</controls:ControlBase>