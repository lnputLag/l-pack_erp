<UserControl 
    x:Class="Client.Interfaces.Stock.OperationWriteOffList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:main="clr-namespace:Client.Interfaces.Main"
    xmlns:controls="clr-namespace:Client.Interfaces.Main"
    mc:Ignorable="d" 
    d:DesignHeight="450" d:DesignWidth="800"
    Style="{DynamicResource UserControlCanvas}"  
    >

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/l-pack_erp;component/Assets/Styles/Common.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Style="{DynamicResource DataGridContainerHolder}"  >
        <Grid Style="{DynamicResource DataGridContainer}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"  Grid.Column="0" Style="{DynamicResource GridControlPanel}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="11.5*"/>
                    <ColumnDefinition Width="0.5*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"></RowDefinition>
                </Grid.RowDefinitions>

                <StackPanel Name="GridToolbar" Grid.Row="0" Grid.Column="0" Style="{DynamicResource FormHeaderButtonsPanel}" HorizontalAlignment="Left"  >
                    <Label Content="С:"  Style="{DynamicResource TopLabel}"/>
                    <Grid Style="{DynamicResource FormFieldDateHolder}">
                        <controls:DatePicker x:Name="FromDate" Style="{DynamicResource DateEditStyle}" >
                            <controls:DatePicker.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="Текущие сутки" Click ="OnCurrentDay" />
                                    <MenuItem Header="Предыдущие сутки" Click="OnPrevDay" />
                                    <MenuItem Header="Текущая неделя" Click="OnCurrentWeek"/>
                                    <MenuItem Header="Предыдущая неделя" Click="OnPrevWeek"/>
                                    <MenuItem Header="Текущий месяц" Click="OnCurrentMonth"/>
                                    <MenuItem Header="Предыдущий месяц" Click="OnPrevMonth"/>
                                </ContextMenu>
                            </controls:DatePicker.ContextMenu>
                        </controls:DatePicker>
                    </Grid>
                    <Label Content="По:"  Style="{DynamicResource TopLabel}"/>
                    <Grid Style="{DynamicResource FormFieldDateHolder}">
                        <controls:DatePicker x:Name="ToDate" Style="{DynamicResource DateEditStyle}" >
                        </controls:DatePicker>
                    </Grid>
                    <Button Name="ResreshButton"    Content="Показать"  Style="{DynamicResource Button}" Click="ShowButton_Click"   />
                    <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>

                    <Label Content="Поиск:"  Style="{DynamicResource TopLabel}" />
                    <TextBox Style="{DynamicResource FormFieldTopPanel}" Width="60" TextWrapping="NoWrap" Name="SearchText" Text=""  />
                    <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>

                    <controls:SelectBox  x:Name="ConsumptionZoneSelectBox" SelectedItemChanged="ConsumptionZoneSelectBox_SelectedItemChanged"  Style="{DynamicResource CustomFormField}" ListBoxMinHeight="110" Width="120" Autocomplete="False"  />
                    <Border Style="{DynamicResource FormButtonsPanelSpacer}"></Border>
                    <controls:SelectBox x:Name="FactorySelectBox" SelectedItemChanged="FactorySelectBox_SelectedItemChanged"  Style="{DynamicResource CustomFormField}" ListBoxMinHeight="80" Width="120" Autocomplete="False"/>
                    <Border Style="{DynamicResource FormButtonsPanelDoubleSpacer}"></Border>
                    
                    <Button     Content="В Excel"  Style="{DynamicResource Button}" Click="ExportToExcelButton_Click" />
                    <Border Style="{DynamicResource FormButtonsPanelSpacer}"/>
                </StackPanel>

                <StackPanel Grid.Row="0" Grid.Column="1" Style="{DynamicResource FormHeaderButtonsPanel}"  HorizontalAlignment="Right" >
                    <Button Name="HelpButton"  Width="24" MinWidth="24"  Content="?"  Style="{DynamicResource Button}" Click="HelpButton_Click" Margin="0" />
                </StackPanel>
            </Grid>

            <Border Grid.Row="1"  Style="{DynamicResource DataGridMainHolder}">
                <main:GridBox x:Name="Grid" />
            </Border>
        </Grid>
    </Border>
</UserControl>
